# AGENTS.md — LawEverything 프로젝트 메모리

> lazycodex `$init-deep`가 소비하는 계층적 프로젝트 메모리. 이후 Codex/Claude로 이어서 개발할 때 컨텍스트 앵커.

## 한 줄 정의
조달청 실무자용 **법령 즉시검색 + 위임·인용 관계 그래프**. 내부망(폐쇄망)에서 오프라인 동작이 절대 제약.

## 불변 원칙 (깨지 말 것)
1. **런타임 외부 의존성 0** — 산출물은 자족형 `web/index.html` 하나. CDN·API·폰트 외부 호출 금지.
2. **런타임 AI 호출 금지** — 관계는 규칙(정규식+상태머신)으로 도출. LLM/오픈API는 *빌드 단계에서만*.
3. **데이터 수집(배치) ↔ 관계 계산 분리** — 폐쇄망이라 실시간 API 불가. 온라인 빌드→스냅샷 반입→오프라인 조회.
4. **재사용 우선** — korean-law-mcp(데이터/그래프), kordoc(HWP 파싱)을 부품으로. 엔진 재발명 금지.

## 아키텍처
`data/*.json (스냅샷)` → `src/extract.mjs (관계추출)` → `src/build.mjs + src/template.html` → `web/index.html`

## 도메인: 조달 법령군
국가계약법(법률/시행령/시행규칙) · 조달사업법(법률/시행령) · 전자조달법 + 계약예규(행정규칙, 예정).
외부 인용: 민법, 지방계약법 등 → 외부법 노드.

## 관계 모델
- 노드: `법령` / `조문` / `외부법`. 색상 = 가문(hue) × 유형(명도).
- 엣지: `위임`(법→시행령→규칙) · `인용`(조문↔조문) · `소속`(법령→조문).

## 현재 상태 (v0.1 프로토타입)
- ✅ 규칙기반 추출: 위임 5 · 인용 12 (샘플 6법령 21노드).
- ✅ 오프라인 UI: 즉시검색, 포스그래프, 클릭 상세(향하는 곳/가리키는 것), 유형 토글.
- ⏳ 실데이터 미연결(샘플). kordoc 문서연동·개정추적 미착수.

## 다음 작업 후보
1. 빌드 단계를 오픈API/korean-law-mcp로 교체 (샘플→실데이터).
2. 문맥 인용 고도화(`이 법`, 항·호, `제N조부터 제M조까지`).
3. kordoc으로 HWP 계약/공고 → 관련 조문 링크.
4. 스냅샷 diff 기반 개정 임팩트 뷰.

## 검증 방법
`node src/extract.mjs` 통계 확인 → `node src/build.mjs` → `web/index.html` 헤드리스 렌더 스모크(노드/엣지 수, JS 에러 0, 클릭 상세, 검색).
