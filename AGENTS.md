# AGENTS.md — 몇 조항이더라 프로젝트 메모리

> lazycodex `$init-deep`가 소비하는 계층적 프로젝트 메모리. 이후 Codex/Claude로 이어서 개발할 때 컨텍스트 앵커.

## 한 줄 정의
조달청 실무자용 **법령 즉시검색 + 원문 열람** 도구. 관계 그래프는 원문을 읽다 근거로 점프하기 위한 **보조**.
내부망(폐쇄망)에서 오프라인 동작이 절대 제약.

## 제품 우선순위 (2026-07-21 재정의)
**검색과 원문이 본체, 그래프는 부차.** 실무자가 매일 하는 일은 관계 탐색이 아니라 "그 조문 어디 있더라"이다.

실측이 이 판단을 뒷받침한다:
- 전역 포스그래프는 1,354노드에서 **프레임당 916K 쌍 계산(55M ops/sec)** — 브라우저에서 돌지 않는다.
  게다가 1,354노드를 한 화면에 뿌리면 털뭉치라 정보가 0이다. → **국소 그래프(2홉·최대 48노드)로 축소**.
- 검색은 선형 스캔으로도 **타건당 2~3ms**. 속도는 병목이 아니었다.
  진짜 문제는 "수의계약" 한 번에 60건이 순위 없이 쏟아지는 것 → **랭킹이 핵심**.

## 불변 원칙 (깨지 말 것)
1. **런타임 외부 의존성 0** — 산출물은 반입 후 그 자체로 도는 물건. CDN·API·폰트 외부 호출 금지.
   *형태는 바뀐다*: Phase 1~2는 자족형 `web/index.html`, Phase 3에서 Tauri 데스크톱 앱으로 전환(확정).
   전환 사유는 원칙이 아니라 물리 — 실데이터가 원문+색인 15~25MB라 단일 HTML로 감당 불가 (docs/scope.md §5.4).
2. **런타임 AI 호출 금지** — 관계는 규칙(정규식+상태머신)으로 도출. AI는 빌드 단계의 *감사 도구*로만.
   근거: 관계망은 규칙으로 전부 도출된다는 것이 실데이터로 확인됨 (docs/scope.md §5.6).
   규칙 엣지는 원문 문자열을 근거로 제시할 수 있고, AI 추론 엣지는 못 한다 — 법령 도구에서 이건 결정적이다.
3. **데이터 수집(배치) ↔ 관계 계산 분리** — 폐쇄망이라 실시간 API 불가. 온라인 빌드→스냅샷 반입→오프라인 조회.
4. **재사용 우선** — 법제처 오픈API(빌드), korean-law MCP(탐색·검증), kordoc(HWP 파싱). 엔진 재발명 금지.
5. **수집 대상은 `data/seed.json` 이 단일 원천** — 법제처 검색 API가 쿼리당 20건 상한이라
   소관부처 기준 완전열거가 불가능하다. 시드는 수기 관리하고 누락은 별도로 감사한다 (docs/scope.md §5.1).
6. **낡음은 항상 눈에 보여야 한다** — 폐쇄망 데이터는 늘 과거다. 가장 위험한 실패는 틀린 답이 아니라
   *낡은 답을 최신인 것처럼 보여주는 것*. 확인일을 문서마다 기록하고 UI에 노출하며,
   시행예정본은 인용 대상에서 제외한다. 끊어낸 관계는 조용히 버리지 않고 감사에 남긴다 (docs/scope.md §5.7).
   **"현행"은 단정이 아니라 확인일 시점의 주장이다** — 경과일과 함께 제시할 것.

## 아키텍처
```
data/seed.json ──▶ src/collect.mjs ──▶ data/snapshot.json ──▶ src/extract.mjs ──▶ src/build.mjs ──▶ web/index.html
  (수집 대상)        법제처 오픈API        (원문 스냅샷)          (관계 추출)         + template.html      (산출물)
                     .cache/ 재사용                              └─▶ data/audit.json (커버리지·미매칭)
```
**MCP는 빌드에 못 쓴다** — 에이전트 세션에서만 접근 가능. 무인 월간 재빌드를 위해 빌드는 오픈API를 직접 호출한다.
MCP는 탐색·검증용으로만 쓴다 (docs/scope.md §5.2).

## 도메인: 조달 법령군
국가계약법(법률/시행령/시행규칙) · 조달사업법(법률/시행령) · 전자조달법 + 계약예규(행정규칙, 예정).
외부 인용: 민법, 지방계약법 등 → 외부법 노드.

## 관계 모델
- 노드: `법령` / `조문` / `외부법`. 색상 = 가문(hue) × 유형(명도).
- 엣지: `위임`(법→시행령→규칙) · `인용`(조문↔조문) · `소속`(법령→조문).

## 현재 상태 (v0.2 — Phase 1 진행중)
- ✅ **전체범위 탐색 완료** → `docs/scope.md`. 조달 핵심 30종 + 부처 교차 26개 법령(11개 부처) + 조달청 행정규칙 89종 확인.
- ✅ **수집 파이프라인**: `seed.json`(30종 식별자) + 법제처 오픈API 클라이언트 + 캐시.
- ✅ **추출 규칙 보강**: 연결형 위임 · 지역별칭 · 법률/시행령 표기 · 항호목 · 범위(내지) · 가지조문 · 같은 영 ·
  **행정규칙 제1조 역파싱으로 위임근거 도출**(Layer 3). 실원문 픽스처 기준 14개 테스트 통과.
- ✅ **실수집 완료** (2026-07-21) — 30/30 문서 · **1,223 조문** · 노드 1,354 · 엣지 3,403
  (위임 155 · 인용 2,070 · 소속 1,178). 오픈API 응답 스키마 추정이 맞아 첫 시도에 성공.
- ✅ **위임근거 커버리지 17/17 (100%)** — 조문단위 10 · 문서단위 7 (본문 11 · 시드 6).
  본문만으로는 11/17(64.7%)이고, 계약조건 문서 6건은 텍스트에 근거가 양방향 모두 없어 시드 수기 매핑(docs/scope.md §5.6).
- 📦 산출물 `web/index.html` **1.7MB** (30종 기준) → Phase 3(200종) 추정 15~25MB 검증됨.
- ✅ **UI 재편(스포틀라이트형)** — 검색이 첫 화면, Enter로 원문 전체 열람, 우측에 국소 그래프.
  원문 안의 인용 표기가 클릭 가능(`공사계약일반조건 제7조` → `시행령 제50조`로 점프). 브라우저 실동작 확인 완료.
- ✅ **검색 랭킹** — 조문제목 > 법령명 > 본문, 초성 검색(`ㅅㅇㄱㅇ`→수의계약),
  조번호 정규화(`7조`=`제7조`), 조번호+키워드 혼용(`7조 보증금` → 공사계약일반조건 제7조 계약보증금).
- ⏳ Phase 2(부처 교차 법령 스텁) · Phase 3(Tauri+네이티브 색인) · Phase 4(개정추적) 미착수.

## 로드맵 (docs/scope.md §6)
1. **Phase 1** 조달 핵심 30종 — 목표는 커버리지가 아니라 *추출 규칙 완성*. ← 현재
2. **Phase 2** 부처 교차 법령 26종 — 인용되는 조문만 노드화, 나머지 스텁(볼륨 1/5로 억제).
   산출물: `분리발주 3법` 뷰, `우선구매 경합` 뷰.
3. **Phase 3** 조달청 행정규칙 89~200종 — 볼륨 임계점. **Tauri 전환 + 네이티브 색인 + 전역 핫키.**
4. **Phase 4** 개정추적 — 시행예정본 diff (시행예정 MST 다수 확보됨).

## 핵심 지표
**위임근거 커버리지** = 행정규칙 중 제1조에서 근거 조문이 자동 도출된 비율.
이 숫자가 규칙기반 노선의 성패를 말해준다. `npm run build` 마지막 줄과 `data/audit.json` 에서 확인.
실데이터에서 60% 밑으로 나오면 보완책을 재논의할 것 (현재 픽스처 기준 2/2).

## 검증 방법
```bash
npm test                      # 추출 규칙 + 신선도 회귀 (23케이스)
npm run collect               # 실데이터 수집 (LAW_API_OC 필요)
npm run extract               # 통계 + 위임근거 커버리지 + 미매칭 목록
npm run build                 # web/index.html  (--strict: 낡은 법령 있으면 실패)
npm run smoke                 # 산출물 검사 — GRAPH 주입·확인일 메타·문법·외부의존성 0
```
`npm run smoke` 는 브라우저 없이 되는 것만 본다. 클릭 상세·검색·그래프 상호작용은 브라우저 스모크의 몫.
⚠️ 프리뷰 창은 프로젝트 밖 file:// 을 정적 스냅샷으로만 렌더해 JS가 돌지 않는다 — 상호작용 검증은 `npm run serve` 후 확인할 것.
