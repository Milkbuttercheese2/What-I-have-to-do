node_modules/
package-lock.json
web/graph.json
web/preview-*.png

# 빌드 산출물 — 매 빌드마다 재생성된다
data/audit.json

# 법제처 오픈API 자격증명 — 절대 커밋하지 말 것
.env

# 수집 캐시 (문서별 원본 응답). 재수집으로 항상 복원 가능.
.cache/

# 수집 산출물 — `npm run collect` 로 재생성 (실데이터는 반입 대상이 web/index.html 이다)
data/snapshot.json

# 별표 원본 파일 — `npm run annex` 로 재생성 (Tauri 배포 리소스, ~200MB)
web/annex/
data/collect-failures.json
