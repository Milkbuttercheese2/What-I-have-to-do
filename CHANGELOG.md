# 변경 이력 (CHANGELOG)

깃허브에 업데이트할 때마다 무엇이 바뀌었는지 이 파일에 기록한다.
버전 규칙: 표준 X.Y.Z(semver) — X=큰 개편, Y=기능 추가, Z=**기능이 아닌 로직 변경·디버깅·조정**(2026-08-02 소유자 지정).
**정책(소유자 지정): 사소한 수정이라도 배포(빌드/머지)할 때마다 패치(Z)를 올린다(v2.4.0 → v2.4.1 → …).**
(v2.31까지의 옛 규칙: 큰 업데이트 +0.1, 작은 업데이트 +0.01, 십진수 크기순 —
v2.2 → v2.21 → … → v2.3(=2.30) → v2.31. 즉 v2.3은 v2.23 다음(더 최신) 버전이다.)

> **⚠️ 버전 번호 안내(꼭 읽기):** 아래 v3.0.0·v3.1.0 항목은 *다른 계정에서 진행되던 미배포
> 개편 작업분*이며 **실제로 배포된 적이 없다.** 당시 파일 링크·이름 변경 정도의 변화를 두고
> 대규모 개편도 아닌데 버전을 3.0.0으로 **임의 상향(잘못된 범프)** 한 것이라, 소유자 결정으로
> 바로잡아 **구 v2.31 라인을 잇는 v2.4.0**으로 정식 배포한다. 즉 v3.x는 내부 이력일 뿐 실 배포
> 버전은 **v2.4.x(현재 v2.5.0)**이고, 다음은 v2.4.x/v2.5.0으로 이어간다(진짜 대규모 개편 전엔 3.x로 올리지 말 것).
> (자동 업데이터·버전 비교 로직이 없어 번호 역전은 무해하다.)

## v3.3.1 — 2026-08-02

### 수정 (데이터 안전)
- **"가끔 DB를 못 불러오는" 문제와 그로 인한 데이터 되돌아감 위험 제거.** DB 를 열 때
  `busy_timeout` 이 없어서, 앱을 껐다 바로 켜면(이전 프로세스가 아직 파일 잠금을 들고
  있는 찰나) 새 프로세스가 **기다리지 않고 즉시 실패**했다. 더 큰 문제는 그 다음이었다 —
  `open_with_recovery` 가 이 실패를 '파일 손상'으로 보고 **자동 백업으로 원본을 덮어썼다**.
  즉 멀쩡한 최신 데이터가 옛 백업으로 되돌아갈 수 있었다.
  - 잠금은 손상이 아니라 **대기** 대상이다 → `busy_timeout` 5초 설정.
  - 복구는 되돌릴 수 없으므로 **곧바로 넘어가지 않는다** → 0.4초 간격 4회 재시도 후,
    그래도 안 되는 '진짜' 실패만 복구 경로로 보낸다.
  - 회귀 테스트: `busy_timeout` 설정 확인 + 다른 연결이 DB 를 쥔 상태에서도 정상 오픈
    (복구 안내가 뜨지 않아야 한다).
- **로드 실패 안내를 '해볼 순서'로 개정** (소유자 지정) — 원인 나열 대신 ① 트레이
  아이콘 우클릭 [종료] 후 재실행 ② [설정] → 저장 위치 변경 ③ [설정] → JSON·DB 백업
  불러오기 순서로 안내한다. 저장 잠김(무결성 실패) 메시지도 같은 문구로 통일.

## v3.3.0 — 2026-08-02

### 변경
- **모든 알림·확인 창을 앱 UI로 통일** (소유자 지정) — 네이티브 `alert`/`confirm` 은
  제목이 `wmhh-desktop` 으로 뜨고 버튼·글꼴이 OS 것이라 앱과 따로 놀았다. 이제
  앱 표준 모달(`.modal-bg > .modal`) 문법을 그대로 쓰는 `appAlert`/`appConfirm`
  으로 39곳(알림 27 · 확인 12)을 교체했다. ESC·배경 클릭은 취소, Enter 는 확인.
  줄바꿈이 있는 문구는 그대로 문단으로 보인다.

## v3.2.7 — 2026-08-02

### 변경
- **미니 창(바로 검색·빠른 메모) 가로폭을 700px 로 고정** (소유자 지정). 창 설정과
  크기 조정 코드 두 곳을 같은 값으로 두어, 화면 전환·자동완성에도 폭은 변하지 않는다.

### 수정
- **패널이 왼쪽으로 치우쳐 좌우가 비대칭이던 문제** — v3.2.5 에서 '패널은 내용 높이'
  규칙을 넣으며 **가로까지 내용 폭**이 돼(좌 0 / 우 359px 실측) 패널이 창 왼쪽에
  붙어 있었다. 세로만 내용 높이가 되도록 바로잡아(세로 flex) 가로는 창을 꽉 채운다 —
  입력줄·힌트·목록 행의 좌우 여백이 모두 대칭이 됐다.

## v3.2.6 — 2026-08-02

### 수정
- **미니 창(바로 검색·빠른 메모) 가로폭이 늘었다 줄었다 하던 문제** — v3.1.0 에서
  창 설정(tauri.conf.json)의 폭만 560→620 으로 바뀌고 크기 조정 코드의 폭
  (`CAPTURE_W`)은 560 그대로여서, **창이 처음 뜰 땐 620, 크기가 한 번 조정되면 560**
  으로 끌려갔다. 예전 값 **560 으로 통일**(두 곳이 같아야 한다는 주석도 코드에 남김).

## v3.2.5 — 2026-08-02

### 수정
- **빠른 메모 자동완성 잘림 — 구조를 바꿔 원천 차단**. 지금까지는 '창 높이를 정확히
  맞추는' 싸움이었다(패널이 창을 꽉 채우니 몇 px만 어긋나도 행이 잘리거나 빈 띠).
  미니 창은 **투명 창**이므로, 패널이 창을 채울 이유가 없다 — 이제 **패널은 내용
  높이만** 차지하고 창에 남는 여백은 투명해 보이지 않는다. 창을 넉넉히 잡아도
  아무 문제가 없어 **잘림이 구조적으로 불가능**해졌다.
- **10행 상한을 목록 자체에 실측으로** — 창 여유 때문에 11·12행이 삐져나오던 것을
  막는다. 정확히 10행에서 멈추고 그 아래는 스크롤.
- 검증(실리사이즈 재현 하니스): 후보 1·3·10명 → 딱 그만큼 표시·스크롤 없음,
  13·30명 → 정확히 10행+스크롤. **창이 요청보다 30px 작게 적용되는 최악 환경**에서도
  1명 1행·13명 10행으로 정상(구조 덕에 흡수).

## v3.2.4 — 2026-08-02

### 수정
- **빠른 메모 자동완성 창 크기 자가 보정** — 높이를 계산해 넘기는 것으로 끝내면
  그 계산이 틀린 환경에서 잘림/빈 띠가 그대로 남는다(v3.2.1~v3.2.3이 그랬다).
  이제 창을 조정한 뒤 **실제로 목록이 넘치는지(잘림)·남는지(빈 띠)를 다시 재서
  그 차이만큼 한 번 더 조정**한다. 계산이 어긋나는 환경(배율·폰트 폴백·플랫폼)에서도
  결과가 수렴한다. 10행 초과는 스크롤이 정상이라 보정 대상에서 제외.

## v3.2.3 — 2026-08-02

### 수정
- **빠른 메모 @자동완성 창 높이를 '실측'으로 전환 (근본 수정)** — 지금까지는 상수
  공식(기본 높이 + 행수×33)으로 계산해, 행 마진·입력칸 확장·화면 배율·폰트 폴백 차이
  때문에 환경에 따라 **빈 띠**(v3.2.1 이전)와 **마지막 행 잘림**(v3.2.2)을 오갔다.
  이제 펴진 상태의 실제 픽셀(입력줄 + 힌트줄 + 첫 행 top→마지막 행 bottom + 목록
  패딩·테두리)을 재서 넘긴다 — 환경 변수들이 저절로 반영된다.
  검증(실렌더): 후보 1·2·3·10명에서 **목록 여유 0px**(빈 띠도 스크롤도 없음),
  10명 초과는 창이 10행에서 멈추고 목록만 스크롤.

## v3.2.2 — 2026-08-02

### 수정
- **빠른 메모 @자동완성 목록 아래 빈 띠 제거** — 창 높이 공식이 메모 모드 기본 높이
  (126, 입력칸 여유 슬랙 포함)에 목록 높이를 더해 실제보다 ~35px 크게 잡혔다.
  실제 소비량(입력줄 56 + 힌트 32 + 목록 패딩 12 + 행×33)으로 교정 — 실렌더 rect 로
  확정·검증. 10행 초과분은 그대로 목록 스크롤(v3.2.1 규칙 유지).

## v3.2.1 — 2026-08-02

### 수정
- **@태그 자동완성 후보를 최대 10개로** (소유자 지정) — 빠른 메모(미니 창)는 10행까지
  스크롤 없이 한눈에 보이도록 창 높이도 함께 늘어난다. 메인 창·Rust 검색과 한 값 통일.

## v3.2.1 — 2026-08-02

### 수정
- **@태그 자동완성: 10행까지 한눈에, 넘치면 스크롤** (소유자 지정) — 후보 목록을
  자르지 않고(상한 50), 미니 창은 후보 수만큼 창이 늘어나되 10행에서 멈추고 스크롤,
  메인 드롭다운도 10행 높이 상한 + 스크롤. ↑↓ 이동 시 선택 행 자동 추적.

## v3.2.0 — 2026-08-02

### 추가
- **하나의 전화번호 체계** (소유자 지정) — 관련인 연락처를 어디서 입력하든(전화번호부·
  양식 관련인 칸·엑셀·자동 연동) 저장 시 **표준 하이픈 표기**로 정규화한다
  (`phonebook-core.formatPhone`): 휴대폰 01X(3-4-4/구형 3-3-4) · 서울 02(2-4-4/2-3-4) ·
  지역 031~064·070(3-4-4/3-3-4) · 안심 050X(4-4-4/4-3-4) · 전국대표 15XX/16XX/18XX(4-4).
  **기존 저장분**(`01012345678` 등)도 로드 시 표준형으로 회복돼 다음 저장 때 영속된다.
  원칙은 보수적 파서 — **확실히 인식되는 번호만 바꾸고**, 내선·교환 표기("내선302"),
  국제번호(+82), 지역번호 없는 국번, 자리수 미달 등 '변수'는 **원문 그대로 보존**(유실 없음).
  중복 판정·검색은 원래 숫자만 비교라 영향 없음. (v2.5.1의 "입력 그대로 저장" 결정 개정.)

## v3.1.2 — 2026-08-02

### 수정
- **@태그 자동완성 후보를 최대 5개로** (소유자 지정) — 메인 창(바로 입력·양식)과
  미니 창 빠른 메모, Rust 검색(phonebook_search)까지 한 값으로 통일.
- **관련 업무 팝업의 완료 항목에 완료 날짜 표시** — 완료 탭 카드와 같은 표기(fmtT).

## v3.1.1 — 2026-08-02

### 변경 (소유자 지정)
- **관련 업무를 고르는 기준을 하나로 정리** — 이제 **전화번호부 정보(관련소속·관련인·연락처)가
  모두 일치하는 업무**만 엮인다. v3.0.2까지 있던 '메모에 같은 @태그가 있으면 엮음' 경로는
  폐지했다(이름만 같은 동명이인이 걸렸고, 팝업 설명 문구도 실제 규칙과 어긋나 있었다).
  @태그로 등록한 업무는 등록 시 관련인 3칸이 자동 첨부되므로 새 기준으로도 그대로 걸린다.
  팝업 설명도 규칙 그대로 고쳤다: "전화번호부 정보(관련소속·관련인·연락처)가 모두 일치하는 업무 N건".
  전화번호부 행의 `업무 N` 배지·정렬도 같은 기준을 쓴다.
- **전화번호부 입력줄과 목록 행의 열을 같은 격자로** — 관련소속·관련인·연락처의 시작선이
  위아래로 정확히 맞는다(넓은 창 3열, 560px 2열 모두). 실렌더로 좌표 일치를 확인했다.
- **[엑셀 양식]의 제목 줄을 화면 입력칸과 같은 이름으로** — 소속·이름·연락처 → **관련소속·관련인·
  연락처**. 불러오기 헤더 인식은 그대로 왕복되며, 열을 못 찾았을 때의 안내도 [엑셀 양식]을
  가리키도록 고쳤다.

### 문서 (전체 현행화)
- **사용 설명서** — 사용자 시나리오 중심으로 다시 썼다. "이럴 땐 이렇게" 상황별 찾아보기(§4)와
  **전화번호부·@태그 장(§6)** 을 새로 넣고, 카드 합치기·파일/폴더 링크·미니 창 검색 동작을
  현재 UI에 맞췄다. 스크린샷 전량 재생성 + 3장 추가(전화번호부·관련 업무 팝업·미니 창 검색).
- **README** — 버전 배지(v3.1.1)·테스트 수(237)·브랜드 색 갱신, 전화번호부 문단을 현재 규칙으로
  (3칸 완비 · [엑셀 양식] · 본문 태그 하이라이트 · 관련 업무 = 3칸 완전 일치).
- **구조 분석 보고서** — §10 신설(v2.7.0~v3.1.1 구조 변화: 순수 모듈 분리와 테스트 가능화,
  전화번호부 테이블·읽기 전용 커맨드, 두 웹뷰 구성 전달 계약, 디자인 단일 소스, 현재 검증 총괄).
  §1~§8은 v2.2.0 시점 감사 기록으로 보존한다는 '읽는 법'을 머리에 명시.
- **기능 제안서** — §0 반영 현황 신설(제안 8건의 구현/보류 상태 + 제안에 없던 사용자 요청 기능이
  3문 테스트를 어떻게 통과했는지).
- **시장 리서치 보고서** — 조사 시점 기록임을 명시하고, 앱 이름 변경·로드맵 반영 현황의 단일
  소스를 가리키도록 머리말 추가.
- **디자인 정책** — §5.2.1 신설: '입력줄 ↔ 목록 행'을 같은 격자로 맞추는 규칙(이번 전화번호부
  정렬 수정에서 얻은 재사용 가능한 규칙, 좌표로 검증).

## v3.1.0 — 2026-08-02

### 추가
- **전화번호부 [엑셀 양식] 버튼** — 제목 줄(소속·이름·연락처)만 있는 빈 엑셀 파일을
  저장한다. 채워서 [엑셀 불러오기]로 넣으면 그대로 인식된다(제목 문구를 불러오기의
  헤더 인식 규칙에 맞춰 두었고, 저장한 파일을 다시 읽는 왕복까지 확인). 예시 행은
  넣지 않는다 — 지우지 않고 불러오면 가짜 관련인이 등록되므로.

### 변경
- **버튼 전면 정리 — 통합 디자인 정책(Milkbuttercheese-Design §3)에 맞춤.** 버튼 역할을
  셋으로 못 박았다: 주 동작 = 청록 솔리드 / 보조·취소 = 옅은 회색 면 / 삭제 = 위험 빨강.
  - **[양식 입력]의 검정 솔리드 폐지** — 정책에 없는 네 번째 역할이었다. 보조 면으로 바꾸고
    쓰이지 않게 된 `--alt-btn`·`--alt-btn-fg`·`--alt-btn-hover` 토큰을 삭제했다.
  - **곡률을 두 단계로**: 글자 버튼 10px(r2_5), 아이콘 전용 미니 버튼 8px(r2). 스케일 밖이던
    프리셋 9px·요일/실행목록 7px·[찾기]/달력이동/토스트 6px를 흡수했다.
  - **보조 버튼은 테두리가 아니라 면으로** — 프리셋·[찾기]·달력 이동 버튼이 `--paper`(다크에선
    카드보다 어두운 '대지') 위에 테두리를 두르던 것을 `--chip-bg` 면으로 통일했다.
  - 칩·배지도 알약으로(요약 스트립 pill·열 카운트·완료 카운트가 9px이라 `.pb-cnt` 등과 어긋났다),
    카드 파일 칩의 스케일 밖 글자 11.5px → 11px(t1).
  - 실렌더 오버플로 감사(`tools/win-render.mjs`, Edge) 전 화면 통과 — 0건.

### 수정
- **빠른 검색 화면이 3단으로 쪼개지던 버그(가로줄 하나 + 빈 띠)** — @ 자동완성
  목록(#cap-pb)의 표시를 JS가 인라인 style로 폈다 접었다 한 탓에, 그 인라인이
  'block'인 채 남으면 검색 화면이 [입력칸 | 빈 띠 | 결과]로 갈라졌다. 실렌더로 그
  빈 띠가 자동완성 목록임을 확정했다. 이제 표시의 주인은 CSS 한 곳이고(body.pb),
  검색·등록 플래시 화면에서는 규칙상 자리조차 갖지 못한다. 목록을 접는 closePb도
  내부 상태(pbOpen)와 무관하게 항상 화면을 정리한다.
- **바로 입력의 @태그 하이라이트가 안 보이던 버그(진짜 원인)** — `textarea#inp:focus`가
  배경을 다시 불투명하게 칠하는데 특정도가 백드롭용 투명 규칙과 같아 뒤에 오는
  :focus가 이겼다. 글을 쓰는 동안은 늘 포커스 상태라 하이라이트 층이 항상 가려져
  있었다(v3.0.3의 로드 타이밍 수정과는 별개 원인). :focus까지 투명으로 못 박았다.
- 전화번호부 설명에서 괄호 안내 삭제 — "등록 시 소속·이름·연락처를 모두 입력합니다."
- **그 설명줄이 본문 글자색·크기로 나오던 문제** — `.m-sub`(한 줄 설명) 규칙이 `.modal`
  하위로 한정돼 있어 화면 본문에 쓴 설명줄은 아무 스타일도 못 받았다. 한정을 풀어
  어디에 있든 같은 톤(13px / `--ink-soft`)이 되게 했다(모달 안 모양은 그대로).
- **좁은 창(560px) 전화번호부 입력줄** — 세로 3단 스택을 폐지하고 **관련소속·관련인을
  한 줄**로 둔다(한 사람을 적는 짝). 연락처·[추가]는 다음 줄로 흘러내린다. (소유자 지정)

## v3.0.3 — 2026-08-02

### 수정
- **빠른 메모(메모 모드) 화면의 3단 띠 제거** — v3.0.2에서 힌트줄에 넣은 구분선이
  목록 없는 메모 모드까지 그어져 띠를 만들었다(경계선 프로브로 확정). 구분선은
  목록이 있는 검색 모드에서만 긋는다. 메모 모드 = 입력칸 + 바닥 힌트 한 판.
- **바로 입력 @태그 하이라이트가 안 뜨던 문제(로드 타이밍)** — 전화번호부가 DB에서
  도착하기 전에 타이핑하면 (빈 목록 기준으로) 평문으로 그려진 채 남았다. 초기 로드·
  백업 복원 완료 시 하이라이트를 다시 그리는 훅(refreshTagHl) 추가.
- 전화번호부 설명의 볼드체 제거(소유자 지정).

### 정책 확인 (변경 없음)
- **검색과 @태그는 별개다**: 태그는 3정보(소속·이름·연락처) 완비 strict, **검색은
  아이템의 관계인 정보 아무 필드나 부분일치** — 보드·완료 검색(haystack)과 미니
  바로검색(quick_search)은 원래부터 관련인 이름·소속·연락처(하이픈 무관 번호 포함)로
  걸린다. 검토 중 잠시 넣었던 '태그 경유 전화번호부 연동 검색'은 정책과 어긋나 철회.

## v3.0.2 — 2026-08-02

**@태그 통일 정책(소유자 지정)**: 어디서 입력하든(바로 입력·미니 창 빠른 메모·양식 메모)
태그는 똑같이 ① 본문 하이라이트 ② hover 반응 ③ 클릭 → 관련 업무 검색 ④ 등록/저장 시
관련인 자동 첨부로 동작한다.

### 수정
- **관련 업무 매칭 strict 화**: 매칭이 헐거워 엉뚱한 사람이 걸리던 문제 — 관련인은
  **소속·이름·연락처(숫자 비교) 3칸이 전화번호부 항목과 모두 동일**할 때만, 메모는
  **정확히 그 @태그**가 있을 때만 엮인다(부분일치·이름만·번호만 매칭 폐지).
  '업무 N' 배지·정렬도 같은 기준.
- **태그에 닿지 않았는데 클릭되던 문제**: 줄 끝을 클릭하면 캐럿이 태그 끝으로 스냅돼
  팝업이 뜨던 오작동 — 캐럿 판정을 폐지하고 **hover 와 같은 기하 판정**(커서가 실제로
  태그 사각형 위일 때만)으로 통일.
- **빠른 검색 3단 구조 해소**: 원인은 입력칸과 결과 사이에 끼어 있던 **힌트줄** —
  맨 아래 푸터로 내려 화면이 '입력칸/결과' 2단이 됐다(실렌더 스크린샷 검증).

### 변경 (통일 정책 완성)
- **미니 창 빠른 메모의 태그도 클릭 = 관련 업무 검색** — 팝업 대신 검색 화면으로
  점프해 그 이름을 검색한다.
- 바로 입력의 태그 클릭 → 관련 업무 팝업(v3.0.1)도 기하 판정으로 재정비.

## v3.0.1 — 2026-08-02

### 추가
- **빠른메모에도 본문 @태그 하이라이트** — 바로 입력(메인 창)과 미니 창 빠른 메모 모두,
  양식 메모와 같은 백드롭 층 기법으로 `@김철수` 글자 그 자리에 하이라이트가 깔리고
  **본문에서 태그를 클릭하면 관련 업무 팝업**이 열린다(바로 입력; 미니 창은 표시만).
  미니 창은 DB 를 직접 읽지 않는 원칙대로 새 읽기 전용 커맨드 `phonebook_list` 로
  실존 관련인 목록을 받아 판정한다. 하이라이트 전용 색 토큰(`--tag-hl-bg`) 신설 —
  기존 slate-bg 는 흰 면에서 보이지 않았다(실렌더 스크린샷으로 확인).
- **태그 hover 반응** — 빠른메모·양식메모에서 태그 가까이 커서를 가져가면 청록 링이
  켜지고 커서가 포인터로 바뀐다(백드롭 태그의 실제 사각형으로 판정 — textarea 가 위라
  CSS :hover 불가).

### 변경
- **파일·폴더 찾아보기 = 메뉴 버튼 패턴** (소유자 지정) — 제자리에서 버튼이 쪼개지던
  v3.0.0 방식 철회. 상위 버튼 하나를 누르면 아래로 하위 메뉴(파일 선택…/폴더 선택…)가
  열린다(@ 자동완성 드롭다운과 같은 시각 언어, 바깥 클릭으로 닫힘).
- **전화번호부 설명 최소화** — "등록 시 소속·이름·연락처를 모두 입력합니다" 한 줄.

### 수정
- **빠른 검색 화면 수리(실렌더 검증)** — v3.0.0의 '검색 모드 동적 높이'가 오히려
  빈 검색을 150px 3단 슬리버(입력칸·힌트·좁은 안내)로 만들었음을 스크린샷으로 확인,
  **철회**. 검색 모드는 예전처럼 고정 406 한 판이고, 빈 안내문("검색어를 입력하세요")은
  넓은 목록 영역 **가운데**에 띄운다. 타이핑 중 창 높이가 널뛰던 것도 함께 사라짐.

## v3.0.0 — 2026-08-02 (정식 배포 — 소유자 결정)

**v2.7.0~v2.10.0 에 걸친 대규모 기능 개편(전화번호부 · @태그 · 메모 합치기 · 통합 디자인)을
마무리해 묶은 판.** 실사용 피드백 반영 + 전면 점검(디버깅) 포함.
(⚠️ 아래 2026-07-14 자 구 v3.0.0·v3.1.0 항목은 *다른 계정의 미배포 내부 이력*이며 이 판과
무관하다 — 번호가 겹치지만 정식 배포는 이 항목이다.)
**버전 정책 갱신(소유자 지정, 2026-08-02): 앞으로 Y=기능 추가, Z=기능이 아닌 로직 변경·디버깅·조정.**

### 변경
- **@태그는 본문 안 하이라이트로** (소유자 지정 — 별도 칩 영역 아님). 양식 메모의
  `@홍길동` 은 본문 글자 그 자리에 하이라이트가 깔리고(백드롭 층 기법 — textarea 와 같은
  글꼴 메트릭의 뒤판에 태그 배경만 비침), **본문에서 태그를 클릭하면** 관련 업무 팝업이
  열린다(태그 밖 클릭은 그냥 편집). v2.9.0의 메모 아래 칩 줄은 제거.
- **@태그는 전화번호부에 실존하는 관련인만** — `@홍길동` 에서 '균' 한 글자만 지워도
  하이라이트·클릭이 즉시 해제되고 평문이 된다(소유자 지정). 카드 색 표시(linkifyAt)와
  양식 본문 하이라이트가 같은 규칙을 쓴다.
- **파일 링크 → 파일·폴더 링크** 로 명칭 변경, 버튼도 **[＋ 파일·폴더 찾아보기] 하나로 통합** —
  누르면 파일/폴더 선택지가 그 자리에 펼쳐진다(네이티브 선택창이 파일용·폴더용으로 분리돼
  있어 대화상자 자체는 못 합침 — 들어가는 문 하나 + 갈림길 둘).
- **전화번호부 목록 정렬(소유자 지정)**: ① **엮인 업무 수** 많은 순 → ② 소속 → ③ 이름.
  근거가 보이도록 각 행에 `업무 N` 배지를 달았다(태그 클릭 팝업과 같은 이름 OR 연락처 기준).
- **전화번호부 행 hover 반응** — 카드와 같은 시각 언어(소프트 리프트 + 청록 경계 + 포인터).
- **전화번호부 탭 설명 간결화** — 규칙 나열 대신 등록 → 사용 → 추적 세 줄.

### 수정
- **미니 창 화면이 3단으로 쪼개져 보이던 문제(근본 해결)**: 검색 모드가 결과 유무와 무관하게
  창을 406px 로 늘려 둬서, 빈 결과일 때 '입력칸·힌트·거대한 빈 목록'이 세 덩어리로 보였다 —
  이제 결과가 없으면 컴팩트하고 결과 수만큼만 늘린다(최대 406, @ 자동완성과 같은 원리).
- **ESC 순서 역전**: 양식 위에 관련 업무 팝업이 떠 있는데 ESC 가 밑의 양식부터 닫던 문제 —
  위에 뜨는 팝업을 먼저 닫도록 순서 교정.
- **양식 하단이 답답하던 문제**: 링크 버튼 줄과 저장 액션 줄(저장 위치·임시저장·되돌리기·저장)
  이 붙어 있던 것을 옅은 구분선 + 여백으로 분리.
- 미니 창 초안 회수(앱 재시작 시 미등록 초안 자동 등록)도 빠른 메모와 같은 규칙으로
  **@태그의 관련인을 자동 첨부**하도록 통일.
- 다크 포커스 링이 옛 액센트(#5bb3a6) 기준 rgba 로 남아 있던 것을 통합 값(#56bdab)으로 교정.
- 설정 팝업 저장 위치 표시가 값을 못 받았을 때 'undefined' 로 보일 수 있던 것 방어.
- **팝업이 열려 있으면 창을 못 움직이던 문제**: 양식·모달·알람 덮개가 커스텀 타이틀바까지
  가리고 있었다(560px 컴팩트에서 특히 답답) — 덮개를 타이틀바(34px) 아래부터 깔아,
  양식 메모가 열린 채로도 상단바 드래그 이동·최소화·닫기가 된다.

## v2.10.0 — 2026-08-01

### 추가
- **폴더 링크** — 양식의 파일 링크에 [＋ 폴더 찾아보기]가 생겼다. 폴더를 골라 링크하면
  클릭할 때 탐색기로 열린다(행 구조·활성/수정 토글은 파일 링크와 동일, 경로 직접 붙여넣기도 가능).
- **저장 위치 상시 표기** — [설정] 팝업 데이터 구역 상단에 현재 데이터 저장 폴더 경로가
  항상 표시된다(열 때마다 갱신 — 위치 변경 예약 후에도 최신 상태를 보여준다).

### 변경
- **@ 자동완성 문턱값(소유자 지정)** — 목록이 소음이 되지 않게: 이름은 **2글자부터**,
  전화번호는 **숫자 3자리부터**(단 모든 휴대전화의 공통 접두인 `010` 만으로는 열지 않음).
  **관련소속 칸은 자동완성을 하지 않는다** — 같은 소속이 수십 명에 걸려 목록이 의미가 없다.
  (메인 창·미니 창 공통.)
- **통합 디자인 정책(Milkbuttercheese-Design)과 결합** — 상위 정책의 확정 값을 앱 토큰에
  이식했다: 주 액센트 `#4d938a`→**`#2a7c6e`**(다크 `#5bb3a6`→`#56bdab`) — 흰 글자 대비
  3.0→4.7:1 로 WCAG AA 확보 · 본문 12.5→**13px**(t3) · 카드 곡률 13→**12px**(r3) ·
  버튼 9→**10px**(r2_5) · 액센트 배경/눌림색 동반 조정. `디자인 정책.md` 는 통합 정책을
  상위로 참조하고 이 앱 고유 규칙(카드 3줄·양식 정렬·560px·win-render)만 담당하도록 재편.
  저장 구조·데이터는 불변(순수 표시 변경).

## v2.9.0 — 2026-08-01

v2.7.0 전화번호부·@태그를 실사용 피드백으로 손질한 버전. **메모는 자유 형식,
전화번호부는 "명함 한 장"(소속·이름·연락처 완비)** 이라는 역할 구분이 뼈대다.

### 변경
- **전화번호부 무결성 규칙(소유자 지정): 관련소속·관련인·연락처 3칸이 모두 있는 사람만
  저장된다** — 직접 입력·엑셀·새로고침·자동 연동 모든 경로 공통. 일부만 아는 관련인은
  메모에 자유롭게 적는다(태그 검색은 그대로 걸린다). 구버전에 저장된 불완전 항목은
  지우지 않고 그대로 두며(pass-through), 완전한 정보가 들어오면 빈 번호를 보강한다.
- **@ 선택 삽입 형식: 완성형 태그만** — 바로 입력·미니 창에서 고르면 `@김철수(행정과 010-…)`
  가 아니라 `@김철수` 만 들어간다. 소속·번호는 등록 시 관련인으로 자동 첨부되므로(아래)
  메모에 중복으로 남길 이유가 없어졌다(메모가 어수선하던 비용 제거).
- **@태그 클릭 위치가 카드 → 양식 메모 아래 칩으로 이동**(소유자 피드백). 카드 위 태그가
  클릭을 받으니 카드를 열려던 클릭이 팝업을 띄우는 오동작이 됐다 — 이제 카드의 태그는
  색 표시일 뿐이고, 양식을 열면 메모 아래 **@태그 칩**이 떠서 클릭하면 관련 업무 팝업이
  열린다(전화번호부 행 클릭도 동일).
- **[아이템에서 가져오기] → [새로고침]** 으로 개명, 확인 창을 네이티브 confirm 대신
  **앱 표준 모달**(찾은 관련인 목록 + [모두 추가])로 교체. 엑셀 불러오기도 같은 모달을 쓴다.

### 추가
- **빠른 메모 @태그 → 관련인 자동 첨부**: 바로 입력·미니 창으로 등록하는 순간 메모 속
  @태그를 전화번호부와 대조해 그 관련인(소속·이름·연락처)을 업무에 구조화해 붙인다.
  같은 태그를 여러 번 적어도 한 번만.
- **양식 저장 → 전화번호부 자동 연동**(소유자 지정): 양식의 관련인 중 3칸 완비인 사람은
  저장 시 전화번호부에 자동 반영된다. 같은 이름·소속의 기존 항목에 번호가 없으면
  **번호만 보강**하고, 번호가 다르면 새 항목(한 사람 번호 둘은 정상). 삭제는 전파하지
  않는다 — 아이템에서 지워도 전화번호부는 그대로. [새로고침]은 과거 데이터·복원분 회수용.

### 수정
- **미니 창 @ 자동완성이 뜰 때 화면이 3분할로 찢어지던 문제**: 입력칸(flex:1)이 늘어난
  창 높이를 흡수해 입력·힌트·목록이 벌어져 보였다 — 목록이 펴진 동안 입력칸 늘어남을
  꺼서(body.pb) 입력칸·힌트는 위에 붙고 목록만 아래를 채운다.

## v2.8.0 — 2026-08-01

### 추가
- **메모 합치기** — 보드에서 카드를 끌어 **다른 카드 위에 놓으면 하나로 병합**된다
  (같은 업무가 두 번 적힌 것을 정리하는 용도 — 아침에 던져둔 바로 입력 메모 + 오후에
  같은 건으로 또 온 전화). 받는(가만히 있던) 카드가 기준이다:
  - **id·알람 상태·담당 = 받는 카드 유지** (소유자 지정 — 알람·세부 상태가 그 카드에 붙어 있으므로)
  - 메모 = 이어붙임(받는 쪽 + 빈 줄 + 끌려온 쪽)
  - **접수·마감 = 둘 중 이른 쪽** (마감을 놓치는 사고가 없는 안전한 방향, 소유자 지정;
    마감이 바뀌면 F2 규칙대로 알람 재무장)
  - **세부할일 = 합쳐서 점검시각 오름차순 자동 정렬** (시각 없는 것은 뒤, 원래 순서 유지)
  - 관련인·식별정보·파일 = 이어붙임 + 중복 제거(전화는 숫자만 비교)
  - 분류 상태: 어느 한쪽이라도 분류를 마쳤으면(양식 저장) 분류 대기로 되돌리지 않는다
  - 실수 방지: 병합 직후 **[실행 취소] 토스트**(6초) — 카드 삭제와 같은 패턴(소유자 지정,
    확인창 대신). 60초 자동 재렌더는 드래그 중이면 건너뛴다(드래그가 끊기지 않게).
  - 대상은 보드(4열·5열)의 카드만 — 완료 탭·캘린더 상세에서는 드래그되지 않는다.

### 수정
- **번호만 저장된 관련인의 @태그(`@010-1234-5678`) 클릭**이 연락처 조건(숫자만 비교)으로도
  검색되도록 수정 — 아이템에 `01012345678` 처럼 표기가 다르게 적힌 연락처도 걸린다.

## v2.7.0 — 2026-08-01

### 추가
- **전화번호부 탭** (보드·캘린더·완료 옆 네 번째 탭). 업무(아이템)와 독립적으로 관련인
  ({관련소속·관련인·연락처})을 모아두는 곳이며, 세 가지 방법으로 채운다:
  - **직접 입력** — 탭 상단의 3칸 입력줄(추가/수정/삭제, Enter 로 등록).
  - **[아이템에서 가져오기]** — 기존 업무에 적어둔 관련인 중 전화번호부에 없는 사람만 골라
    확인 후 일괄 추가한다(자동 동기화가 아니라 명시적 1회 흡수 — 소유자 지정). 전화번호는
    하이픈·공백 표기가 달라도 같은 사람으로 판정해 중복을 막는다.
  - **[엑셀 불러오기]** — `.xlsx/.xls` 첫 시트에서 이름(성명)·소속(기관·부서)·전화(연락처)
    제목 줄을 자동 인식해 일괄 추가한다(중복 제외, 이미 내장된 SheetJS 사용 — 인터넷 불필요).
- **@ 자동완성** — 전화번호부에 저장된 관련인을 입력 중에 불러 쓴다:
  - **바로 입력 / 미니 창 빠른 메모**: `@김철` 처럼 치면 커서 옆에 후보가 뜨고(↑↓·Enter·클릭),
    고르면 `@김철수(행정과 010-1234-5678)` 형태로 **@태그 + 정보가 병기**된다. 미니 창은 DB 를
    직접 읽지 않는 원칙 그대로 새 읽기 전용 커맨드(`phonebook_search`)로 검색한다(quick_search 와 같은 경로).
  - **양식 메모**: 같은 @ 트리거 — 고르면 토큰이 완성형 `@김철수` 태그로 남고 **관련인 칸이
    채워진다**. 같은 사람을 여러 번 골라도 관련인은 **한 번만** 들어간다(소유자 지정).
  - **양식 관련인 칸**: @ 없이 이름·소속·번호 일부만 쳐도 후보가 뜨고, 고르면 그 행 3칸이 채워진다.
  - 트리거는 줄 시작·공백 뒤 @만 인식(이메일 주소 한가운데 @ 는 무시), 한글 IME 조합 중에는
    목록 조작·확정을 하지 않는다.
- **@태그 클릭 → 관련 업무 팝업** (소유자 지정). 카드 메모의 `@김철수` 태그를 클릭하면(카드는
  안 열리고) 그 관련인과 엮인 업무 목록이 팝업으로 뜨고, 행을 누르면 그 업무의 양식이 열린다.
  검색은 **이름 OR 연락처(숫자만 비교)** — 아이템에 이름만 적었거나 연락처만 적었을 수 있기
  때문. 이름은 관련인·관련소속·메모 본문에서 찾는다. 전화번호부 탭의 행을 눌러도 같은 팝업이
  뜬다. (바로 입력의 `@김철수(행정과 010-…)` 는 이름 부분만 태그가 되고 괄호 정보는 평문.)
- Rust: `phonebook` 테이블(006 마이그레이션, append-only) + `save_phonebook`/`phonebook_search`
  커맨드. JSON 백업(`phonebook` 키)·DB 백업에 함께 실려 왕복하며, 구버전 백업(키 없음)을
  복원해도 현재 전화번호부를 비우지 않는다(관대한 복원 계약 유지).

### 수정
- `tests/dom/form-route.test.js` 의 '마감 오늘' 검사가 **21시 이후 실행 시 매번 실패**하던
  시간 의존 결함 수정(now+3h 가 자정을 넘어 '예정·대기'가 됨 → '오늘 23:59' 고정으로 교체).

## v2.6.9 — 2026-07-29

### 변경
- **README를 사용자 관점으로 다시 썼다.** 기능 나열 대신 "이런 적 있으시죠(문제) → 그래서 뭐가
  달라지나(비교표 + 실제 흐름) → 카드를 손으로 옮기지 않습니다 → 이렇게 씁니다 → 시작하기·데이터·
  자주 묻는 것 → 만들면서 → 개발" 순서로 바꿨다. 처음 보는 사람이 **무엇에 쓰는 물건인지**를
  먼저 알게 하는 것이 목적이고, 개발 내부 사정(모듈 목록 등)은 필요한 만큼만 남기고 `CLAUDE.md`에
  맡겼다. 문서 표에 `디자인 정책.md` 를 추가했다.
- 프로그램 동작 변화 없음(문서만). exe 는 다음 빌드에서 v2.6.9 로 갱신된다.

## v2.6.8 — 2026-07-28

### 수정
- **검색칸이 위로 밀리고 휠 스크롤이 먹지 않던 문제(한 원인)**: 결과 목록을 감싼 flex 칸에
  `min-height:0` 이 없어, 목록이 '스크롤되는 영역'이 아니라 **내용만큼 길어지는 영역**이 됐다.
  그래서 결과가 많으면 검색칸이 밀려 올라가고, 스크롤 컨테이너가 없으니 휠도 반응하지 않았다.
  이제 **검색칸·힌트줄은 상단 고정, 목록만 스크롤**된다.
- **목록 밖(검색칸·힌트줄)에서 휠을 굴려도 결과가 스크롤**되도록 했다 — 커서가 어디에 있든
  마우스 휠로 결과를 오르내릴 수 있다.

## v2.6.7 — 2026-07-28

### 수정
- **엉뚱한 줄이 계속 하이라이트되던 문제(원인 둘)**:
  ① **마우스 hover** — 미니 창은 커서 근처에 뜨는데, 그 자리에 결과 목록이 깔리면 **커서 밑의 줄**이
  곧바로 `:hover` 가 되고, 마우스를 안 움직이니 계속 켜진 채 남았다(첫 줄이 아니라 아무 줄이나).
  이제 hover 표시는 **마우스를 실제로 움직였을 때만** 켜지고 키보드를 쓰면 꺼진다(`body.mouse`).
  ② **자동 선택** — v2.6.4에서 "검색 후 바로 Enter"를 노려 첫 줄을 미리 골라뒀다. 이제 검색만
  하면 아무 줄도 켜지지 않고 `↑`/`↓` 를 눌러야 선택이 생긴다(첫 `↓`=첫 줄, 첫 `↑`=마지막 줄).
  아무것도 안 고른 채 `Enter` 를 누르면 첫 결과가 열려 편의는 그대로다.
- **끝에서 반대쪽으로 튀던 이동**: 맨 아래에서 `↓` 를 누르면 맨 위로 순환했다. 이제 **끝에서는
  멈춘다** — 맨 아래에서 `↑` 를 누르면 바로 위 줄로 정상적으로 올라간다.

## v2.6.6 — 2026-07-28

### 수정
- **미니 창을 열 때 지난 검색어·결과가 한 번 번쩍이고 지워지던 문제**: 이 창은 닫히는 게 아니라
  숨는 것뿐이라 화면이 그대로 남는데, 비우는 시점이 **다시 뜬 뒤**였다. 이제 **감출 때 미리**
  비운다 — 검색어·결과·선택을 지우고 첫 화면과 창 높이까지 맞춰 둔다. 다음에 뜰 때는 첫
  프레임부터 빈 화면이다. (단축키를 두 번 눌러 Rust 가 숨긴 경우에도 `wmhh://capture-hidden`
  으로 알려 같은 처리를 한다.) 메모 초안은 그대로 유지된다.

## v2.6.5 — 2026-07-28

### 수정
- **다른 창을 잠깐 보고 돌아오면 미니 창이 초기화되던 문제**: 치던 검색어가 지워지고 첫 화면으로
  되돌아갔다. 원인은 웹뷰의 `focus` 이벤트마다 화면을 초기화한 것 — 창이 **새로 뜬 것**과
  **포커스만 돌아온 것**을 구분하지 못했다. 이제 초기화는 단축키로 창을 실제로 띄울 때만 한다
  (Rust `show_capture_window` → `wmhh://capture-shown`). 포커스만 돌아오면 보던 화면·검색어가
  그대로 있고 입력칸 커서만 복구된다.
- **검색 결과에서 ↓ 로 내리다가 선택이 첫 항목으로 되돌아가던 문제**: 검색어를 치고 곧바로
  방향키를 누르면, 250ms 입력 디바운스가 뒤늦게 터지며 목록을 다시 그려 선택이 0번으로
  리셋됐다. 이제 다시 그릴 때 **고른 업무를 id 로 기억**해 같은 업무가 새 목록에도 있으면 그
  자리를 그대로 이어간다(없으면 첫 항목).

## v2.6.4 — 2026-07-28

### 추가
- **미니 창 검색을 키보드로 조작**: `↑`/`↓` 로 결과를 오르내리고 `Enter` 로 연다(끝에서 순환).
  검색하면 첫 항목이 미리 선택돼 있어, 검색어를 치고 바로 `Enter` 만 눌러도 열린다.
  마우스 hover 와 같은 표시(`.sel`)를 쓰므로 눈에 보이는 규칙은 하나다.
- **빠른 메모를 `Ctrl+S` 로도 등록**: 미니 창의 빠른 메모, 메인 창의 바로 입력칸 둘 다
  `Ctrl+Enter` / `Ctrl+S` 어느 쪽으로도 등록된다(앱 전체 'Ctrl+S = 저장' 규칙과 일치).
  바로 입력칸에 커서가 있을 때 `Ctrl+S` 가 백업 파일 대화상자를 띄우던 어긋남도 함께 해소.

### 수정
- **다른 창(브라우저 등)이 뜨는 중에 단축키를 누르면 미니 창이 떴다가 사라지거나 반응이 없던 문제**:
  두 군데를 고쳤다. ① 창이 뜬 직후(450ms) 들어오는 `blur` 는 무시한다 — 새로 뜬 앱이 포커스를
  가져가며 날아온 것이라 창을 닫을 이유가 아니다. ② 단축키 토글 판정이 '보이는가'만 보던 것을
  **'보이고 포커스도 있는가'** 로 바꿨다 — 창이 다른 앱 뒤에 남아 있으면 이제 숨기지 않고 앞으로
  끌어온다(`show_capture_window`).
- **화면 설정(테마·화면 크기·미니 창 구성) 적용 중 예외가 초기 로드 전체를 무너뜨리던 구조**:
  이 호출들을 각각 `try` 로 감쌌다. 예전에는 화면 꾸미기 한 줄이 실패해도 초기 로드가 통째로
  catch 로 떨어져 *"데이터를 불러오지 못했습니다"* 경고가 뜨고 저장까지 차단됐다(데이터는 멀쩡한데
  앱이 죽은 것처럼 보임). 데이터 로드와 화면 꾸미기를 분리했다.

## v2.6.3 — 2026-07-28

### 변경
- **Ctrl+Alt+Space 화면 배치를 직접 정한다 (3P2 = 6가지)**: 세 화면 — **내 업무 검색 · 빠른 메모 ·
  양식 메모** — 중 **먼저 뜨는 화면**과 **Alt 를 누르면 넘어갈 화면**을 각각 고른다. 둘은 서로
  달라야 하므로 겹치는 버튼은 고를 수 없고, 첫 화면을 바꿔 겹치면 둘째가 자동으로 밀린다.
  기본값은 **기존 그대로**(먼저 뜨는 화면 = 내 업무 검색, Alt = 빠른 메모).
  - **양식 메모**(신규)는 미니 창 대신 **메인 창이 앞으로 나오며 빈 양식**이 열려 처음부터
    구조화해서 적을 수 있다. 첫 화면이 양식 메모면 미니 창은 아예 뜨지 않는다(이때는 Alt 전환 없음).
    깜빡임이 없도록 이 분기는 단축키를 받는 Rust 에서 한다 — 설정의 진실은 여전히 프런트/DB
    하나이고, Rust 는 `set_capture_form_mode` 로 받은 값만 캐시한다.
- **테마는 앱 전체에 하나로 통일**: 메인 창과 미니 창을 따로 고르던 2×2를 없애고 **[밝게/어둡게]
  하나**로 바꿨다. '양식 메모'가 메인 창의 양식 팝업이라, 테마가 둘이면 단축키로 오가는 세 화면의
  색이 서로 달라지기 때문이다(소유자 지정: 셋은 항상 같은 색). **기본값은 밝게 — 기존 화면 그대로**이며,
  어둡게로 바꾸면 미니 창까지 함께 어두워진다.
- **'메모 등록(Ctrl+Enter): 분류 대기로 바로 / 양식으로 열기' 설정을 없앴다** — 위 '양식 메모'가
  같은 필요를 더 단순하게 대신한다. 미니 창의 `Ctrl+Enter` 는 언제나 분류 대기 등록이다.
- **설정 팝업의 섹션 제목을 선택지보다 크게**(14px/700) — 훑을 때 구역이 먼저 읽힌다.

### 수정
- **다크 모드에서 토스트의 [실행 취소] 글씨가 검은색이던 문제**: 어두운 토스트 면 위에 강조색용
  글자 토큰(`--on-accent`)을 쓰고 있었다. 색면별로 글자 토큰을 어떻게 고르는지 정책으로 못박고
  (`디자인 정책.md`), 토스트는 `--toast-fg`, 테마와 무관한 고정색 면(닫기 hover)은 흰색 고정으로
  정리했다.
- 선택 상태(탭·세그먼트)가 밝게에서 어둡게보다 흐릿하던 것도 같은 정도로 또렷하게 맞췄다.

## v2.6.2 — 2026-07-28

### 수정
- **실행할 때 화면이 한 번 번쩍이던 문제**: 설정은 DB에서 읽어오므로 로드 전 한 프레임은
  기본값(밝게)으로 그려졌다 — 어둡게로 쓰면 매 실행마다 흰 화면이 번쩍였다(미니 창은 반대로).
  이제 마지막 선택을 **첫 페인트용 캐시**로 남겨 처음부터 제 색으로 그린다. 캐시는 화면을
  칠하는 데만 쓰고 어떤 판단에도 쓰지 않는다 — 설정의 진실은 여전히 DB 하나다.
- **양식을 쓰던 중 미니 창에서 '양식으로 열기'로 다른 메모를 던지면** 쓰던 내용의 임시저장이
  확정되지 않은 채 화면이 갈아치워지던 문제 — 양식을 새로 열기 전에 이전 임시저장을 먼저 확정한다.
- **임시저장 쓰기 압력 제한(데이터 안전)**: 임시저장은 설정 테이블에만 쓰고 업무 항목(items)은
  건드리지 않지만, 설정은 저장할 때마다 통째로 다시 쓰이므로 초안이 쌓이면 매 저장이 무거워진다.
  ① 내용이 직전 저장과 같으면 다시 쓰지 않고, ② 초안 총량이 상한(약 40만 자)을 넘으면 오래된
  것부터 버린다(지금 쓰고 있는 초안은 어떤 경우에도 남긴다). 최근 30개 보관 규칙은 그대로.
- **백업 왕복에서 임시 상태를 양방향으로 차단**: 내보낼 때뿐 아니라 **불러올 때도** 초안
  (`captureDraft`·`formDrafts`)을 털어낸다(`stripTemp`). 손으로 고친 파일이나 예전 개발 빌드가
  만든 백업에 초안이 들어 있어도 복원 직후 '저장한 적 없는 내용'이 되살아나지 않는다.
- **JSON 백업에 양식 임시저장(formDrafts)을 담지 않는다**: 복원한 항목을 열었을 때 백업 시점의
  옛 초안이 되살아나 저장된 내용이 아닌 것이 보이던 문제(캡처 초안 `captureDraft`와 같은 이유).

## v2.6.1 — 2026-07-28

### 수정
- **다크 모드 가시성 개선(자매 앱 UI 정책 반영)**: 다크 팔레트를 '어느 조항이었지
  (Which-article-was-it)'의 다크 정책과 같은 값으로 맞췄다 — 그쪽 라이트 팔레트가 이미 이 앱의
  종이톤을 참조하고 있어 두 앱이 서로의 밝기 짝이 된다.
  - 면을 **남색 3단**으로 확실히 나눴다: 배경 `#0f1420` → 카드 `#161c29` → 입력칸·칩 `#1e2537`.
    이전 회색 3단은 단차가 좁아 카드·입력칸 경계가 잘 안 보였다.
  - 보조 글자를 밝게(`#98a1ac` → `#aeb9cf`), 경계선을 또렷하게(`#333a43` → `#2b3346`).
  - **옅은 회색 면 전용 토큰 `--chip-bg` 신설** — 다크에서 `--line-soft` 가 카드와 거의 같아
    [되돌리기]·[알람]·[설정] 같은 보조 버튼과 카운트 칩이 배경에 묻히던 문제 해결.
  - 강조 청록은 라이트와 같은 계열(`#5bb3a6`), 태그·캘린더 색도 어두운 배경에서 읽히는 값으로
    (마감 `#f08098` / 점검 `#5cc8ff` / 식별정보 `#c79bf0` / 진행 중 `#f6a94a`).
  - 미니 창(빠른 메모·검색)의 어두운 팔레트도 같은 값으로 통일하고, 안내 힌트줄 글자를 밝혔다.

## v2.6.0 — 2026-07-28

### 추가
- **다크 모드 / 화이트 모드 (메인 화면 · 미니 창 각각)**: 설정에서 **메인 화면**과
  **미니 창(Ctrl+Alt+Space)** 의 밝게/어둡게를 따로 고른다(2×2 조합). 화이트는 기존 화면 그대로,
  다크는 같은 청록 강조색·같은 배치에 면 색만 어둡게 바꾼 것이라 두 모드가 한 시스템으로 보인다.
  색은 전부 CSS 토큰이라 `html[data-theme="dark"]` 한 줄로 전환된다(하드코딩 색 제거).
- **미니 창 시작 화면 선택**: `Ctrl+Alt+Space` 로 열 때 **내 업무 검색**(기본)과 **빠른 메모** 중
  무엇이 먼저 뜰지 고른다.
- **미니 창 메모 등록 방식 선택**: `Ctrl+Enter` 를 **분류 대기로 바로 등록**(기존)할지,
  **메인 창의 양식으로 열기**(메모가 채워진 양식 팝업이 뜨고 메인 창이 앞으로 나옴)로 할지 고른다.

### 변경
- **설정이 드롭다운 메뉴 → 팝업(모달)으로 바뀌었다.** 화면 테마·미니 창 설정을 위에,
  보드 모드/프리셋·식별정보/주기 업무, 저장 위치·백업·불러오기·XLSX 를 아래에 모았다.
  선택형 항목은 헤더 탭과 같은 알약형 세그먼트(청록 선택 표시)라 화면 언어가 통일된다.
  `ESC`·배경 클릭으로 닫힌다.
- 양식의 저장 버튼 이름을 **`저장`** 으로 되돌렸다(단축키 표기는 툴팁으로 — `Ctrl+S`·`Ctrl+Enter` 모두 동작).

## v2.5.22 — 2026-07-28

### 추가
- **양식 임시저장 + [되돌리기]**: 양식에 쓰다가 `ESC`·다른 화면 이동·앱 종료로 나가면 쓰던
  내용이 통째로 사라지던 문제를 없앴다. 이제 입력이 잠깐 멈추면(0.7초) 화면 내용이 자동으로
  **임시저장**되고(저장 버튼 왼쪽에 `임시저장됨 HH:MM` 표시), 그 양식을 다시 열면 쓰던 자리에서
  이어서 쓸 수 있다. 임시저장은 **항목 자체(보드 카드)를 바꾸지 않는다** — 보드에 반영되는 건
  최종 저장뿐이다.
  - 기존 `[취소]` 버튼을 **`[되돌리기]`** 로 교체: 마지막으로 저장한 내용으로 되돌리고
    임시저장분을 버린다(새 항목이면 작성 중인 내용 비우기). 실수 방지를 위해 확인을 한 번 묻는다.
  - 새 양식을 이미 채워진 상태로 여는데(바로 입력 텍스트·프리셋) 남은 '새 업무' 임시저장분이
    있으면, 어느 쪽을 이어 쓸지 물어본다(조용히 덮어쓰지 않는다).
  - 임시저장은 설정에 함께 저장되며(`settings.formDrafts`), 삭제된 항목의 잔여분은 자동 정리하고
    최근 30개까지만 보관한다.

### 변경
- **저장 단축키 통일**: `Ctrl+S` 는 열려 있는 편집 화면(양식 → 주기 업무 → 프리셋)을 저장하고,
  아무것도 열려 있지 않으면 기존처럼 JSON 백업을 내보낸다. **`Ctrl+Enter` 로도 저장**되도록
  맞췄다(바로 입력칸의 `Ctrl+Enter`=등록에 익숙한 손 그대로). 세부 할 일·관련인 칸에서 그냥
  `Enter` 는 지금처럼 다음 줄 추가.
- 저장 버튼 이름을 `저장 (Ctrl+S)` 로 바꿔 단축키를 화면에서 바로 알 수 있게 했다.

## v2.5.21 — 2026-07-28

### 변경
- **Ctrl+Alt+Space 미니 창이 검색 모드로 먼저 뜬다**: 이제 단축키를 누르면 **내 업무 검색**이
  먼저 열리고, **Alt** 를 누르면 빠른 메모로 전환된다(기존과 반대). 창을 열 때마다 검색어는
  비운 상태로 시작하고, 메모 초안은 그대로 남아 Alt 로 넘어가면 이어서 쓸 수 있다.
- **입력칸 안내 문구(placeholder)를 아래 힌트줄로 이동**: 빈 입력칸에 뜨던 회색 문구
  (`빠른 메모 · Alt 검색`, `검색`)를 실제로 입력된 글자로 오해해 *"그 단어 뒤를 눌러도 커서가
  안 간다"* 는 혼선이 있었다(placeholder는 값이 아니라 그림이라 커서가 항상 맨 앞으로 간다).
  이제 입력칸은 완전히 비어 있고, 안내는 입력칸 바로 아래 힌트줄에 모드별로 표시한다 —
  검색: `내 업무 검색 · Alt 를 누르면 빠른 메모` / 메모: `빠른 메모 · Ctrl+Enter 등록 · Alt 를 누르면 검색`.

## v2.5.20 — 2026-07-22

### 추가
- **대량 축소 시 덮어쓰기 전 강제 스냅샷(데이터 유실 방지)**: 저장은 항목 전체를 지우고 다시
  쓰는 방식이라, 프론트 오류나 실수로 항목이 통째로 줄어든 채 저장되면 전체가 그걸로 대체될 수
  있었다. 이제 저장 직전 항목 수가 **급격히 줄면**(기존 5건 이상 → 절반 이하) 30분 스로틀과
  무관하게 **덮어쓰기 전에** 타임스탬프 스냅샷(`wmhh_presave_*.sqlite`)을 먼저 남긴다. 사소한
  1~2건 삭제는 스냅샷하지 않는다. (`db::save_items_guarded`)
- **입력 길이 제한**: 무제한이던 텍스트 입력에 상한을 뒀다 — 메모 20,000자, 세부 할 일 500자,
  연락처(관련인·소속 100자·전화 40자)·식별정보(명칭·번호 100자)·담당 100자·파일 경로 1,000자.
  날짜/시각 입력은 이전부터 제한돼 있었다. 비정상적으로 큰 붙여넣기가 매 저장마다 전체 데이터셋을
  직렬화하는 경로를 부풀리는 것을 막는다. (메모 5만 자 ≈ 원고지 250매 / A4 25장 수준이라 정상
  메모로는 닿지 않으며, `maxlength`는 새 입력만 제한하고 기존에 저장된 더 긴 값은 불러오기·저장
  시 그대로 보존한다.)

### 수정
- **자동 복구가 손상된 백업을 그대로 채택하던 문제**: 원본 DB를 못 열 때 가장 최근 자동 백업으로
  복구하는데, 그 백업 자체의 무결성은 확인하지 않았다. 최신 백업이 손상돼 있으면 더 오래된 정상
  백업이 있어도 건너뛰지 못하고 새로 시작(유실)할 수 있었다. 이제 복구는 백업을 **최신순으로
  훑으며 `PRAGMA integrity_check`를 통과하는 첫 번째 정상 백업**을 채택한다(수동 불러오기 경로와
  대칭). (`db::open_with_recovery`)
- **저장 위치 이동이 대상 폴더의 기존 데이터를 덮어쓸 수 있던 문제**: 이미 이 프로그램 데이터가
  들어 있는 폴더를 새 저장 위치로 고르면 다음 재시작 때 현재 데이터가 그 위에 복사돼 기존 데이터가
  사라질 수 있었다. 이제 대상에 데이터가 있으면 이동을 취소하고 안내한다. (`choose_data_dir`)

### 문서
- v2.5.19/v2.5.20 데이터 안전 변경을 문서에 현행화: `README.md`(버전 배지 v2.5.20, 데이터
  저장 섹션), `사용 설명서.md`(9장 — 저장 실패 경고·대량 삭제 직전 스냅샷), `구조 분석 보고서.md`
  (§9 후속 데이터 안전 강화 이력), `CLAUDE.md`(store.js·db/mod.rs·commands.rs·저장 안전 섹션).
  코드/exe 변경은 없으므로 버전 번호는 v2.5.20 그대로 둔다.

## v2.5.19 — 2026-07-22

### 추가
- **저장 실패 경고 표시(조용한 성공 / 시끄러운 실패)**: 이 앱은 손댈 때마다 백그라운드로
  자동 저장하는데, 그 저장이 실패해도(디스크 꽉 참·저장 위치 분리·권한/백신 잠금·무결성
  게이트 등) 지금까지는 콘솔에만 한 줄 찍고 화면엔 아무 표시가 없었다. 그래서 저장이 조용히
  실패한 채로 앱을 닫으면 그 변경이 메모리에만 있다가 사라질 수 있었다. 이제 저장이 실패하면
  화면 상단에 **빨간 경고 배너**가 뜨고, **다음 저장이 성공할 때까지 계속 떠 있는다**(성공하면
  자동으로 사라짐). 저장 성공 시엔 아무 것도 띄우지 않는다.
  - 배너 안내: 앱을 닫기 전에 [설정 → JSON파일 백업]으로 따로 저장하거나 저장 공간·위치를
    확인하도록 유도한다.
  - 해제는 **아이템 저장(save_all) 성공만** 담당한다 — 설정 등 사이드카 저장 성공이 아이템
    저장 실패를 가리지 않도록. 사이드카 저장 실패도 경고를 켜지만 끄지는 않는다.

## v2.5.18 — 2026-07-21

### 변경
- **시각을 하나도 안 정한 업무는 '분류 대기'로**: 마감일도 검토일(세부 점검시각)도 없는 업무가
  '예정 · 대기'에 섞여 있었다 — 예정이 하나도 안 잡혔는데 '예정·대기'에 있는 셈이라 칸 이름과
  실제 의미가 어긋났다. 이제 각 칸의 뜻이 일치한다:
  **분류 대기 = 아직 언제 할지 안 정함 / 예정 · 대기 = 언제 할지 정해졌고 아직 손 안 댐.**
  - 4단계 판정 순서: ② 오늘(지났거나 오늘) → ③ 할 일 하나라도 완료 → ① 시각 미지정 →
    ④ 예정 · 대기. **①을 ③ 뒤에 두어**, 이미 손댄 업무(세부를 하나라도 완료)는 시각이 없어도
    '진행 중'을 유지한다 — 진행하던 일이 미분류 칸으로 역행하지 않게.
  - 5단계(시간·담당자)도 동일하게 시각 미지정이면 분류 대기, 그 외에는 오늘·담당자로 분류.
    단 5단계에는 '진행 중' 칸이 없으므로, **손댄 업무(세부를 하나라도 완료)는 5단계에서도
    분류 대기로 보내지 않고** `본인 진행 · 오늘 외`로 둔다 — 그러지 않으면 같은 업무가
    보드 모드를 바꿀 때마다 '진행 중' ↔ '분류 대기' 를 오간다.
  - '정해졌다'는 **하나라도** 정해진 것을 뜻한다(유효한 마감 또는 유효한 세부 검토일 하나).
    완료된 세부의 검토일도 정해진 것으로 센다. 손상된 ISO 는 시각 없음으로 친다(F7 계열).
  - 배치는 늘 파생값이라 데이터 변경 없이 기존 업무도 다음 렌더부터 새 규칙으로 재배치된다.

### 추가
- **파일 링크도 드래그로 순서 변경**: 세부 할 일·식별정보·관련인처럼 `⠿` 핸들을 잡아
  순서를 바꿀 수 있다. 저장은 DOM 순서를 그대로 읽으므로 별도 처리가 없다.
  - 덤으로 파일 행 구조가 세부할일 행과 같아져(핸들+체크+필드) **좌측 시작선 정렬**도 맞았다.

### 수정
- **560px 컴팩트에서 세부 할 일 줄 간격이 들쭉날쭉하던 문제**: 1줄(제목)↔2줄(담당) 사이가
  2줄↔3줄(날짜·시각)보다 5px 넓었다. 줄바꿈용 `::after` 스페이서가 **자기 줄을 하나 차지**해
  1↔2 구간에만 `row-gap` 이 두 번(5+0+5=10) 들어간 것이 원인. `row-gap` 대신 각 줄에
  `margin-top` 을 명시해 스페이서 줄 수와 무관하게 균등해졌다(1↔2 = 2↔3 = 34px).
- **세부 할 일 항목 사이가 줄 간격과 비슷해 덩어리 구분이 안 되던 문제**: 항목 사이를
  10px 로 벌려(줄 간격의 2배) 세 줄이 한 항목으로 읽히게 했다.

### 내부
- `tools/win-render.mjs` 에 회귀 방지 검사 3종 추가 — ① 줄 간격 1↔2 와 2↔3 의 차이가 2px
  초과면 실패 ② 항목 사이 간격이 5px 이하면 실패 ③ 파일 링크 행에 드래그 핸들이 없으면 실패.
  검사 픽스처의 세부할일을 1→2개로, 파일 링크를 0→1개로 늘렸다(기존엔 항목 사이 간격과
  파일 행이 아예 측정되지 않았다).

## v2.5.17 — 2026-07-20

### 변경
- **캘린더 일정 색을 보드 단계색과 분리**: 마감이 `--red`(#C0271C)라 '오늘 처리'(#cf6a55
  테라코타)와, 점검이 `--slate`(#4d938a)라 '예정·대기'(#5aa08f 녹청) 및 UI 액센트와 같은
  계열이어서, 캘린더의 '일정 종류'와 보드의 '업무 단계'가 시각적으로 섞였다.
  팔레트에서 비어 있던 파랑 축을 점검에 주고, 마감은 빨강 계열을 지키되 명도·온도를 낮춘
  버거디로 내려 테라코타와 떼어놨다.
  - 점검 `#3E6FA8` 인디고 / bg `#E9EFF8`, 마감 `#9B1B30` 버거디 / bg `#FAE9EC`
  - 새 토큰 `--cal-mid`/`--cal-due` 로 뺐다 — 톤 조정은 `:root` 두 줄만 고치면 전파된다.
- **캘린더 범례 명칭 변경**: `세부 할일(지정 시각)` → **`세부할일 점검일`**(`업무 마감일`과 대칭).

### 수정
- **캘린더 범례 점이 pill 색과 어긋나던 문제**: 범례가 인라인 스타일로 `--slate`/`--red` 를
  직접 참조해, pill 색만 바꾸면 범례는 옛 색으로 남았다. 같은 토큰을 쓰도록 고치고
  재발 방지 주석을 남겼다.
- **`tools/guide-shots.mjs` 가 Windows 에서 실행되지 않던 문제**: 동적 `import` 에 `c:\...`
  경로를 그대로 넘겨 `ERR_UNSUPPORTED_ESM_URL_SCHEME` 으로 죽었다(리눅스 CI 에선 `/...`
  가 우연히 통해 드러나지 않던 버그). `pathToFileURL` 로 변환하도록 수정.

## v2.5.16 — 2026-07-20

### 변경
- **화면 전체를 본문 폰트 하나로 통일 — 모노 폰트 폐지**: 시각·숫자가 모노 폰트라,
  카드 안에서 바로 위 메모(14px)·세부와 붙어 있으면서 이질적으로 보였다. 다른 서체를
  써야 할 명확한 이유가 없으므로 **글자·숫자·날짜·시각을 예외 없이** 메모와 같은
  Pretendard 로 통일했다.
  - 바뀐 곳: 보드 카드 태그(`마감:`·`완료:`·`점검:`), 양식의 날짜·시각 입력칸, 주기 업무
    시각 입력칸, 알람창 시각, 헤더 시계, 버전 표기, 완료 카운트.
    (달력 pill·날짜 숫자·양식 메모는 원래 본문 폰트였다.)
  - `--font-mono` 변수와 `.mono` 클래스를 **삭제**했다.
  - 자릿수 정렬은 새 `.num{font-variant-numeric:tabular-nums}` 가 맡는다(서체가 아니라
    숫자 폭만 고정) — **이게 없으면 시계가 1초마다 폭이 흔들리고** 접수/마감 칸의
    세로 정렬이 깨진다.
  - `디자인 정책.md` §2·§4의 "시각·번호·카운트는 모노" 규칙을 **단일 서체 규칙으로 개정**
    (문서와 코드가 어긋나지 않게). 새 서체를 도입하려면 왜 본문 폰트로 안 되는지를
    근거로 남기도록 했다.
- 검증: 양식의 날짜·시각 입력칸은 폭이 정밀하게 맞춰진 자리라(디자인 정책 §7-I,
  v2.5.9~v2.5.10에서 세 번 고쳤던 곳) 고정폭→가변폭 전환이 위험했다.
  `win-render`(실제 Edge) 결과 **오버플로 0건 + 세부 할 일 3줄 유지(452/491/525) +
  관련인 2줄 유지** 로 확인 — 폭 조정 없이 그대로 통과했다.

## v2.5.15 — 2026-07-20

### 추가
- **Ctrl + 휠로 화면 크기 조절**: 내부망 PC에서 글자가 너무 작다는 요구에 따라,
  화면 아무 데서나 **Ctrl을 누른 채 휠을 굴리면** 크기가 바뀐다(위=확대, 아래=축소).
  80%~150% 사이를 10%씩 오가고, 바꾼 값은 `settings.uiScale`에 저장돼 재시작해도 유지된다.
  바꿀 때마다 현재 배율이 알림으로 잠깐 뜬다.
  - **글자만 커지는 게 아니라 화면 전체가 같은 비율로 커진다.** 글자 크기만 올리면
    고정폭 요소(날짜 112px·시각 64px·담당 96px·식별 select 130px·보드 열 minmax)가
    따라 커지지 않아 글자가 칸 밖으로 넘치는데, 이는 v2.5.8~v2.5.10에서 세 번 연속
    고쳤던 Windows 레이아웃 깨짐과 똑같은 문제다.
  - **CSS `zoom` 이 아니라 웹뷰 자체 배율(`set_zoom`)을 쓴다.** CSS zoom 은 뷰포트
    크기를 바꾸지 않아 미디어 쿼리가 속는다 — 컴팩트(560px) 창에서 확대하면 560px 기준
    레이아웃 그대로 내용만 커져 가로로 넘친다. `vh`·`innerHeight` 도 확대를 모른 채
    계산돼 모달이 화면 밖으로 넘치고(닫기 버튼이 안 보임) 보드에는 늘 세로 스크롤이
    생긴다. 웹뷰 배율은 **CSS 픽셀 뷰포트 자체를 줄이므로** 확대가 "그만큼 좁은 창"과
    동치가 되어, 미디어 쿼리·vh·innerHeight 가 전부 저절로 맞는다(보정 코드 불필요).
    덕분에 **컴팩트·전체화면·빠른 메모 어디서나 같은 방식으로 동작**한다.
  - **맨 휠은 그대로 스크롤**이다. 보드 열·완료 목록·달력이 모두 세로 스크롤을 쓰기 때문에
    맨 휠에 확대를 걸면 목록을 내릴 방법이 없어진다.
  - **빠른 메모·빠른 검색 창(Ctrl+Alt+Space)도 같은 배율로 커진다.** 이 창은 네이티브
    창 크기가 따로 있어서 내용 배율만으로는 글자가 잘린다 — 창 크기도 함께 맞춘다.

### 내부
- Rust 커맨드 `set_ui_scale` 추가 — 메인·캡처 **두 웹뷰에 같은 배율**을 걸고 캡처 창
  크기까지 맞춘다. 현재 배율과 캡처 창의 마지막 논리 높이를 `AtomicU32` 로 기억해,
  배율이 바뀌면 모드(메모/검색)에 맞는 높이로 다시 잰다. `resize_capture` 는 이제
  논리 높이만 받는다(배율은 Rust 가 안다).
- `settings` 는 자유 키-값 JSON 맵이라 **DB 마이그레이션·Rust 모델 변경 없음.**
- 휠은 연속으로 쏟아지므로 저장은 400ms 디바운스로 묶는다(DB 쓰기 폭주 방지).
  배율 적용 자체는 F1 로드 게이트를 걸지 않는다 — 데이터 저장이 아니라 표시라서,
  로드 완료 전에도 저장된 크기를 그대로 보여줘야 한다.
- 미설정값 처리 주의: `Number(null)===0` 이라 그냥 clamp 하면 하한 80%로 떨어져
  설정을 만진 적 없는 사용자의 화면이 제멋대로 작아진다 — `normScale`이 명시적으로 100 처리.
- **`tools/win-render.mjs` 에 확대 폭 검증 추가**: 웹뷰 배율은 좁은 뷰포트와 동치이므로,
  배율로 실제 도달 가능한 폭(1477·1371·1280·1138·1108·1090px)을 4열·5열 두 모드에서
  모두 렌더해 가로 오버플로를 단언한다. 특히 1080px 경계 바로 위는 5열 최소폭(200×5)이
  4열(240×4)보다 넓어 사각지대가 의심됐으나, 실측 결과 **전 구간 오버플로 0건**으로 확인.
- 새 모듈 `src/ui-scale.js` + 테스트 8건(정규화·단계 이동·배율 적용·로드 게이트 비적용·
  휠 조작·디바운스 저장·맨 휠 비간섭·상한 폭주 방지). JS 136건 + Rust 18건 전부 통과.

## v2.5.14 — 2026-07-19

### 변경
- **'식별번호' → '식별정보' 명칭 통일**: 양식 필드는 이미 '식별정보'였으나 설정 메뉴
  (`프리셋·식별정보 관리`)·명칭 관리 모달·주기 업무 안내·XLSX 내보내기 열 머리글이 옛 이름
  '식별번호'로 남아 있어 혼선이 있었다. 사용자에게 보이는 표기를 모두 **식별정보**로 통일.
  (저장 구조·백업 JSON 키·데이터는 불변 — 표시 문구만 변경.)

### 문서
- **사용 설명서 정리**: 첫 사용자가 바로 읽고 쓸 수 있도록 변화 이력성 서술(이름 변경 안내·
  버전 언급·과도하게 세세한 컴팩트 레이아웃 설명)을 덜어내고, 잘못 적힌 '식별번호'를 '식별정보'로
  바로잡음. 명칭 통일에 맞춰 설명서 스크린샷 재생성.

## v2.5.13 — 2026-07-18

### 라이선스
- **라이선스를 Apache 2.0 → GNU AGPL v3 로 변경**: 소스를 재배포·개작 배포하거나 네트워크
  서비스로 제공하면 소스도 AGPL v3로 공개해야 한다(개인·기관 내부 사용만으로는 공개 의무 없음).
  `LICENSE` 원문 교체 + README 배지/라이선스 섹션 반영. 앱 동작·버전(exe)은 그대로.


### 변경
- **양식 섹션 명칭 간결화**: 긴 설명 괄호를 떼고 짧은 제목만 — `세부 할 일`,
  `관련소속 · 관련인 · 연락처`, `파일 링크`. 560px 컴팩트에서도 모두 한 줄로 표기.
  (떼어낸 설명 문구는 유실 방지를 위해 index.html 에 HTML 주석으로 보존.)

## v2.5.12 — 2026-07-18

### 변경
- **카드 메모는 본문 '첫 줄'만 표시**: 양식 메모에 여러 줄을 적어도 보드 카드에는 첫 줄만 나오고,
  그 첫 줄이 길면 최대 2줄까지 감싼다(나머지 줄은 카드를 눌러 양식에서 확인). v2.5.11의 줄바꿈
  전체 보존 대신 첫 줄만 가져오는 방식으로 정리.
- **삭제 안내 문구·버튼 정리**: '업무를 삭제했습니다' → **'업무 삭제함'**, 되돌리기 버튼은
  '실행취소' → **'실행 취소'** + `word-break:keep-all` 로 좁은 화면에서 음절 중간(실행취/소)이
  아니라 어절 경계(실행/취소)로 줄바꿈.

### 문서
- **README 상단 데코 헤더 추가**: 앱 아이콘 + 태그라인 + 배지(platform·Tauri·저장 100% 로컬·
  license Apache 2.0·version). 스테일 표기 정리.

## v2.5.11 — 2026-07-18

### 추가/개선
- **카드 메모에 줄바꿈 보존**: 보드 카드 메모가 사용자가 입력한 줄바꿈(\n)을 그대로 표시
  (`white-space:pre-line`, 2줄 클램프 유지).
- **빠른 메모창(Ctrl+Alt+Space) 첫 열 때 글자 배열 정상화**: textarea 기본 `rows=2`라 한 줄
  메모가 2줄 박스 위쪽에 붙던 것을 `rows=1`+auto-grow로 고쳐 한 줄은 바 중앙, 여러 줄은 확장.
- **타이틀바 더블클릭 = 최대화/복원 토글**: 상단 창 영역 빈 곳을 더블클릭하면 전체화면↔컴팩트로 전환.

### 제거
- **알람 '10분 뒤 다시'(스누즈) 삭제**: 알람창은 이제 [확인]만. 옛 데이터의 snooze-until 값은
  계속 호환되며 카드 점은 '울림'으로 표시된다.

### 수정 (데이터 안전 — 코드 감사 반영)
- **자동 백업 복구/정리의 파일명 정렬 오류(데이터손실)**: `wmhh_`·`wmhh_prerestore_`·
  `wmhh_preimport_` 접두사가 섞이면 파일명 문자순 정렬에서 `'p' > '2'`라 오래된 pre* 스냅샷이
  항상 최신으로 뽑혀, DB 손상 복구가 옛 데이터로 되돌리고 정리가 최신 백업을 지웠다. 파일명에
  박힌 타임스탬프(`YYYYMMDD_HHMMSS`)로 정렬하도록 수정(`stamp_key`) + 회귀 테스트 추가.
- **레거시/부분 백업 복원 거부**: `RecurDef`/`RecurTime`에 `#[serde(default)]`가 없어 `freq`/`time`
  누락된 정기 정의 한 건이 전체 복원을 실패시키던 문제(정기함은 제거됐고 이 데이터는 보존용).
- **손상된 알람 상태 디코드 방향**: 알 수 없는 알람 TEXT를 `Fired(true)`(확인함)로 읽어 알람이
  다시 안 울리던 것을 `Fired(false)`(미확인)로 — 안전방향은 '울리게'.
- **빠른 메모 등록 직후 크래시 시 중복 등록**: 등록과 동시에 초안(captureDraft)을 즉시 비워
  다음 실행에 같은 메모가 한 번 더 등록되던 문제 방지.
- **JSON 백업에 임시 초안 포함**: 백업 시 `captureDraft`를 제외해 복원 후 유령 항목 생성 방지.
- **검색어 오염**: `filters.js`의 식별정보·세부 제목에 `||''` 가드 추가(누락 필드가 "undefined"로
  검색되던 것 방지).
- **저장 완료 대기**: `STORE.saveAll`이 진행 중 프로미스를 반환해 `await`가 실제 저장 완료를 기다리게.
- **비-Windows 컴파일**: Windows 전용 `set_overlay_icon`을 `#[cfg(windows)]`로 게이트해
  리눅스/맥에서도 crate가 빌드·테스트되게(Windows 동작 불변).

## v2.5.10 — 2026-07-18

### 수정
- **560px 양식 세부 할 일을 3줄로 확정** — 1줄 제목 / 2줄 담당 / 3줄 날짜·시각. v2.5.9의
  2줄(담당·날짜·시각 한 줄) 배치는 사용자 PC의 DPI·폰트 환경에서 밀려 깨졌다(개발/CI Windows와도
  다른 배율). 각 요소를 자기 줄에 강제 고정(`::after` 개행 + `flex-basis:100%`)해 DPI·폰트와
  무관하게 항상 3줄로 안정. 담당칸·날짜칸 모두 112px, 세 줄과 접수/마감·식별·관련인 필드까지
  좌측 시작선을 x≈85로 정렬 통일.
- 검사도구(`win-render.mjs`)의 세부할일 검증을 3줄(제목/담당/날짜·시각) 확인으로 갱신.

## v2.5.9 — 2026-07-18

### 수정 (560px 컴팩트 양식 레이아웃 정리)
- **세부 할 일이 Windows에서 깨지던 문제**: 담당·날짜·시각을 한 줄에 욱여넣던 배치가
  Windows(WebView2)의 넓은 네이티브 컨트롤에서 줄이 밀려 깨졌다(리눅스 렌더에선 폭이 좁아
  재현 안 됨). **날짜칸을 112px 고정으로 줄여** 담당(112)+날짜(112)+시각(64)+아이콘이 한 줄에
  들어가게 하고, **1줄 제목 / 2줄 담당·날짜·시각**으로 분리. 담당칸도 날짜칸과 동일한 112px.
- **관련인도 2줄로**: 1줄 관련소속·관련인 / 2줄 연락처.
- **좌우 정렬 통일**: 세부할일·접수/마감시각·식별정보·관련인·연락처의 필드 시작선을 모두
  같은 세로선(x≈85)에 맞춤.
- 검사도구(`win-render.mjs`)에 세부할일 줄 수(담당·날짜·시각이 한 줄인지) 실측 검증 추가 —
  overflow만으론 못 잡는 Windows 줄바꿈 회귀를 실제 Edge에서 잡는다.

## v2.5.8 — 2026-07-18

### 수정
- **완료 화면에서 검색하면 검색창이 옆으로 툭 튀던 문제**: 목록이 줄어 세로 스크롤바가
  사라질 때 콘텐츠 폭이 스크롤바 너비(11px)만큼 늘어나 `margin-left:auto`로 우측 정렬된
  검색창이 이동하던 현상. Windows 클래식 스크롤바에서만 보이던 문제라(리눅스 오버레이
  스크롤바에선 재현 안 됨) `html{scrollbar-gutter:stable}`로 스크롤바 자리를 항상 확보해
  나타나든 사라지든 레이아웃이 흔들리지 않게 고침. 하드코딩이 아닌 표준 CSS 속성.
- **좁은 화면(560px 컴팩트)에서 검색창이 빈 띠에 홀로 떠 있던 배치**: 보드 상단·완료 상단
  검색창을 단일 열에서 한 줄 전체 폭으로 정렬 — 컬럼 제목과 시작선을 맞춤.

### 추가(개발 도구, 앱 미포함)
- **실제 Windows 렌더링 오버플로 자동 검사** `tools/win-render.mjs` + `.github/workflows/win-render.yml`:
  windows-latest 러너의 Edge(=WebView2와 동일한 Chromium/Blink 엔진·Windows 폰트)로 각 화면을
  렌더해 `scrollWidth > clientWidth` 가로 넘침을 자동 단언하고, 넘치면 CI를 실패시킨다.
  리눅스 개발환경과 Windows 타깃의 폰트 metric·네이티브 컨트롤 차이로 재발하던 오버플로를
  픽셀 하드코딩 대신 실제 타깃에서 잡기 위함. playwright-core 는 러너에서만 `--no-save` 설치 —
  앱 package.json/lockfile·exe·폰트에는 아무 영향 없음.
- **사용 설명서 스크린샷 현행화** `tools/guide-shots.mjs`: `docs/screenshots/*.png` 10장을 현재 UI로 재생성.

## v2.5.7 — 2026-07-18

### 수정
- **전체화면에서 양식의 접수·마감시각이 좁게 눌려 날짜/시각이 잘리던 문제**: `fm-grid`가
  `repeat(4,1fr)`이라 각 칸이 ~180px로 좁아 dt 입력(날짜 112·시각 64)이 flex-shrink로
  "2026"·"10"처럼 잘렸다. 칸을 내용 너비(`repeat(2,max-content)`)로 두어 접수·마감이 온전한
  폭으로 나란히 보이게 수정. (좁은 창 ≤1080은 기존대로 1열 세로 스택 유지.)

## v2.5.6 — 2026-07-18

### 변경
- **보드 모드 선택을 설정 팝업으로 이동**: 헤더의 `시간 │ 시간·담당자` 토글 필이 산만하다는
  피드백으로 제거 — 이제 **[설정] 메뉴 → 보드 모드 선택**에서 팝업으로 고른다. 팝업은 기존
  모달과 같은 문법(제목·설명·옵션 카드·닫기)이며, 옵션을 누르면 즉시 적용되고 선택 표시가
  남는다(ESC·배경 클릭 닫기). 저장·복원(settings.boardMode)은 기존 그대로.

### 수정
- **560px 컴팩트 창에서 입력칸이 화면 밖으로 나가던 문제**: ① 양식의 접수시각·마감시각
  그리드를 세로 1열로, ② 세부 할 일 행의 날짜·시각 위젯을 제목 아래 들여쓴 둘째 줄로,
  ③ 프리셋 모달 '새 프리셋 만들기' 입력칸(버튼 이름·메모 기본값)을 1열로 접어 넘침 제거
  (v2.5.4의 1080px 단일 열 반응형 블록에 통합).
- (작성: Opus / 검토: Fable — 옵션 카드 버튼의 폰트 미상속 보완)

## v2.5.5 — 2026-07-18

### 수정
- **빠른 메모창 'Ctrl 단독 → 메인 창 최대화' 기능 제거**: Ctrl+Alt+Space로 빠른 메모를 띄운 뒤
  Ctrl 키를 눌렀다 떼면 메인 창이 최대화로 튀어나오던 동작(v2.5.3 추가)이 **의도치 않게 자주 발동**돼
  삭제했다. 관련 프런트 로직(capture-win.js), Rust 커맨드(`open_main_maximized`), 테스트를 함께 제거.
  빠른 메모창의 나머지 동작(Ctrl+Enter 등록·Alt 검색 토글·Esc/blur 숨김)은 그대로.

## v2.5.4 — 2026-07-18

### 변경
- **카드 표기 원칙 조정**: 메모를 최대 **2줄**(기존 1줄)로, 세부 할 일 1줄·시각 1줄은 그대로 —
  카드가 한눈에 담는 정보량을 늘렸다.
- **카드 2·3번째 줄 들여쓰기 통일**: 세부 할 일(▸)과 시각(알람 점) 줄의 앞 마커 폭을 같은 9px
  슬롯으로 맞춰, 두 줄의 텍스트 시작 위치가 정확히 정렬된다(마감·점검 시각은 세부 할 일에 맞춰 왼쪽 정렬).
- **알림창 왼쪽면을 메인 카드와 통일**: 상단 빨간 바 → 카드처럼 **왼쪽 짧은 색 세그먼트**로.
  알림 목록 항목도 풀-높이 좌측선 대신 짧은 세그먼트(단계색=빨강 유지, 모서리도 카드와 동일한 곡률).

### 수정
- **바로 입력창 세로 확장 버그**: WebView2에서 드래그 리사이즈가 잘 안 잡히던 문제를 근본 해결 —
  내용을 입력하면 입력창이 **자동으로 세로로 늘어난다**(최소 높이 유지, 최대 화면 60%까지 늘고 그 뒤
  스크롤). 드래그 리사이즈 핸들도 그대로 둠.
- **좁은 화면에서 요소가 밖으로 튀어나가던 문제**: 헤더의 모드 토글(시간·담당자)이 창을 줄이면
  화면 밖으로 잘리던 것을 수정 — 좁은 창에선 부제(설명글)를 숨기고 브랜드 줄바꿈을 허용해 폭 안에
  들어오도록(줄 수를 늘리지 않고 가로폭을 줄이는 방향). **현재시각(시계)도 헤더가 접히는 폭부터
  항상 오른쪽 끝에 정렬**되도록(넓은 창 배치는 그대로).
- **가로 스크롤 제거**: 어떤 창 폭에서도 가로 스크롤이 생기지 않도록(세로 sticky 헤더에 영향 없는
  `overflow-x:clip`).
- **메인 창을 두 크기(560px 컴팩트 / 전체화면)로 고정(소유자 지정)**: 자유로운 드래그 리사이즈를
  끄고(`resizable:false`), 창은 **560px 폭 컴팩트** 또는 **전체화면(최대화)** 두 상태만 갖는다.
  타이틀바 최대화/복원 버튼(□)으로 두 상태를 오간다(기본값 전체화면으로 시작). 애매한 중간 폭이
  없어 항상 아래 두 레이아웃 중 하나로만 보인다.
- **레이아웃 560px 단일 열 / 데스크톱 두 가지로 통일(소유자 지정)**: 창 폭이 1080px 이하면
  **560px 폭 단일 열(가운데 정렬)** — 헤더 컴팩트, 입력부 세로 스택, 현재시각 오른쪽 끝. 넓은
  화면(전체화면)은 기존 4·5열 데스크톱 배치 그대로. (2열 중간 단계는 폐지.)
- **입력밴드 주변 회색 톤 제거**: 바로 입력창 배경을 순백(#fff)으로 통일(기존의 옅은 회색 톤 제거).
  보드 배경의 종이톤은 그대로 유지.

## v2.5.3 — 2026-07-18

### 추가
- **주기 업무 생성 시점·시각 선택**: 자식 카드가 보드에 나타나는 시점을 주기 업무별로 고른다 —
  `주초(월요일) 한 주치 일괄`(기존 동작·기본값) / `하루 전` / `당일` + **생성 기준 시각**도 직접
  지정(기본 06:00, 콜론 없이 0600 입력 허용). 주기 업무 입력·관리 모달의 기존 주기 행에
  드롭다운·시각 입력으로 추가(동일 UI 문법), 관리 목록에도 `보드 생성: 당일 06:00`처럼 표기.
  옛 데이터(spawn 값 없음)는 기존 주초 06:00 일괄 그대로.
- **캡처 창에서 Ctrl 단독 입력 → 메인 창 최대화 호출**: Ctrl+Alt+Space로 빠른 메모를 띄운 뒤
  Ctrl만 눌렀다 떼면(조합 아님) 메모 초안은 보존한 채 '뭐하려 했더라' 메인 창이 화면 가득
  (최대화) 전면에 뜬다. Ctrl+Enter 등록 조합과는 충돌하지 않는다.
- **알람 작업표시줄 표시**: 알람이 울리면 확인 전까지 하단 작업표시줄 아이콘이 깜빡이고
  (Windows 기본 주목 효과) 아이콘 위에 **빨간 점 배지**가 얹힌다. [확인]·[10분 뒤 다시]·알람
  끄기 시 배지가 걷힌다(미룸은 재울림 때 다시 표시).
- **관련인도 드래그(⠿) 정렬**: 양식의 관련소속·관련인·연락처 행 순서를 드래그로 변경
  (세부 할 일·식별번호와 동일 UX).
- **관련인 Enter로 다음 줄 추가**: 관련소속·관련인·연락처 칸에서 Enter를 치면 마지막 행이면
  새 행이 추가되고, 아니면 아래 행의 같은 칸으로 이동(세부 할 일과 동일 UX·한글 조합 중 무시).

### 변경
- **양식 순서 재배치**: 메모 → 세부 할 일 → 접수·마감시각 → 식별정보 → 관련소속·관련인·연락처
  → 파일 링크 순으로 변경(자주 쓰는 항목이 위로).
- 양식 세로 정렬 통일: 접수·마감시각 **입력칸**을 드래그 핸들 있는 행들의 첫 칸과 같은
  시작선으로(라벨 글자는 다른 섹션 제목처럼 왼쪽 끝 유지).
- 표기 변경: 양식의 `식별번호 (명칭 + 번호)` → **`식별정보`** (＋ 버튼 포함). 표시 명칭만
  바뀌며 저장 구조(`ids` 배열·`identifiers` 테이블·백업 JSON 키·식별번호 명칭 관리)는 그대로 —
  기존 데이터·백업과 충돌 없음.
- **주기 배지 축소**(10px): 시간·담당자 5열 모드 전체화면에서도 시각 태그와 주기 배지가
  한 줄에 온전히 표기되도록.
- **스크롤바 자동 숨김**: 평소엔 안 보이고, 스크롤 영역에 마우스를 올리면 유리 썸이
  나타난다. 빠른 메모 창 스크롤바도 검정 패널에 어울리는 유리 캡슐 스타일(흰 반투명·
  트랙 없음)로 교체 — 메모 입력칸·검색 목록 공통.
- **최종 산출물 파일명에 버전 표기(소유자 지정)**: 빌드 워크플로가 exe를
  `뭐하려 했더라v{X.Y.Z}.exe`로 산출하고 이전 버전 파일은 정리(최신 하나만 유지).

### 수정
- **파일 링크 섹션 글자 통일**: '파일 링크' 섹션 제목에만 스타일 규칙이 빠져 본문 크기
  (14px)로 렌더되던 것을 다른 섹션 제목과 동일(11px·600)하게 — 제목 규칙을 공용
  `.fm-inner .t-head` 하나로 일반화해 재발 방지. '찾기' 버튼 크기도 12.5px로 통일.

## v2.5.2 — 2026-07-18

### 변경 (카드 3줄 원칙)
- **보드 카드 = 메모 1줄 · 세부할일 1줄 · 시각 1줄** (각 1행 말줄임). 진행상황(#세부 x/n)과
  담당(#담당) 배지는 메인 화면에서 제거 — 자세한 건 카드를 열어 양식에서 본다.
  주기 배지는 시각 줄로 이동. 시각 태그의 **'#' 접두 제거**(`#마감:`→`마감:`, `#점검:`→`점검:`).
- **완료 탭 카드 = 메모 + 종료시각**: 완료 화면에서는 세부·마감 대신 `완료: 날짜 시각`
  (완료 처리한 시각)을 보여준다.

### 변경 (반응형 전환)
- **고정 1460px 레이아웃 → 반응형**: 창을 줄이면 열 폭이 먼저 줄고(내용은 1행 말줄임 원칙
  유지), 더 좁아지면 보드가 2열 → 1열로 접힌다. 헤더·입력부도 폭에 맞춰 재배치되어
  창 축소 시 UI가 깨지던 문제 해결.
- **유리(반투명) 스크롤바**: 트랙 없는 둥근 반투명 썸 스타일로 전체 통일.

### 변경 (기타)
- **담당자는 세부할일 전용으로**: 할일(항목) 단위 담당자 입력을 양식에서 제거 — 담당자는
  세부할일별로만 지정한다. 배치 판정도 세부할일 담당자만 본다. (기존에 저장된 항목 담당자
  값은 데이터로는 보존되고 화면·판정에만 안 쓰임 — 데이터 호환 유지.)
- **글자 폰트·크기 정책 정리**: 본문 UI 텍스트는 앱 서체(Pretendard) 12.5px로 통일, 모노
  폰트는 날짜·시각·숫자에만 사용. 파일 링크 경로 입력칸이 모노 폰트로 달랐던 것을 본문
  서체로 맞춤.

### 문서
- 사용 설명서 전면 재구성(내용 동일, 가독성 개선): 맨 앞에 '3분 요약'과 단축키 표 추가,
  '카드 읽는 법' 표 분리, 긴 문단을 짧은 문장·표로 분해, 섹션 흐름 통일.

## v2.5.1 — 2026-07-18

### 변경 (시간·담당자 보드)
- **배치 규칙 수정: 시각(마감·세부 점검) 정보가 전혀 없는 업무는 '진행·오늘'이 아니라
  '진행·오늘 외'로 간다.** (기존에는 분류 대기에서 꺼낸 무시각 업무가 '본인 진행·오늘'로
  새던 문제 — 오늘 열은 "오늘 처리할 시각이 실제로 잡힌 업무"만 담는다.)
- 열 이름 변경: **본인 예정 → 본인 진행·오늘 외**, **타인 예정 → 타인 진행·오늘 외**.
- '타인 진행·오늘' 열 머리 점 색을 '본인 진행·오늘'과 같은 적색으로 통일
  (카드 좌측 세그먼트는 이미 동일했음).
- 담당 배지 표기를 `#담당: ○○○` 형식으로 통일(#마감 태그와 같은 꼴)하고, 배경색을 다른
  태그가 안 쓰는 **뮤트 블루**로 교체(시각 태그의 청록·식별번호 보라와 구분).
- 세부 진행 배지도 같은 꼴로: `세부 x/n` → **`#세부: x/n`**.

### 변경 (카드 표시)
- **시각 중복 표시 제거**: 마감보다 앞서는 미완료 세부 할일이 있으면 카드에는 그 세부의
  시각(#점검)만 보이고 #마감 태그는 감춘다. 앞서는 세부가 없거나 세부를 전부 완료했으면
  #마감만 표시. (카드 구성 = 최대 4줄, 각 1줄: 메모 · 세부 · 시각 · 진행상황/담당 —
  시각·진행 줄은 좁은 열에서도 줄바꿈 없이 말줄임)
- 카드 세부 할일 글자 크기를 메모와 동일하게(11px → 14px).
- 시각 태그의 **남은 시간 꼬리표(N분후/N시간후) 제거** — 날짜·시각만 표기.
- 시각 태그 **긴급도 배경색을 임박만 강조하도록 축소**: 지남~2시간 이내(임박)만 코랄 레드로
  강조하고, 오늘/내일/이틀(오렌지·앰버·샌드 3단계)은 중립색으로 되돌림 — 색이 너무 많아
  어지럽다는 피드백 반영(v2.4.5의 5단계 → 2단계).

### 추가
- **커스텀 타이틀바** (v2.4.5에서 "다음 빌드로 보류"했던 건 반영): 메인 창 기본 창틀을 없애고
  (`decorations:false`) 앱 디자인과 통일된 타이틀바로 교체 — 빈 영역 드래그로 창 이동,
  더블클릭 최대화, ─ □ × 버튼(최소화/최대화·복원/닫기). 닫기는 기존과 동일하게 closeToTray
  설정을 따라 트레이 숨김 또는 종료. (빠른 메모 창은 원래부터 창틀 없음 — 변화 없음.)
- 양식의 **식별번호도 드래그(⠿ 핸들)로 순서 변경** 가능(세부 할일과 동일 UX).
- **연락처 숫자만 검색**: `010-1234-5678`로 저장된 연락처가 `01012345678` 검색으로도
  걸린다(보드·완료 검색 + 미니 캡처 빠른 검색 모두). 입력값 자체는 손대지 않는다
  (지역번호·내선 등 자동 하이픈 파싱은 애매해서 하지 않기로 함).

### 수정
- **XLSX 내보내기 누락 보정**: 시간·담당자 모드에서 무시각 업무가 '오늘 외'로 가도록 배치가
  바뀌면서, 손 댄(세부 일부 완료 — 시간 모드의 '진행 중' 상당) 업무가 내보내기에서 빠질 수
  있던 회귀를 보정 — 오늘 외 열이어도 세부를 하나라도 완료한 업무는 포함한다.
- 빠른 검색(미니 캡처)의 연락처 숫자 검색이 공백 구분 저장분(`010 1234 5678`)도 잡도록
  정규화를 보드 검색과 맞춤. 관련인 필드가 비어 있을 때 'undefined' 문자열이 검색
  대상(haystack)에 새던 잠재 문제도 가드.
- **빠른 메모 창 폰트 버그**: `font: 14px/1.5 inherit` 같은 축약 선언에 전역 키워드(inherit)를
  섞으면 선언 전체가 무효가 되는 CSS 규칙 때문에, 빠른 메모 입력창이 Pretendard가 아닌
  브라우저 기본 폼 폰트로 렌더되고 검색 결과·태그의 지정 크기(13px/10.5px 등)도 무시되던 문제.
  롱핸드(font-family/size/weight 분리)로 고쳐 입력 14px·결과 13px·앱과 동일 서체가 실제로 적용된다.
- 양식의 관련인 구역 제목을 입력칸 순서와 같게 `관련소속 · 관련인 · 연락처`로 정정.

## v2.5.0 — 2026-07-18

### 추가 (보드 모드 선택 — 시간 / 시간·담당자)
- **보드를 두 가지 방식으로 볼 수 있다.** 헤더의 `시간 │ 시간·담당자` 토글로 전환하며 설정에
  저장된다(기본값은 기존 그대로 **시간**).
  - **시간**(기존): 분류 대기 / 오늘 처리 / 진행 중 / 예정·대기 — 동작·화면 변화 없음.
  - **시간·담당자**(신규, 5열): 분류 대기 / **본인 진행·오늘** / **타인 진행·오늘** / **본인 예정** /
    **타인 예정**. 긴급도는 시각 태그 색(5단계)이 담당하므로 열 축을 담당자로 돌린 구성.
- **담당자 필드 추가**: 양식에서 항목·세부할일별로 지정(자유 텍스트, **비우면 본인**). 카드 귀속은
  *마감·점검이 가장 빠른 미완료 세부의 담당자 → 없으면 항목 담당자 → 없으면 본인* 순으로 판정하고,
  타인 카드에는 `담당 ○○○` 배지가 붙는다. 검색(보드·완료·빠른 검색)도 담당자 이름을 찾는다.
- 바로 입력은 여전히 분류 대기로 들어가며, 분류 해제 후 시각이 없으면 시간·담당자 모드에서는
  '본인 진행·오늘'로 계산된다(데이터에 마감을 임의로 쓰지 않음 — 모드 전환 안전).

### 데이터
- 마이그레이션 005(append-only): `items.owner`·`subtasks.owner` TEXT 추가. 구버전 DB·JSON 백업
  모두 그대로 호환(담당자 없으면 본인 취급). 배치는 항상 계산 전용이라 모드를 오가도 데이터 불변.
- 수정: XLSX 내보내기가 시간·담당자 모드에서 진행 업무를 누락하던 문제(신규 모드 키 미반영) 보정.

## v2.4.6 — 2026-07-18

### 변경
- **Pretendard 폰트를 앱에 내장.** 지금까지는 CSS에 이름만 지정돼 있어 Pretendard가 설치되지 않은
  PC(대부분의 내부망 PC)에서는 맑은 고딕으로 폴백돼 화면이 다르게(더 크고 투박하게) 보였다.
  가변 폰트 파일(`src/fonts/PretendardVariable.woff2`, 약 2MB, OFL 라이선스)을 번들해 메인 창·빠른
  메모 창 모두 어느 PC에서든 동일한 서체로 렌더된다. (오프라인 동작 그대로 — 외부 요청 없음.)
- **빠른 검색 결과 글자 크기 14px → 13px** (입력칸은 14px 유지) — 결과 목록이 과하게 커 보이던
  문제 조정.

## v2.4.5 — 2026-07-17

### 변경 (전체 UI 색감·카드 리디자인)
- **색 팔레트 리뉴얼:** 진한 청록 → **뮤트 틸 액센트(#4d938a) + 따뜻한 뉴트럴 배경(#f4f3f0) + 흰 카드**.
  오래 봐도 눈이 편한 소프트 톤으로 통일(활성 탭·카운트·태그의 솔리드 색 블록 제거).
- **카드 디자인:** 좌측 풀 높이 색바 → **Things/Bear風 짧은 색 세그먼트**. **원형 체크박스**,
  알약형 탭·태그, 소프트 섀도(hover 시 카드가 살짝 떠오름), 라운드 상향. 단계 파악(좌측 색)은 유지.
- **빠른 메모/검색 창 다크 리디자인:** 딥 차콜(#1e2128) + 틸 액센트 + 라운드 18px. 검색 결과에
  **진행/완료 상태 태그**(진행=틸, 완료=회색)를 붙이고 `[완료]` 텍스트 접두어·'내 업무' 라벨을 제거.
- **설명 텍스트 정리:** 컬럼 부제 4줄·바로 입력 힌트줄 제거, 입력/검색 placeholder를 핵심 단축키만
  남겨 축약(`바로 입력 · Ctrl+Enter` / `검색` / `빠른 메모 · Alt 검색`).
- **마감·세부 시각 태그를 긴급도 5단계 배경색으로 차등:** 지남·2시간 이내 임박(코랄 레드, 같은 색) /
  오늘 마감(오렌지) / 내일까지(앰버) / 이틀 안까지(샌드) / 그 이후(중립). 오늘·내일·이틀은 달력일
  (자정 경계) 기준. 세부할일(중간점검) 태그에도 동일 적용.
- 데이터·기능·저장 포맷 변화 없음(순수 프론트 스타일링). 커스텀 타이틀바는 Windows 창 동작 확인이
  필요해 다음 빌드로 보류.

## v2.4.4 — 2026-07-15

### 변경
- **알람 점을 "울림 여부" 3단계로 단순화.** 기존 4단계(○ 대기 · ● 울림 · ● 확인 · ● 미룸)에서
  '미룸(파랑)' 색 구분을 없애 **○ 대기(안 울림) · ● 울림(빨강, 시각 지남) · ● 확인함(초록)** 세 가지로
  줄였다. 스누즈(미룸)한 알람도 이미 울린 것이므로 '울림(빨강)'으로 함께 표시한다. (완료 업무·세부는
  점을 표시하지 않는 규칙은 그대로.)
- **세부 점검이 여러 개일 때 '울린 것(지난 미완료 점검)'을 카드에 우선 표시.** 이전에는 가장 임박한
  '미래' 점검을 먼저 보여줘서, 한참 전에 울린 점검이 미래 점검에 가려 카드 앞면에 안 보이던 문제가
  있었다. 이제 지나서 울린 점검이 있으면 그것(가장 오래된 것)을 먼저 보여줘 빨간 점이 바로 눈에 띈다.
- **주기 업무에서 '며칠마다'(N일 간격) 입력을 제거.** 이제 주기 유형은 **매주 요일 / 매월 며칠** 두
  가지다. (이미 저장된 '며칠마다' 정의는 계속 생성·표시되어 데이터 호환은 유지 — 신규 입력에서만 빠짐.)

## v2.4.3 — 2026-07-14

### 변경
- **빠른 메모/빠른 검색 창을 다시 검정(어두운) 배경으로** (v2.4.1에서 밝은 톤으로 바꿨던 것을
  소유자 요청으로 원복 — 스포트라이트풍 검정 패널, 그림자 없음). 단 **폰트는 앱과 통일한 그대로**
  유지하고, **글씨크기는 입력·검색·검색결과 모두 14px로 통일**(검색된 메모 크기 = 검색 입력 크기).

## v2.4.2 — 2026-07-14

### 수정
- **완료된 업무·세부에 빨간 알람 점(울림·미확인)이 뜨던 버그 수정**: `alarmDot`이 완료 여부를
  보지 않아, 마감이 지난 완료 업무의 카드에 '울림(미확인)' 점이 남아 있었다. 완료면 알람이
  무의미하므로 완료 업무·완료 세부에는 알람 점을 아예 표시하지 않도록 고쳤다(회귀 테스트 추가).

### 문서
- 사용 설명서 알람 설명에 "완료한 업무에는 알람 점이 표시되지 않는다" 추가, 주기 업무 진입점을
  `[설정] 메뉴 → 주기 업무 입력·관리`로 문구 정정([주기 입력] 버튼은 v2.4.0에서 설정 메뉴로 이동).
- 개발 문서 현행화: CLAUDE.md 테스트 수(88→108)·주기 생성(매주 월요일 06:00 배치)·진입점 서술 갱신,
  README 테스트 수 갱신, 기능 제안서에 버전(v3.x→v2.4.x)·Everything 제거 현행화 주석 추가.

## v2.4.1 — 2026-07-14

v2.4.0 배포 후 파일 링크 UX 개선(데이터 저장 형식·스키마 변경 없음).

### 변경
- **카드 앞면에서 파일 링크 표시 제거**: 보드 카드는 이제 *메모(2줄)·가장 빠른 세부 할일·마감*만
  보여준다. 파일은 카드를 클릭해 열리는 양식 팝업에서 다룬다.
- **양식 팝업의 파일 링크에 행별 활성/수정 토글**: 각 파일 행 왼쪽 체크 버튼으로
  **활성화**(이름이 링크로 표시 · 클릭하면 열림 · 수정 잠금) ↔ **비활성화**(경로 입력칸으로 직접
  수정, 옆의 **[찾기]** 버튼으로 파일 선택창을 열어 그 행 경로를 새로 지정) 를 전환한다.
  경로 값은 모드와 무관하게 항상 유지되어 저장/데이터 호환은 그대로다.

## v2.4.0 — 2026-07-14

'다른 계정'에서 진행되던 개편(구 v3.0.0·v3.1.0)을 정식 배포용으로 통합·정리한 버전.
기존 데이터·JSON 백업·DB 백업과 완전 호환(내부 식별자·스키마 유지 — 데이터 경로가 끊기지 않음).

### 추가
- **주기 업무 — 부모-자식 자동 생성 구조(정규화)**: **[설정] 메뉴 → 주기 업무 입력·관리**
  에서 *공통 내용(제목/메모)과 반복 주기만* 최소로 등록하는 '부모 정의'를 만든다. 예정일이 되면
  그 내용으로 **자식 업무가 보드에 자동 생성**되고, 자식은 일반 업무처럼 완료하면 [완료] 탭으로
  간다. 자식은 `recurId`로 부모를 가리키고(자식→부모), 부모는 자식 목록을 역참조한다(부모→자식).
  반복 주기 3종: **매주 요일 / 매월 며칠(말일 자동보정) / 며칠마다**. 같은 화면에서
  목록·수정·일시정지·삭제·다음 예정·생성 건수를 볼 수 있다. 카드에는 '주기' 배지 표시.
- **자식 생성은 매주 월요일 오전 6시 배치**: 월요일 06:00을 지나면 그 주(월 06:00~다음 월 06:00)에
  해당하는 회차를 한꺼번에 생성한다. 자식의 마감시각은 원래 회차 시각 그대로라, 금요일 회차도
  월요일 아침에 미리 예정·대기로 올라온다. (오래 실행하지 않았어도 14일 이내 밀린 회차는 따라잡아 생성.)
- 매월 반복에서 그 달에 없는 날짜(예: 31일)는 그 달 말일로 자동 보정.
- 주기 업무 **시각 입력은 콜론 없이도** 가능(`0930`·`930` → `09:30`으로 자동 정규화).

### 변경
- **검색 속도 개선을 위해 Everything(voidtools) 연동 전부 제거**: 보드 검색·빠른 메모창의
  파일 검색, 설정의 자동 실행/사용 토글, Rust 커맨드(`everything_search` 등)·자동 실행 로직까지
  모두 삭제. 파일 링크(카드에서 파일 열기/폴더 열기) 기능 자체는 그대로 유지.
- **빠른 메모/빠른 검색 창 UI를 메인 앱과 통일**: 폰트(Pretendard/맑은 고딕)·글씨크기·색을
  메인 앱 팔레트(종이 톤·잉크색)로 맞춘 밝은 플로팅 카드로 정리(기존 어두운 스포트라이트풍 →
  앱과 일관된 밝은 톤). 검색 결과는 '내 업무' 단일 열.
- **빠른 메모창 검색(quick_search) 최적화**: 기존 `LEFT JOIN` 4개 + `DISTINCT`는 한 업무에
  관련행(세부·관련인·식별번호·파일)이 많을수록 중간 결과가 폭증한 뒤 다시 접어야 해서 데이터가
  쌓일수록 급격히 느려졌다. 이를 상관 `EXISTS` 서브쿼리로 바꿔 각 업무를 한 번만 훑고, 자식
  검색은 기존 `item_id` 인덱스를 그대로 탄다(결과·정렬·저장 형식 동일, 스키마 변경 없음).
- **주기 업무를 기존 양식 입력에서 분리**: 양식 입력 창에는 더 이상 반복 설정이 없다(주기는
  전용 [주기 입력]으로). 예전의 '완료 시 자기 자신 재장전' 방식은 부모-자식 생성 방식으로 대체.
- **캘린더는 '진행 중'만 표시**: 완료된 업무·완료된 세부 할일은 캘린더에 나타나지 않는다.
- **'마감 지남' 표시 제거**: 상단 '기한 지남 N' 카운터와 카드의 '지남' 강조를 없앴다.
  임박 안내(남은 시간)는 **2시간 이내**만 표시.
- **빠른 메모 팝업**: 그림자 효과 제거 후 더 세련되게 정리(은은한 그라데이션·강조 점·통일된
  타이포). 검색 모드는 '내 업무' 단일 열(파일 열 제거).
- 날짜 선택 **📅 아이콘** 정상 표시(이모지 폰트 폴백 지정).
- 배포 버전 표기 **v2.4.0** (세 매니페스트: package.json·Cargo.toml·tauri.conf.json).

### 내부
- 새 구조: `recur.js`(순수 반복 계산 + `spawnDueOccurrences` 생성기), `recur-box.js`(주기
  입력·관리 UI + 생성 트리거). 부모=`it.recur` 있는 보드 밖 아이템, 자식=`it.recurId`.
  **스키마 변경 없음** — 기존 `items.recur`/`recur_id` 컬럼을 그대로 재사용(마이그레이션 위험 0).
- 자식 중복 생성 방지: 부모 `recur.next`를 전진시키고 즉시 저장. 장시간 미실행 시 14일 넘게 밀린
  회차는 건너뛰어 폭주 방지. 로드 직후 + 60초 주기로 생성 점검.
- Everything 관련 코드/테스트 제거(`everything.js`, `everything_search`·`launch_everything`·
  `load_settings_only` 커맨드와 그 테스트, 설정 토글).
- 데이터 소실 집중 검증: 주기 생성·중복 방지·부모/자식 링크·매월 말일 보정·백업 왕복 테스트 포함
  — JS 106건 전부 통과.

## v3.1.0 — 2026-07-14 (구·미배포 내부 이력 — 위 2026-08-02 정식 v3.0.0 과 무관)

### 추가
- **주기 업무(반복)**: 양식 입력에 반복 선택(매주 요일 / 며칠마다 + 시각) 추가.
  반복을 켜면 마감시각이 자동 계산되고, 완료 체크 시 그 회차의 완료 기록이 [완료] 탭에
  남고(아카이빙) 다음 회차가 자동으로 다시 올라온다(세부 체크·알람 초기화, 실행취소 지원).
  카드에 '반복 · 매주 월·수 09:00' 태그 표시. items.recur 컬럼(마이그레이션 004, append-only) —
  기존 데이터·구버전 JSON 백업과 완전 호환.
- **빠른 메모창(Ctrl+Alt+Space) 개편**: Esc/포커스 이탈 시 내용을 절대 지우지 않음(삭제는
  사용자만). 작성 중 초안을 수시로 DB(settings.captureDraft)에 저장 — 앱/PC가 꺼져도 다음
  실행 때 초안이 '분류 대기'로 자동 등록(등록 시엔 초안 즉시 비워 중복 방지). **Alt 키로
  검색 모드 전환**: 왼쪽=내 업무(quick_search), 오른쪽=파일(Everything). 업무 클릭 → 메인
  창에서 열기, 파일 클릭 → 바로 열기. 창은 스포트라이트풍(투명 창 + 둥근 패널 + 그림자),
  메모 높이 2배(128px 상당).
- **설정 메뉴 통합**: 헤더의 저장 위치/JSON 백업/불러오기/XLSX/프리셋 관리를 [설정]
  드롭다운 하나로 통합(알람 토글만 밖에 유지). 메뉴에 체크 옵션 2종 — '실행 시 Everything
  자동 실행'(일반 설치 경로 자동 탐지, 없으면 조용히 생략), '빠른 검색 시 Everything 사용'.

### 변경
- UI 전반의 이모지 제거 (알람·저장 위치 버튼, 카드 파일 링크, 캘린더 등 — 텍스트로 대체).
- 마스코트 이미지는 이번 버전에서 보류 (원본 파일 미전달).

### 내부
- 새 모듈 recur.js(순수 반복 계산)·settings-menu.js·capture 검색 모드, Rust 커맨드
  quick_search/resize_capture/load_settings_only/launch_everything, 마이그레이션 004.
- 데이터 소실 집중 검증: recur 왕복·완료 재장전 상태 보존·LIKE 이스케이프·초안 게이트(F1)
  테스트 포함 — JS 101건 + Rust 21건 전부 통과.

## v3.0.0 — 2026-07-14 (구·미배포 내부 이력 — 위 2026-08-02 정식 v3.0.0 과 무관)

프로그램 이름이 **'뭐하려 했더라'** 로 바뀌고 버전 체계가 X.Y.Z로 전환된 큰 업데이트.
기존 데이터·JSON 백업·DB 백업과 완전 호환 (내부 식별자는 유지되어 데이터 경로가 끊기지 않음).

### 변경
- **프로그램 명칭 '뭐해야 했더라' → '뭐하려 했더라'**: 창 제목·헤더·알림·트레이 툴팁·
  백업 파일명·산출물 exe 파일명까지 일괄 변경. 새로 만들어지는 데이터 폴더 이름도
  `뭐하려했더라_데이터`로 변경 (기존 설치는 config.json의 절대경로를 그대로 사용 — 영향 없음).
- **버전 체계 X.Y.Z(semver) 전환**: 헤더의 버전 표기도 `v3.0.0` 그대로 표시 (옛 `.0` 절삭 폐지).
- **캘린더 표시 규칙 단순화**: 그날이 **마감일인 업무** + 그날 시각이 걸린 **세부 할일**만 표시.
  ("시각 있는 세부가 있으면 업무 마감일을 숨김" 로직 제거 — 이제 마감일은 항상 뜬다.)

### 추가
- **파일 링크**: 양식에서 업무에 파일 경로를 연결 ([＋ 파일 찾아보기] 또는 경로 붙여넣기).
  카드의 📄 클릭 = 파일 열기, 📂 = 폴더에서 보기. 검색창에서 파일명으로도 카드가 검색되고,
  XLSX 내보내기에 '파일링크' 열 추가. DB에 item_files 테이블 신설(마이그레이션 003),
  JSON 백업에 items[].files로 포함 (구버전 백업은 자동으로 빈 값 처리 — 왕복 호환).
- **Everything(voidtools) 파일 동시검색 (선택 기능)**: Everything의 HTTP 서버 옵션이 켜져
  있으면 보드 검색 시 내 PC 파일도 함께 검색되어 검색창 아래 표시, 클릭으로 바로 열기.
  통신은 127.0.0.1 로컬 한정. Everything 미실행 시 UI가 아예 나타나지 않는 무침습 설계
  (연결 실패 후 2분간 재시도 안 함). 사용법은 `사용 설명서.md` §5 참고.

### 수정
- **드래그 순서변경 미작동 수정**: 세부 할일·프리셋·식별번호 명칭의 ⠿ 드래그가 Windows에서
  아예 시작되지 않던 문제. 원인은 JS가 아니라 Tauri 창 설정 — WebView2에서 기본값
  `dragDropEnabled:true`가 OS 파일드롭을 위해 드래그 이벤트를 가로채 HTML5 드래그를 차단.
  `dragDropEnabled:false`로 해결 (트레이드오프: 창으로의 OS 파일 끌어놓기는 사용 불가 —
  이 앱은 원래 안 씀).
- **세부 할일 전부 완료 시 '예정 · 대기'로 떨어지던 배치 수정**: 이제 세부를 하나라도
  완료한 업무는 전부 완료했어도 (상위 완료 체크 전까지) '진행 중'에 남는다 —
  '예정 · 대기'는 손 안 댄 업무 전용.

### 내부
- 프론트 모듈 `everything.js` 신설, Rust 커맨드 `pick_file_path`/`open_file_path`/
  `reveal_file_path`/`everything_search` 추가 (Everything HTTP 클라이언트는 가짜 로컬
  서버 대상 단위테스트 4건 포함). 테스트: JS 91건 + Rust 21건 전부 통과.
- `기능 제안서.md`에 v3.0.0 기준 후속 제안(§8: 완료 아카이브 월별 묶음, 기간 XLSX,
  주간 리뷰 등) 추가. `사용 설명서.md`·`README.md`·`CLAUDE.md` 갱신.
- 산출물 exe는 다음 GitHub Actions(build-windows-exe) 실행 시 새 이름·새 기능으로 갱신됨
  (현재 커밋된 `뭐하려 했더라.exe`는 파일명만 바꾼 v2.31 빌드).

## v2.31 — 2026-07-11

툴바·기능을 덜어내는 정리 업데이트 — "단축키 → 메모창 → Ctrl+Enter 등록" 흐름만 남기고
주변 부가 기능(정기함·단축키 설정·상주 토글·저장 실패 표시)을 제거했다.

### 변경
- **미니 캡처(메모창) 단축키를 Ctrl+Alt+Space 고정으로**: 단축키 변경 UI(⚡ 빠른 메모
  버튼·설정 모달)를 삭제. 다른 프로그램 사용 중 단축키를 누르면 메모창이 뜨고,
  입력 후 Ctrl+Enter로 '분류 대기'에 등록되는 동작 자체는 그대로 유지.
- 트레이 메뉴를 '열기 / 종료' 둘로 정리 ('빠른 메모' 항목 삭제 — 메모창은 단축키로만).
- 창 X = 트레이로 숨김 동작은 유지 (토글 UI만 제거, 기본값 켬).

### 삭제
- **정기함(반복 일정) 기능 제거**: 🔁 정기함 버튼·모달·생성기(reconcileRecur)·카드 🔁 배지
  일괄 삭제. 이미 보드에 올라온 회차는 일반 카드라 그대로 남는다. DB의 recur_defs
  테이블과 JSON 백업의 recurDefs 항목은 **데이터 보존을 위해 왕복만 유지**(스키마
  마이그레이션은 append-only 원칙상 되돌리지 않음) — 기능만 사라졌고 데이터는 유실되지 않음.
- 헤더의 '⚠ 저장 실패' 상태 표시(saveStatus/setStatus) 제거 — 초기 로드 실패 시의
  안내(alert + 저장 차단)는 그대로 유지.
- 설정 모달에 있던 '윈도우 시작 시 자동 실행' 토글 제거 (기존에 켜둔 PC는 그대로 동작).

### 내부
- 프론트 모듈 `recur-box.js` 삭제, `capture-bridge.js`는 메모 라우팅+트레이 안내만 남김.
- Rust 커맨드 `save_recur_defs`/`set_capture_shortcut`/`set_autostart` 제거
  (백업 복원 경로의 recur_defs 저장은 유지).
- 테스트 JS 110 → 88개 (제거 기능의 테스트 삭제), Rust 17개 유지. `cargo check` 경고 0.
- 버전 v2.31 (매니페스트 2.31.0 ×3 + 락파일).

### 산출물
- `최종 프로그램 산출물/뭐해야 했더라.exe`를 v2.31 빌드로 갱신 (build-windows-exe
  워크플로 run #3 — windows-latest MSVC, 테스트 JS 88·Rust 17 통과 후 빌드, 단일 exe 12.5MB).

## v2.3 — 2026-07-11

v2.2 이후의 작은 중간 업데이트(v2.21, v2.22, v2.23)를 묶어 정식 반영한 **큰 업데이트**.
(v2.23의 다음 버전이 v2.3이다 — 위 버전 순서 규칙 참고.)

### 변경
- **보드 정렬 통일**: 세부할일(점검 시각)과 할일(마감시각)의 레벨 구분을 없애고,
  둘 중 먼저 도래하는 시각 순(오름차순)으로 카드를 정렬. 시각이 없는 카드는 뒤로,
  그 안에서는 최신 등록 순.
- **시각 도래 빨간 카드 제거**: 마감이 지났거나 임박했을 때 카드 전체 배경을
  빨갛게 칠하던 표시(urgent)를 삭제. 마감 태그(pill)의 빨간 지남/임박 표시는 유지.
- 프론트엔드 구조 개편(v2.21): 단일 `app.js`를 관심사별 ES 모듈로 분리
  (`state/store/render/form/placement/datetime/calendar/alarms/backup/presets/dom-utils/main`).
- 도메인 봉합선 정리: 아이템 생성을 `makeItem()` 팩토리로, 완료 토글을 `toggleDone()`
  도메인 연산으로 통일하고, 검색 술어를 `filters.js`로 분리 (동작 변화 없음 —
  반복 일정 등 후속 기능이 얹힐 기반).

### 추가
- **정기함(반복 일정)**: 매일/매주(요일 지정)/매월 반복 업무를 정기함(🔁)에 등록하면
  도래한 날에 일반 카드로 보드에 자동 등장(스폰). 반복 규칙은 아이템 속성이 아니라
  보드 밖 생성기(recur_defs)로 분리 — 체크=완료로 떠나는 보드 규칙과 `placeOf()` 불변.
  DB 마이그레이션 002(recur_defs 테이블 + items.recur_id), JSON 백업 자동 왕복.
- **전역 단축키 미니 캡처 창**(v2.23): 다른 프로그램 사용 중에도 단축키로 빠른 메모 입력.
- **트레이 상주 + 자동 시작**(v2.23): 창을 닫아도 트레이에 남고, 윈도우 시작 시 자동 실행 옵션.
- JS 테스트 하네스: node:test + jsdom 기반 자동 테스트 (현재 JS 110개 + Rust 17개).
- LICENSE 파일 추가.

### 수정 (v2.22)
- 업무 삭제 시 실행취소(토스트) 제공.
- 날짜/시간 오입력 거부(빨간 테두리 표시 + 저장 차단).
- 백업의 타임존 오류, torn-copy(불완전 복사) 방지, DB 무결성 게이트 수정.

### 기록
- 협업 워크플로 문서화(브랜치+PR 규칙), 시장 리서치 보고서 추가.
- `CHANGELOG.md` 도입 — 이 버전부터 매 업데이트의 변경 내역을 기록.
- `기능 제안서.md` 추가 — 공공기관 내부망 관점 기능 추가 vs 단순 유지 제안 (보고서로만 유지, 신규 기능 개발은 보류).
- `사용 설명서.md` 추가 — 사용자 관점의 친절한 안내서 (시작하기·기능별 사용법·FAQ).
- README를 v2.3 현재 상태로 정비(전역 캡처·트레이 기능, 모듈 구조, 문서 목록).
- GitHub Actions 수동 빌드 워크플로 추가(`build-windows-exe.yml`) — Actions 탭에서
  버튼 한 번으로 Windows 러너가 정식 MSVC 빌드를 돌려 exe를 브랜치에 커밋.
  push/PR에는 반응하지 않는 수동 전용 (상시 CI 아님).

### 산출물
- `최종 프로그램 산출물/뭐해야 했더라.exe`를 **v2.3 빌드로 갱신 완료** (GitHub Actions
  windows-latest, MSVC 정적 링크 단일 exe 12.6MB, 테스트 JS 110·Rust 17 통과 후 빌드).
- 이후 갱신 방법 두 가지:
  ① GitHub **Actions 탭 → build-windows-exe → Run workflow** (Windows 러너가 MSVC 빌드 후 exe를 브랜치에 자동 커밋),
  ② Windows PC에서 `npm run tauri build` 후 `src-tauri/target/release/wmhh-desktop.exe`를 `뭐해야 했더라.exe`로 개명해 교체.
  (리눅스 MinGW 크로스빌드는 WebView2Loader.dll 동적 의존이 생겨 단일 exe 원칙에 맞지 않아 불채택.
  MSVC 정적 링크가 되는 Windows 네이티브 빌드만 공식 산출물로 사용.)

## v2.2 — 이전 릴리즈

구조 분석 기반 안정성·보안 전면 개선. 상세는 `구조 분석 보고서.md` 참고.
