# 변경 이력 (CHANGELOG)

이 프로젝트의 모든 주요 변경을 기록한다. 형식은 [Keep a Changelog](https://keepachangelog.com/ko/)를 따르고,
버전은 `X.Y.Z` 로 매긴다.

## 버전 규칙

- **Y (기능)** — 기능 추가 시 `Y` 를 +1 하고 `Z` 를 0 으로 되돌린다. (예: 0.1.3 → 0.2.0)
- **Z (개선)** — 사소한 개선·버그 수정 시 `Z` 를 +1 한다. (예: 0.2.0 → 0.2.1)
- **X (대변경)** — 하위호환이 깨지는 대전환에서만 올린다.

버전은 세 곳을 함께 맞춘다: `package.json` · `src-tauri/tauri.conf.json` · `src-tauri/Cargo.toml`.

---

## [0.2.0] - 2026-07-23

### 추가 (기능)
- **관련법령(부처 교차) 수집 범위 확장** — 국토부·고용부·공정위·중기부 등 11개 부처의
  관련법령 28개 법령군(75문서)을 `data/seed.json` 의 `relatedLaws` 로 추가. 수집
  파이프라인이 핵심 법령·조달청 행정규칙 전수와 함께 이들을 전문 수집한다.
  재정경제부 소관 혁신제품구매규정도 개별 지정으로 보완(`docs/law-coverage.md`).
- **맥 Spotlight 식 빠른 검색 오버레이** — 앱 어디서나 `Ctrl+Shift+Space` / `Ctrl+K` 로
  중앙 검색창을 띄운다. 데스크톱 앱은 Tauri 전역 핫키로 다른 창을 쓰다가도 불러온다.
- **검색 대상 스코프 전환** — 검색창 칩 클릭 또는 `Ctrl` 단독 탭으로 전체·법령·행정규칙 순환.
- **설정 버튼(⚙) — 5기능 통합** — 테마(라이트·다크·시스템) · 법령노트 저장위치 확인·변경 ·
  JSON 백업 · JSON/DB 불러오기 · XLSX 다운로드. XLSX 는 무의존(라이브러리 0)으로 생성.
- **법령노트 SQLite 영속화** — 데스크톱 앱에서 노트를 SQLite 로 저장(브라우저는 localStorage).
- **Windows 배포 파이프라인 (GitHub Actions)** — `secrets.LAW_OC` 로 라이브 수집·빌드 후
  Windows 러너에서 Tauri `.exe`/`.msi` 를 만들어 `최종 프로그램 산출물/` 에 제공.
- **별표/별지 원본 뷰어 전환** — 罫線 파싱표 표시를 제거하고 **원문(PDF/HWP)만** 뷰어로
  제공한다(파싱표는 신뢰 불가). 검색 색인용 텍스트는 유지 → 별표/별지 내용도 검색되고,
  검색 결과 클릭 시 해당 원문 PDF 로 이동. 파일만 있는 별지 서식도 뷰어로 열린다.
- **전수조사 병렬 수집** — `collect.mjs` 에 동시성 제한 풀(`--concurrency`, 기본 6).

### 개선
- 조문항 표기법(항·호·목 계층 들여쓰기)을 옆 미리보기·노트참조에도 적용.

### 수정
- **테스트 스위트 복구** — `type:module` 환경에서 브라우저 인라인 스크립트(`module.exports`
  방식)를 Node `require` 가 빈 객체로 읽어 100건 중 55건이 실패하던 문제를, 해당 파일들을
  `.cjs` 로 전환해 해결. 전 테스트 통과.

---

## [0.1.0] - 2026-07-22

### 추가
- 조달 핵심 법령·계약예규 + 조달청 소관 행정규칙 전수(문서 320 · 조문 8,075 · 별표 1,869) 실수집.
- 오프라인 즉시검색 + 원문 열람 + 인용 클릭 점프 + 국소 관계 그래프(자족형 `web/index.html`).
- 규칙기반 관계추출(위임·인용·소속), 법령 신선도 판정(현행/시행예정·확인 경과).
- 별표 罫線 텍스트 → HTML 표 변환, 즐겨찾기·법령노트.
- Tauri 데스크톱 앱 스캐폴드.
