# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Collaboration workflow (user-defined — two co-equal collaborators)

`Milkbuttercheese2/WhatIhavetodo` (owner's GitHub account) is the **single shared repo** — both collaborators have Write access directly on it. There is no fork-based workflow anymore: `origin` points straight at `Milkbuttercheese2/WhatIhavetodo`. (An older personal fork still exists as a backup/history artifact from before the collaborator invite, but is not part of the active workflow — don't push there.)

**Never commit directly to `main`.** For any change:
1. Branch off `main`: `git checkout -b <type>/<short-name>` — prefix matches this repo's commit-message convention (`feat/`, `fix/`, `docs/`, `refactor/`, `test/`), e.g. `feat/recurring-tasks`.
2. Push the branch to `origin` and open a PR (branch → `main`) **within this one repo** — not cross-repo.
3. Merge via the PR once reviewed; delete the branch after.

Branch-protection rules on `main` (require PR, block direct push) have **not** been configured yet on GitHub as of 2026-07-11 — that requires the repo owner's admin access, which the assistant does not have. Until it's set, direct pushes to `main` are *technically* possible but against this project's convention; follow the branch+PR flow regardless. There is no always-on CI on this repo — `npm test` / `cargo test --lib` are run locally before opening/merging a PR. Both GitHub Actions workflows are **manual `workflow_dispatch` only**: `build-windows-exe.yml` runs both test suites, does the official MSVC release build on windows-latest, and commits the exe to the branch it was run on; `win-render.yml` (added v2.5.8) renders each screen in the runner's Edge (same Chromium/Blink engine + Windows fonts as WebView2) via `tools/win-render.mjs` and fails on horizontal overflow — run it when a layout change could regress differently on Windows than on a Linux dev box.

**License:** GNU AGPL v3 since v2.5.13 (was Apache 2.0) — redistributing the source or offering it as a network service requires publishing the source under AGPL v3; personal/internal-organization use carries no such obligation.

## Versioning convention (user-defined)

Current release = **v3.6.5** (직전 정식 v3.0.0 (2026-08-02 — v2.7.0~v2.10.0 의 대규모 기능 개편(전화번호부·@태그·
메모 합치기·통합 디자인)을 마무리해 묶은 정식 배포. 소유자 결정으로 아래 경고의 '3.x 금지'를
해제하고 번호를 재사용했다 — CHANGELOG 의 2026-07-14 자 구 v3.0.0·v3.1.0 항목은 여전히
미배포 내부 이력이며 이 판과 무관, '(구·미배포)' 표기로 구분).

**버전 정책 갱신(소유자 지정, 2026-08-02): Y=기능 추가, Z=기능이 아닌 로직 변경·디버깅·조정.**

> ⚠️ **버전 이력 주의 (반드시 읽을 것 — 과거에 여기서 혼선이 있었다):** 저장소 git 히스토리에는
> `v3.0.0`·`v3.0.1` 커밋과 CHANGELOG 항목이 남아 있지만, **v3.x는 실제로 배포된 적이 없다.**
> 그 작업은 *다른 계정에서 진행되던 미배포 개편*이었고, 당시 파일 링크·이름 변경 정도의 변화를
> 두고 **대규모 개편(major)도 아닌데 버전을 3.0.0으로 임의 상향**해 버린 것이다(잘못된 범프).
> 저장소 소유자 결정으로 이를 바로잡아, 실제 정식 배포는 **구 v2.31 라인을 잇는 `v2.4.0`**
> 으로 되돌렸다. 따라서:
> - **현재/실제 배포 버전 = v2.5.14.** 세 매니페스트(package.json·Cargo.toml·tauri.conf.json) 동일.
> - v3.0.0·v3.0.1 CHANGELOG 항목은 *내부 이력*으로만 읽고, 실 배포 번호로 착각하지 말 것.
> - **다음 버전은 v2.5.x(버그·사소한 조정) / v2.6.0(기능 추가)로 이어간다.** 진짜 호환성 깨지는
>   대규모 개편이 아닌 한 다시 3.x로 올리지 말 것. (자동 업데이터·버전 비교 로직이 없어 역전은 무해.)
> - **버전업 정책(소유자 지정): 사소한 수정이라도 새로 배포(빌드/머지)할 때마다 패치(Z)를 올린다.**
>   매 배포마다 세 매니페스트 버전을 함께 올리고 CHANGELOG에 해당 버전 항목을 추가하며 PR 제목에도
>   `[vX.Y.Z]`를 붙인다.

**The project uses standard X.Y.Z semver** (X = 큰 개편/호환성 변화, Y = 기능 추가, Z = 버그 수정·소규모 조정), and the same `"X.Y.Z"` string goes into all THREE manifests together (`src-tauri/Cargo.toml`, `src-tauri/tauri.conf.json`, `package.json`). **버전을 올린 직후 `cd src-tauri && cargo check` 를 한 번 돌린 뒤 `git add` 할 것** — `Cargo.lock` 의 `wmhh-desktop` 버전은 cargo 가 갱신하므로, 안 돌리고 커밋하면 lock 만 옛 버전으로 남아 이후 로컬 빌드마다 작업트리가 더러워진다(v3.3.7·v3.3.8 에서 두 번 반복됐다). The UI header shows `getVersion()` verbatim (v3.0.0에서 구 십진수 규칙의 trailing-`.0` 절삭을 폐지 — `main.js`). The app was also renamed at v3.0.0: **'뭐하려 했더라'** (formerly '뭐해야 했더라'); the internal `productName`/crate name stay `wmhh-desktop` and the bundle identifier stays `com.wooseongkyun.wmhh` on purpose — changing the identifier would move `app_local_data_dir()` and orphan existing users' config/data. Legacy versions (≤ v2.31) used a decimal-magnitude scheme (v2.2 → v2.21 → … → v2.3(=2.30) → v2.31): read old CHANGELOG entries with that rule, and note those old manifest versions sort non-monotonically as semver (harmless — no auto-updater or version-comparison logic exists; don't add tooling that assumes semver ordering across the v2 era). A structural analysis lives in `구조 분석 보고서.md`; the built exe ships in `최종 프로그램 산출물/뭐하려 했더라v{X.Y.Z}.exe` (v2.5.3부터 파일명에 버전 표기 — 소유자 지정; 빌드 워크플로가 이전 버전 exe를 지우고 최신 하나만 남긴다) and IS committed to git per user request.

**Changelog rule (user-defined, since v2.3):** every update pushed to GitHub must add an entry to `CHANGELOG.md` describing what changed (Korean, grouped 변경/추가/수정). Started at the v2.2→v2.3 transition — write the entry as part of the same PR/commit that makes the change.

## Migration in progress

This repo is being converted from the legacy single-file HTML app (still in `legacy/`) into a **Tauri (Rust) + SQLite** desktop app for offline/air-gapped 공무원 내부망 deployment as a small portable `.exe`. The full rationale and architecture (DB schema, data-safety measures, migration/versioning approach, why Tauri, why EAV over a JSON column, etc.) live in the plan doc at `C:\Users\<user>\.claude\plans\rustling-seeking-flurry.md` — read it before making architectural changes to `src-tauri/`.

Status: **Phase 1 and Phase 2 both done.** The frontend now lives in `src/` as browser-native ES modules (see "Frontend layout" below) with `STORE` delegating to Rust via `invoke()`; SQLite is the single source of truth (IndexedDB/localStorage are no longer used). The legacy HTML file in `legacy/` remains the behavioral reference if a business-logic question arises, but `src/` is the live implementation.

## Legacy app (`legacy/뭐해야 했더라v1.41.html`)

Single-file, offline-first Korean personal task-tracking web app (a phone-call triage board — "떠올려서 던져둔 일들을 잊지 않고 처리하기"). It is the **entire (old) application**: one HTML file containing inline `<style>` and `<script>` blocks, plus a vendored copy of SheetJS (xlsx 0.18.5) inlined at the top of the first `<script>` for offline/air-gapped XLSX export. No build step, no package manager, no bundler, no server — opened directly in a browser. Kept around purely as the behavioral reference during the Tauri port; not being developed further itself.

## Running / testing changes

**Rust backend (`src-tauri/`)**: `cd src-tauri && cargo test --lib` runs the DB round-trip test suite (`src-tauri/src/db/tests.rs`) — in-memory + real-file-path tests covering save/load, replace-not-merge semantics, backup export/import, a simulated-restart reopen, and backup rotation pruning. `cargo check`/`cargo build` from `src-tauri/` compile the app. `npm run tauri dev` (repo root) runs the full desktop app. Rust isn't on PATH by default in a fresh shell on this machine — prefix commands with `export PATH="/c/Users/<user>/.cargo/bin:$PATH"` (bash) if `cargo`/`rustc` aren't found.

**Frontend (`src/`)**: no build step, no bundler — `src/` is served raw by the Tauri webview (`frontendDist: "../src"`). **`npm test`** runs the node:test suite in `tests/` (296 tests: pure-logic in `tests/unit/`, jsdom-based DOM tests in `tests/dom/`). The DOM tests need `jsdom`, the repo's only devDependency — run `npm install` first in a fresh clone, or all 14 `tests/dom/*` files fail with `ERR_MODULE_NOT_FOUND` while the unit tests still pass. `tests/helpers/env.js` builds the window/document/`__TAURI__` fake — in DOM test files, src modules MUST be dynamically imported *after* `setupEnv()` (store.js destructures `window.__TAURI__.core` at import time), and `mock.timers.enable(...)` must run at module scope or real `setInterval`s from `init*()` hang the test process. Never import `main.js` in tests (its load IIFE runs at import). dt-widget-shaped fixtures need seconds zeroed (`d.setSeconds(0,0)`) or no-change round-trips falsely re-arm alarms. Still verify UI-feel changes manually via `npm run tauri dev`; `node --check src/<file>.js` catches syntax errors.

**Dev tooling (`tools/`, not shipped in the app)**: **`capture-check.mjs` (v3.4.9) — 미니 창(빠른 메모·빠른 검색)을 고치면 반드시 이걸 돌린다**: `node tools/capture-check.mjs` 가 31가지 상태(검색 0·3·30건·스크롤·↑↓, 메모 빈칸·1줄·12줄·긴 한 줄, @자동완성 후보 1·5·10·12·50명·키보드 이동·삽입·ESC, 모드 토글, 플래시)를 실렌더로 돌며 '창 스크롤 0 / 패널이 창 안 / 입력칸이 창 안 / 목록 min(N,10)행 온전 / 첫 행 온전 / 반쯤 걸친 행 없음 / 백드롭 글자 배치 일치 / 좌우 대칭 / 가로 넘침 없음'을 단언한다. **핵심은 배율 흉내다** — `UI`(uiScale)×`TEXT`(Windows 텍스트 크기 조정) 를 곱한 배율로 CSS 뷰포트를 만들고 Rust `note_capture_ratio` 계산까지 재현하므로, v3.4.8 의 원인이던 텍스트 배율 불일치가 재현된다(기본 1.2×1.35). 배율 조합을 바꿔 `UI=0.9 TEXT=2.25 node tools/capture-check.mjs` 처럼 돌린다. **`interact-check.mjs` (v3.5.2) — @자동완성 드롭다운이나 관련 업무 팝업을 고치면 이걸 돌린다**: `node tools/interact-check.mjs` 가 진짜 앱을 Edge 에 띄워(`__TAURI__` 는 guide-shots 식 스텁) 사람처럼 조작하며 21가지를 단언한다 — 후보 15명에서 '정확히 10행·반쯤 걸친 행 없음', 휠·↓ 로 스크롤해도 안 닫힘(ESC 로만), 전화번호부 행 → 관련 업무 팝업 → **각 줄을 눌러 그 줄의 양식이 열리는지**(완료 업무 포함), 10회 반복, 갓 켠 창에서 최단 경로. **이게 필요한 이유**: `tests/dom/phonebook.test.js` 는 `initRender()` 를 부르지 않아 '팝업의 업무 클릭 → 양식'(render.js 의 body 위임) 경로가 검증된 적이 없었고, 스크롤·클리핑 결함은 실제 레이아웃이 있어야만 드러난다. ⚠️ **드롭다운 높이는 행 높이 고정 + 산술이다**(`#atDrop max-height:346 = 10×34 + 위 패딩 4 + 테두리 2`; 아래 패딩을 더해 350 으로 잡으면 11번째 행이 빼꼼 보인다 — 미니 창이 v3.4.6 에서 겪은 함정과 같다). `.at-item` 의 height 를 바꾸면 이 346 도 함께 고칠 것. `win-render.mjs` (v2.5.8) drives real Edge to assert no `scrollWidth > clientWidth` overflow per screen — this is what catches Windows-only layout regressions that a Linux render can't reproduce (font metrics + native control widths differ); `guide-shots.mjs` regenerates `docs/screenshots/*.png` for the 사용 설명서 after UI changes. Both install `playwright-core` with `--no-save` so the app's package.json/lockfile stay untouched.

**Legacy HTML app (`legacy/뭐해야 했더라v1.41.html`)**: no build or test command. Verify by opening the file directly in a browser and exercising the UI manually — add an item, check board placement, check the calendar/completed tabs, check backup export/import.

Historically (pre-migration) this project bumped the HTML filename itself per version (e.g. `뭐해야했더라1.2.html` → `뭐해야 했더라v1.41.html`) — check `git log` if that matters.

## Rust backend layout (`src-tauri/src/`)

- **`AppDb::conn()` (v3.3.4) — DB 잠금은 반드시 이걸로 잡는다, `conn.lock()` 직접 호출 금지.** `Mutex::lock()` 은 잠금을 쥔 스레드가 패닉하면 그 뒤 **영원히** Err 를 돌려준다(poisoning). 예전엔 그 Err 를 커맨드 실패로 올려서, 한 번 패닉하면 세션의 모든 저장·읽기가 "저장 실패"로 굳었다 — 사용자는 모른 채 계속 작업하다 그대로 잃는다. 중독은 데이터 손상이 아니다(패닉 시 진행 중이던 트랜잭션은 자동 롤백되고 DB 는 마지막 커밋 상태) → `conn()` 은 중독을 지우고 내부 값을 그대로 쓴다.
- **`db::with_write_retry` (v3.3.4):** 모든 쓰기 커맨드를 감싼다. `busy_timeout` 은 SQLite 가 '잠금 대기'로 인식한 경우만 기다려 주는데, 실무에서 저장을 깨뜨리는 나머지 절반(백신·백업 도구·탐색기 미리보기의 순간적 파일 점유)은 `SQLITE_IOERR` 로 올라와 한 번에 실패했다. `is_transient` 가 BUSY/LOCKED/IOERR 만 재시도 대상으로 보고, 제약 위반 같은 영구 오류는 즉시 올려 보낸다(재시도해도 같으므로 알림만 늦어진다).
- `lib.rs` — Tauri builder/setup: resolves the DB path under `%LOCALAPPDATA%` (per-user, no admin rights needed — see `app.path().app_local_data_dir()`), opens the DB, decides the integrity verdict once at startup via `db::integrity_verdict()` and stores it in `AppDb.integrity_ok` (an `AtomicBool`), registers all commands. **v3.3.9 — "검사를 못 했다"와 "검사 결과 손상이다"를 절대 같이 다루지 말 것.** 예전엔 `integrity_check` 가 Err 를 내면(=대개 잠금) 그것도 손상으로 보고 세션의 저장·읽기를 통째로 잠갔다. 그래서 앱을 껐다 2~3초 만에 다시 켜면(이전 프로세스가 아직 파일을 쥔 찰나) 파일이 멀쩡한데도 "저장이 잠겨 있습니다 / 1. 트레이 종료 2. 재실행" 안내가 떴다. `integrity_verdict` 는 Err 면 0.3초 간격으로 다시 해 보고, 끝내 못 끝내면 **손상으로 단정하지 않는다**(`Ok(Err(report))` 일 때만 잠근다).
- `commands.rs` — thin `#[tauri::command]` wrappers (`load_all`, `save_all`, `save_fields`, `save_presets`, `save_id_kinds`, `save_settings`, `backup_export`, `backup_import`, …) that just call into `db::*` and map errors to `String`, plus the global mini-capture shortcut: `CAPTURE_SHORTCUT` is **fixed to Ctrl+Alt+Space since v2.31** (no runtime re-binding; the old `set_capture_shortcut`/`set_autostart`/`save_recur_defs` commands were removed). it also has `pick_file_path`/`pick_folder_path`(v2.10.0 폴더 링크)/`open_file_path`/`reveal_file_path` (file/folder links, via the dialog/opener plugins — opener 는 폴더를 탐색기로 연다) and `quick_search`/`resize_capture` (mini capture window), plus v2.7.0 `save_phonebook`/`phonebook_search`/v3.0.1 `phonebook_list` (전화번호부 — 뒤 둘은 capture webview 전용 읽기 전용: 검색/전체 목록(본문 태그 하이라이트 실존 판정), same rationale as `quick_search`). **v2.5.20:** `choose_data_dir` refuses to relocate INTO a folder that already holds a `data/wmhh.sqlite` (returns an error before staging anything), so the next-launch `apply_pending_move` copy can't clobber another install's dataset. (The `everything_search`/`launch_everything`/`load_settings_only` commands were **removed in v2.4.0** along with the whole Everything integration — search got slow.) Business logic does **not** belong here or in `db/` — it stays in the frontend JS (see Migration-in-progress note above); Rust is CRUD-only.
- `db/model.rs` — serde structs matching the frontend's JSON shapes exactly (`Item`, `Contact`, `Identifier`, `SubTask`, `FieldDef`, `Preset`, `PhonebookEntry`, `Settings`, `AppState`, `BackupPayload`). Field names/renames here are load-bearing — they define the wire format `invoke()` calls and JSON backups use.
- `db/schema.rs` + `db/migrations/*.sql` — ordered migrations run via `rusqlite_migration`, tracked through SQLite's `PRAGMA user_version`. **Only ever append new `M::up(...)` migrations; never edit or reorder a shipped one** — there's no auto-updater on the target intranet, so an install may jump straight from an old schema version to the newest, skipping releases.
- (v3.5.0 migration `008_contact_email.sql` — `contacts`·`phonebook` 에 `email TEXT NOT NULL DEFAULT ''`. 모델의 `#[serde(default)]` 가 **구 JSON 백업 복원이 깨지지 않는 이유**다: 새 필드를 추가할 때 이걸 빼면 기존 백업이 전부 실패한다.)
- `db/meta.rs` (v3.3.7 번호표 `data_version` — 마이그레이션 `007_data_version.sql` 의 `meta` 테이블. **`settings` 에 두지 말 것**: 설정 저장은 테이블을 통째로 지우고 다시 써서 번호가 함께 날아간다), `db/items.rs`, `fields.rs`, `presets.rs`, `id_kinds.rs`, `settings.rs`, `phonebook.rs` (v2.7.0 전화번호부 — search_phonebook 은 미니 창 @ 자동완성용 읽기 전용 검색), `recur_defs.rs` (v2.3 정기함 definitions — the 정기함 *feature* was removed in v2.31, but this module and its table stay so old data keeps round-tripping through `load_all`/backups; migrations are append-only) — one module per table group, each exposing a `save_*_tx(&Transaction, ...)` (the actual delete+reinsert logic) plus a `save_*(&mut Connection, ...)` convenience wrapper that opens its own transaction and calls the `_tx` version. This split exists so `db/backup.rs::import_payload` can compose all of them into **one** all-or-nothing transaction when restoring a JSON backup.
- `db/alarm.rs` — encodes/decodes the single-key alarm-fired state (`items.due_alarm`, `subtasks.alarm`) between the JS shape (`true` or a snooze-until epoch-ms number) and the TEXT column. Snooze itself was removed from the UI in v2.5.11 (alarm popup is [확인] only) but stored snooze-until values still decode, so don't drop that arm. An *unrecognized* TEXT value decodes to unfired (`Fired(false)`) on purpose — the safe direction is to ring again, not to silently swallow an alarm.
- `db/mod.rs` — `open()` (pragmas + migrate), `integrity_check()`, `rotate_backup()` (timestamped `.sqlite` copies under a `backups/` dir, pruned to the newest N), `now_stamp()`. Backup files carry mixed prefixes (`wmhh_`, `wmhh_prerestore_`, `wmhh_preimport_`, `wmhh_presave_`), so recovery and pruning MUST sort by the embedded `YYYYMMDD_HHMMSS` stamp via `stamp_key()`, never by raw filename — a plain lexical sort puts `'p' > '2'` and so picked a stale pre* snapshot as "newest", which restored old data and pruned the real latest backup (data-loss bug fixed in v2.5.11; regression tests cover it). **v2.5.20** added two data-safety measures here: (1) `save_items_guarded()` — the write path `save_all` goes through this instead of `items::save_items` directly; when the item count drops drastically (had ≥`SHRINK_SNAPSHOT_FLOOR`=5 rows AND the new set is ≤ half) it takes an UNCONDITIONAL `wmhh_presave_*` snapshot BEFORE the destructive delete+reinsert, so a mass-delete/accidental-truncation always leaves a pre-shrink restore point even when the throttled after-save rotation would skip; (2) `open_with_recovery` now iterates backups newest-first (`backups_newest_first`) and only adopts one that passes its own `PRAGMA integrity_check` (`backup_is_healthy`) — a corrupt newest backup is skipped in favour of an older good one instead of being restored blindly (symmetric with the manual `.sqlite` import path, which already probed integrity). **v3.3.6:** `open_with_recovery` 는 백업으로 덮기 **전에** 원본을 `wmhh.before-recovery-*.sqlite` 로 반드시 복사해 두고 안내문에 그 이름을 적는다 — 복구는 되돌릴 수 없고, 원본이 사실 멀쩡했다면(일시적 잠금) 그 순간 최신 데이터가 사라지기 때문이다. 같은 판에 `log_line()`/`exe_desc()` 가 추가됐다(아래 진단 로그 참조).

Ids are never reassigned by the DB layer — `items.id`/`subtasks.id` are caller-supplied (from the frontend's `newId()`) and inserted as-is, not autoincremented, so alarm state embedded on a subtask stays attached to the right row across saves. Custom (non-`received`/`due`) item fields live in an EAV table (`item_fields`) rather than a JSON column, chosen specifically so the hand-rolled migration system can use plain `INSERT/UPDATE/DELETE` rather than JSON-path surgery.

## Frontend layout (`src/` — browser-native ES modules, no bundler)

Split from the former single-file `app.js` in v2.21 along single-responsibility lines. Two rules keep the module graph safe (import specifiers are relative `./name.js` with explicit extensions; there is no bundler to fix mistakes):

1. **Feature modules contain only hoisted `function` declarations plus module-local `let` state — no top-level statements besides `import`.** All listener registration, `setInterval`s, and initial render calls live in an exported `init*()` that `main.js` calls in explicit order. This is what makes the two deliberate function-only import cycles (render↔form via `openForm`/`persist`, render↔calendar via `cardHtml`/`renderCal`) safe; don't add top-level `const x = importedValue` inside a cycle member.
2. **All cross-module mutable state lives in the single `S` object** (`state.js`) — mutate properties (`S.items = ...`), never rebind imports. `window.items/FIELDS/PRESETS/ID_KINDS/SETTINGS` are write-only devtools mirrors of `S.*` (kept for console debugging); code always reads `S`.

- `views.js` (v3.6.4) — **화면(탭) 레지스트리.** 각 모듈이 `init*()` 에서 `registerView({key, els, render})` 로 자기 화면을 등록하고, main.js 의 탭 클릭 핸들러는 `showView(key)` 한 줄이다 — **화면을 추가할 때 main.js 는 건드리지 않는다.** `els` 의 각 항목은 `display` 를 주면 그 값으로 켜고(끌 때 `'none'`) 안 주면 `classList` 의 `'on'` 으로 켠다(기존 마크업의 두 방식 그대로); `when` 은 한 화면 안에서 조건부로 갈리는 요소용이다(보드의 4열/5열 — `placeMode()`). ⚠️ **현재 탭을 이 모듈에 저장하지 말 것** — 진실은 DOM 의 `.tab.on` 하나다(구 `syncBoardVisibility` 도 같은 것을 봤다). ⚠️ `refreshView()` 는 표시만 다시 반영하고 **render 를 부르지 않는다** — 보드의 그리기 함수가 `render()` 이고 `render()` 가 `syncBoardVisibility()`→`refreshView()` 를 부르므로, 여기서 render 를 부르면 무한 재귀다.
- `modals.js` (v3.6.4) — **팝업 스택.** `openModal(id,{close,save})` / `closeModal(id)` / `closeTop()` (ESC) / `topSave()` (Ctrl+S·Ctrl+Enter) / `isOpen(id)`. '맨 위'는 **z-index 를 CSS 에서 그때그때 읽어** 판정하고(같으면 나중에 연 것) JS 에 베껴 적지 않으므로, 화면에서 실제로 위인 것과 어긋날 수 없다. **왜 있는가:** 예전엔 팝업 여는 코드는 각 모듈에 있는데 ESC·Ctrl+S·Ctrl+Enter 가 무엇을 고를지는 main.js 의 **손으로 적은 목록 셋**이 정했고, 그 줄 순서가 곧 규칙이라 세 번 어긋났다 — v2.11.0(양식 위 관련 업무 팝업이 떠 있는데 ESC 가 밑의 양식을 닫음), `recurModal` 이 ESC 목록에서 누락(주기 업무 팝업에서 ESC → 밑의 설정 팝업이 닫힘), `pbSyncModal` 을 ESC 로 닫으면 `closePbSync()` 를 안 거쳐 `pendingSync` 가 남음. ⚠️ **각 모듈의 닫기 함수는 반드시 `closeModal(id)` 을 거쳐야 한다** — `classList.remove('on')` 을 직접 부르면 화면만 닫히고 스택에 유령이 남는다. `close` 는 화면에서 지우는 것 말고 **더 할 일이 있을 때만** 준다(양식의 초안 플러시=`closeForm`, 동기화 팝업의 대기 목록 비우기, 알림창은 `$('alarmOk').click()` — 그냥 감추면 F5 재알림 게이트가 풀린다). `save` 는 그 팝업을 소유한 모듈이 자기 저장 버튼을 등록한다(예전처럼 main.js 가 `$('fm-save').click()` 으로 남의 버튼을 대신 누르지 않는다). 캡처 단계에서 ESC 를 먼저 먹는 `#atDrop`(at-complete.js)·`#appDialog`(dom-utils.js)는 이 스택 밖이다 — 그대로 둘 것.
- `quit.js` (v3.3.4) — 종료 플러시만 담당하는 작은 모듈: `wmhh://request-quit` 를 받아 `STORE.flush()` 후 `quit_now`. 위 "종료 플러시" 항목 참조.
- `state.js` — `S` (items/fields/presets/idKinds/settings/recurDefs/loaded/lastId/imported), `CORE_FIELDS` + `DEFAULT_*`, `stripTemp()`/`backupObj()` (백업·비상 덤프 공용 페이로드 — v3.3.4), `newId()` (F12), `makeItem()` (single source of the item shape — new item fields get their default here), `toggleDone()` (domain op behind the card checkbox), `migrateItem()`, `reconcileCore()`. `S.recurDefs` is legacy-data pass-through only since v2.31 (see Data model). `S.imported` is the async handoff channel filled by `STORE.load()`/backup import and consumed by `reconcileImported()`.
- `dom-utils.js` — `$`, `esc`/`escAttr` (F8/F11), `enableDragReorder`, toast, `askNotify`.
- `recur.js` (주기 업무, reworked v2.4.0) — pure math (`isValidRecur`/`nextOccurrence`/`initialNext`/`recurLabel`) **plus the parent-child generator** `spawnDueOccurrences(items, now)`. Recurrence lives on a **parent** item (`it.recur` = `{type:"dow"|"monthly"|"every", dow/day/days, time, next, paused}`, stored in `items.recur` TEXT — reuses migration 004's column, no new migration). A parent lives *off the board* (`placeOf` returns `'recur'`; render/calendar/done filter it out) and holds only common info (memo + schedule). The generator (`spawnDueOccurrences`) runs a **weekly Monday-06:00 batch**: once now is past a Monday 06:00 it spawns every occurrence whose `next` is before the *following* Monday 06:00 (`nextMondaySix`), so a whole week's tasks appear Monday morning (each child keeps its own occurrence time as `f.due`). Each spawn creates a **child** — a normal board item with `recurId` = parent.id and `f.due` = the occurrence — then advances `next` (persisted, so no duplicate spawns; occurrences >14 days stale are skipped to avoid a flood). Time input accepts HH:MM or colon-less HHMM (normalized). Child↔parent are navigable both ways (`it.recurId` ↔ `items.filter(recurId===p.id)`). **v2.4.4: the `every` (며칠마다/N-day) input was removed from the UI — creation offers only 매주 요일(dow) / 매월 며칠(monthly). recur.js still handles `type:"every"` so already-stored `every` parents keep spawning/round-tripping (data compat); only the input dropdown dropped it.**
- `recur-box.js` (v2.4.0) — the **[설정] 메뉴 → 주기 업무 입력·관리** entry + modal: minimal parent form (memo + schedule) and the 주기 업무 관리 list (edit/pause/delete, next-occurrence, child count). No standalone board button — the old `[주기 입력]` capture-area button was removed in v2.4.0 (entry is the settings menu). Exposes `initRecurBox()` and `runRecurSpawn()` (called by main.js on load + every 60s).
- `settings-menu.js` — **v2.6.0: 헤더 [설정] 이 드롭다운에서 표준 팝업(`#settingsModal`)으로 바뀌었다.** 위쪽은 선택형 설정(화면 테마 메인/미니 창, 미니 창 시작 화면, 미니 창 Ctrl+Enter 동작)을 헤더 탭과 같은 알약형 세그먼트(`.seg`/`.seg-btn`)로, 아래쪽은 기존 실행 버튼(저장 위치/백업/불러오기/XLSX/프리셋·식별정보/주기 업무/보드 모드)을 담는다 — **버튼 id 는 불변이라 실제 동작 리스너는 각 모듈(backup.js·presets.js·recur-box.js·main.js)에 그대로 있다.** 설정 키 ↔ 세그먼트 ↔ 정규화 함수의 짝은 모듈 상단 `SEGS` 배열 한 곳에만 적는다(항목 추가 시 여기만 고친다). 선택은 즉시 저장 + `applyTheme()` + `sendCaptureConfig()` 로 반영하고 팝업은 열어둔다(보드 모드 모달과 같은 규칙). (The two Everything toggles were removed in v2.4.0.)
- `theme.js` (v2.6.0) — import 없는 순수 모듈. `THEME_DEFAULTS` + `normTheme`/`normCapTheme`/`normCapStart`/`normCapSubmit`(아는 값만 통과, 나머지는 기본값) + `captureConfig(settings)`(미니 창에 보낼 세 값만 추림) + `applyTheme(settings)`(`html[data-theme]`). **다크/화이트는 CSS 토큰 교체로만 구현한다** — 컴포넌트 규칙에 색을 하드코딩하지 말 것(`styles.css :root` ↔ `html[data-theme="dark"]`, `capture.html :root` ↔ `body.light`). **v2.6.2 `cacheForBoot()` + `theme-boot.js`:** 설정을 DB에서 읽기 전 한 프레임 동안 쓸 값을 localStorage 에 캐시한다(없으면 기본값). `theme-boot.js` 는 index.html `<head>` 의 **클래식** 스크립트여야 한다 — CSP `script-src 'self'` 라 인라인은 차단된다. 미니 창도 init 에서 같은 캐시를 읽어 구성 도착 전 깜빡임을 없앤다. **이 캐시는 페인트 전용이다 — 어떤 로직도 이 값으로 판단하지 않는다(설정의 진실은 SQLite 하나).**
- `filters.js` — `haystack()`/`textMatch()` search predicates (pure functions, no state/DOM; board search and done search share them, and future saved-filter views build on them). Since v3.0.0 the haystack includes linked file *names* (last path segment only, not folder names).
- `phonebook-core.js` (v2.7.0) — **순수 모듈**(상태·DOM·Tauri 없음): 전화번호부 중복 판정(`entryKey` — 전화는 숫자만 비교)·아이템 관련인 수집(`gatherFromItems`)·검색(`matchEntries`, 빈 검색어=빈 목록)·@ 토큰 파싱(`atToken` — 줄 시작/공백 뒤 @만, 이메일 무시)·삽입(`applyInsert`/`entryLabel`)·엑셀 행 매핑(`mapSheetRows` — 헤더는 2개 이상 열이 맞는 줄 우선, "연락처 목록" 같은 제목 줄 오인 방지). 순수라서 **capture-win.js 도 import 한다**(금지 규칙의 이유인 최상위 부작용·이중 모듈 상태가 없음 — 이 성질을 깨지 말 것). **v3.5.0 이메일(선택) 두 규칙 — 이 파일이 원본이다.** ① **이메일은 `entryKey` 에 넣지 않는다**(따라서 `isComplete`·`relatedItems`·`entryLabel`/`tagText` 도 전부 불변): 선택 필드를 키에 넣으면 '빈 이메일 vs 채운 이메일'이 불일치가 되어 나중에 이메일만 채운 같은 사람이 갈라지고 관련 업무가 안 걸린다. ② 그래서 같은 사람으로 접힐 때 **빈 칸이면 채우고 이미 값이 있으면 안 건드린다**(`fillEmail` — 보강 규칙의 단일 출처; `absorbContacts`·`gatherFromItems`·`mapSheetRows`·`merge.js` 가 모두 이걸 쓴다). ①만 하고 ②를 빼면 **이메일이 조용히 사라진다** — entryKey 가 같아 건너뛰는 경로가 셋이라 이미 있는 사람의 이메일은 자동 경로로 영영 못 들어온다. 덮어쓰기는 전화번호부 화면의 [수정]에서만. `absorbContacts` 의 `updates` 는 v3.5.0부터 **'바뀔 칸만 담은 패치'**(`{id, phone?, email?}`)라 호출부는 들어 있는 키만 덮어야 한다(없는 키까지 대입하면 undefined 로 지운다). **검색은 '보이는 곳에서만'**: 이메일을 표시하는 전화번호부 탭(`phonebook.js matches()`)·업무 검색(`filters.js`, Rust `quick_search`)은 이메일로도 찾고, 3칸만 보여주는 @ 자동완성(`matchEntries`, Rust `search_phonebook`)은 찾지 않는다 — 안 보이는 값으로 걸리면 "왜 이 사람이 떴지"가 된다.
- `phonebook.js` (v2.7.0) — 전화번호부 탭(`#view-phone`, 4번째 탭): 목록(소속→이름 정렬로 *파생* — 배열 순서는 저장 순서일 뿐)·직접 입력(추가/수정/삭제)·[아이템에서 가져오기](자동 동기화 아님 — 명시적 1회 흡수, 소유자 지정)·[엑셀 불러오기](`<input type="file">`+FileReader+전역 XLSX). `adoptPhonebook()` 이 로드/백업 복원분을 정규화하고 id 없는 항목에 `newId()`를 채우며 기존 id 로 `S.lastId` 를 시드한다(F12). 저장은 `STORE.savePhonebook` 통째 교체. **v2.9.0 무결성 규칙(소유자 지정): 3칸(소속·이름·연락처) 완비만 입력 가능**(`phonebook-core.isComplete`) — 모든 경로(직접·엑셀·새로고침·자동 연동) 공통, 구버전 불완전 항목은 pass-through 보존. **@태그 클릭 → 관련 업무 팝업**(`openRelated`/`#relModal`, z-index 70 — 양식 위): v2.9.0부터 클릭 대상은 **양식 메모 아래 태그 칩(`#fm-tags`, form.js renderFmTags)뿐**이다(카드 위 태그는 색 표시만 — 카드 열기 클릭과 겹치던 오동작 제거); **v2.11.0: 칩 줄 폐지 → 본문 하이라이트**(소유자 지정): form.js `renderMemoHl` 이 textarea 와 같은 메트릭의 백드롭 층(`#fm-memo-hl`)에 linkifyAt 결과를 깔아 태그 자리만 배경이 비치고(⚠️ 백드롭 태그에 font-weight·padding 주면 글자 폭이 어긋난다 — styles.css 주석 참조; 스크롤바 폭은 scrollbar-gutter:stable + 백드롭 padding-right 보정), 클릭은 `tagAtCaret`(캐럿이 태그 구간 위) → openRelated. **태그는 전화번호부 실존 관련인만**(`linkifyAt(escaped, book)` — @홍길동→@홍길 처럼 한 글자만 지워도 해제). **v3.0.2 통일 정책(소유자 지정): 태그는 어디서든(바로 입력·미니 창 빠른 메모·양식 메모) 하이라이트·hover·클릭→검색·관련인 자동 첨부가 동일하게 동작한다.** 클릭은 캐럿이 아니라 **hover 기하 판정(.at-tag.hover)** — 줄 끝 클릭의 캐럿 스냅 오작동 방지. 관련 업무 매칭 기준은 **하나뿐**이다(v3.1.1 소유자 지정 — `relatedItems(items,{entries})`): 업무의 관련인 중 **관련소속·관련인·연락처 3칸이 그 전화번호부 항목과 모두 같은** 사람이 있을 때만. 메모 @태그 경로는 폐지(동명이인이 걸리고 화면 설명과도 어긋났다 — @태그로 등록한 업무는 등록 시 관련인이 자동 첨부되므로 이 기준만으로 걸린다). 팝업 설명 문구도 이 규칙 그대로 적는다. 미니 창 태그 클릭은 검색 화면 점프. 목록 정렬은 **엮인 업무 수(relCount, relatedItems 기준) → 소속 → 이름**이고 행마다 `업무 N` 배지·hover 리프트(행 클릭=관련 업무 팝업); 검색은 이름 OR 연락처 숫자 비교(`phonebook-core.relatedItems`). 팝업 행은 `data-open` 이라 render.js 의 기존 카드-열기 위임이 양식을 열고, phonebook.js 는 팝업만 닫는다(모듈 순환 없이 재사용). **자동 연동(v2.9.0)**: 양식 저장 시 form.js 가 `absorbIntoPhonebook(contacts)` 호출 → `phonebook-core.absorbContacts` 가 완전 중복 건너뜀·빈 번호 보강·다른 번호는 새 항목을 계산(삭제는 비전파); 빠른 메모는 form.js `contactsFromTags`(extractTags+entriesForTag)가 태그의 관련인을 등록 시 자동 첨부. [새로고침](구 [아이템에서 가져오기])·엑셀은 confirm 대신 표준 모달 `#pbSyncModal` 로 확인. **v3.1.0 [엑셀 양식]**(`#pb-tmpl` → `saveXlsxTemplate`): 제목 줄(`소속`·`이름`·`연락처`)만 있는 빈 xlsx 를 `save_binary_file` 로 저장한다 — 제목 문구는 `mapSheetRows` 의 헤더 정규식에 맞춰야 왕복이 성립하므로 바꿀 때 둘을 함께 본다. 예시 행은 넣지 않는다(안 지우고 불러오면 가짜 관련인이 등록된다). 바이트는 XLSX 내보내기와 같은 F14 규칙(`new Uint8Array(XLSX.write(...))`).
- `at-complete.js` (v2.7.0) — @ 자동완성 드롭다운(`#atDrop`): 바로 입력(`#inp`)·양식 메모(`#fm-memo`) 모두 **완성형 `@김철수` 태그만 삽입**(v2.9.0 — 괄호 정보 병기 폐지: 소속·번호는 등록/저장 시 관련인으로 자동 첨부되므로), 양식 메모는 추가로 관련인 행 채움(같은 사람 중복 선택은 form.js `fillContactFromEntry` 의 entryKey 비교로 1번만), 양식 관련인 칸(**이름·연락처만** — 관련소속은 v2.10.0부터 자동완성 제외, 소유자 지정)=@ 없이 입력값 검색→그 행 3칸 채움. **문턱값(v2.10.0, `phonebook-core.queryReady`, 미니 창 공통)**: 이름 2글자·전화 숫자 3자리부터, 단 `010` 단독은 열지 않음. `initDtDelegation` 식 **문서 위임**이라 form.js 가 이 모듈을 import 하지 않는다(새 순환 없음 — 이쪽이 form.js `fillContactFromEntry` 를 일방향 import). 키 처리는 캡처 단계(관련인 행 Enter=다음 행, ESC=양식 닫기보다 먼저 먹어야 함), textarea 커서 좌표는 미러 div 기법. 미니 창은 이 모듈을 쓰지 않고 capture-win.js 자체 구현 + Rust `phonebook_search`(읽기 전용, quick_search 와 같은 이유로 존재)를 쓴다.
- (`everything.js` — **removed in v2.4.0**. The Everything(voidtools) file-search integration was deleted because it slowed the board/quick-memo search.) File links are **not shown on board cards since v2.4.1** — cards render memo(2 lines)·earliest sub·due only; files live in the form popup (`form.js` `addFormFileRow`) where each row has an active↔edit toggle (active = name shown as a link that opens on click / edit = editable path input + a **찾기** button calling `pick_file_path`).
- `styles.css` — single global stylesheet. **화면 디자인 규칙(색 토큰·카드 3줄 원칙·양식 정렬·표준 모달·560px 반응형·`font` 축약형 함정 등)은 `디자인 정책.md`에 정책화돼 있다 — 새 UI를 추가/수정하기 전에 읽고 따를 것.** **v2.4.5 디자인 리뉴얼:** 팔레트가 뮤트 틸 액센트(`--slate`/`--focus`/`--amber` = `#4d938a`) + 따뜻한 뉴트럴 배경(`--paper:#f4f3f0`) + 흰 카드. 단계 표시는 카드 좌측 풀바 → **`.card::before` 짧은 색 세그먼트**(`--stage-*`). 원형 체크박스(`.chk` border-radius:50%), 알약형 탭/태그(999px), 소프트 hover 섀도. 색은 전부 CSS 변수라 팔레트 변경은 `:root` 토큰만 고치면 전파된다. (`.adot` 3단계 알람 점 규칙은 그대로 — render.js가 emit.) **v2.5.12:** 카드는 메모 **본문 첫 줄만** 가져와 보여주고(나머지 줄은 카드를 눌러 양식에서 확인), 그 첫 줄이 길면 `.card-memo` 2줄 클램프까지 감싼다 — v2.5.11의 줄바꿈 전체 보존(`white-space:pre-line`)은 철회됐다. **v3.3.5 좁은 폭 규칙:** 양식의 날짜·시각 줄(`.dt-inp`)은 자식이 전부 고정폭이라 `nowrap` 이면 244px+들여쓰기 43px 이 절대 줄지 않아 CSS 폭 371px 아래에서 시각칸이 패널 밖으로 잘렸다 — `flex-wrap:wrap` 으로 자리가 없을 때만 접히게 했다(375px 이상은 예전과 동일). **고정폭 입력을 한 줄에 나열할 때는 `min-width:auto` 때문에 shrink 가 무력하다는 것을 기억할 것** — 접히게 하거나 `min-width:0` 을 함께 주지 않으면 좁은 폭에서 반드시 삐져나온다. 메인 창에는 `minWidth` 가 없어(tauri.conf.json) 폭이 300px 대까지 내려갈 수 있으니, 새 레이아웃은 560px 이 하한이라고 가정하지 말 것. **v2.5.4:** `.card-memo` 2줄 클램프(세부 1·시각 1과 함께 카드 최대 4줄); 세부(▸)·시각(알람점) 줄은 앞 마커를 9px 고정폭 슬롯(`.sub-mark`)으로 맞춰 들여쓰기 통일; 시간·담당자 5열 전용 색(`--own-metoday/othtoday/meplan/othplan` = 남색·자주 계열, 기존 단계색과 비겹침 — 카드 세그먼트+`#view-board5` 헤더 밑줄·점에 nth-child로 적용); 알림창(`.alarm`/`.a-item`) 좌측을 카드식 `::before` 짧은 세그먼트로(빨강 유지); `body{overflow-x:clip}`로 가로 스크롤 제거; `textarea#inp` 배경 순백 + `height`→`min-height`(form.js `autoGrowInp`가 내용 따라 세로 확장).
- `placement.js` — `placeOf()` / `dayBounds` / `PLACE_NAME` (the core scheduling logic). **v2.5.0 board modes:** the module stays import-free; `setPlaceMode('time'|'owner')` + `placeMode()` hold module-local mode state (main.js sets it from `settings.boardMode` after load and on the header `#modePill` toggle; default `'time'` keeps the legacy 4-column logic byte-identical). In `'owner'` mode `placeOf` returns `'metoday'|'othtoday'|'meplan'|'othplan'` (5-column view `#view-board5`): assignee via `ownerOf(it)` = earliest valid pending-mid sub's `owner` → `''`(본인) — v2.5.2부터 item-level owner는 배치에 안 쓰는 legacy pass-through(UI 제거, 데이터만 보존); reference time = earliest of pending mids ∪ due. **v2.5.18:** both modes now route no-time items to `'inbox'` via `noTimeSet()` — see "Placement order" under Data model for the exact sequence and why the `done` guard is repeated in owner mode. Placement stays derived-only, so switching modes never mutates data.
- `datetime.js` — date/time parsers (F3/F4/F13), dt input widget helpers, `DOW`/`fmtT`/`fmtDue`, `initDtDelegation()` (document-level delegated listeners for the widget).
- `store.js` — `invoke` (from `window.__TAURI__.core`; `withGlobalTauri: true`), `STORE` persistence facade (single-flight `saveAll` queue, F1 gate on `S.loaded`). **v2.5.19 저장 실패 표시:** a background save can fail silently (disk full, split data dir, AV lock, integrity gate); `saveAll` now calls `clearSaveError()` on success and `showSaveError()` on failure (both in `dom-utils.js`, toggling the `#saveAlert` banner), and the sidecar saves (`saveFields`/`savePresets`/`saveIdKinds`/`saveSettings`) call `showSaveError()` on failure too. Clearing is owned by `save_all` success ONLY — a sidecar success must not hide an items-save failure. This is the deliberate re-introduction of a save-failure signal after v2.31 removed the old `saveStatus` indicator; "조용한 성공 / 시끄러운 실패".
- `form.js` — quick input (`toInbox`) + form panel; `editingId` is module-local, reset only via `closeForm()`. **v2.5.22 임시저장:** the form auto-drafts into `settings.formDrafts[key]` (key = item id, or `'new'`) 700ms after input — `fillForm()` (split out of `openForm`) can repaint the panel from either the item or a draft because `collectForm()`'s output shape IS `openForm`'s input shape. Drafts never touch `S.items` — only 저장 does. `dropDraft()` (최종 저장·되돌리기) nulls `draftKey` so the `closeForm()` flush can't resurrect the draft; `baseline` (JSON of the form right after loading the saved version) is what decides dirty-ness, and a draft equal to baseline deletes itself. `#fm-cancel` was replaced by `#fm-revert` (되돌리기 = reload from the saved item + drop the draft). Ctrl+S / Ctrl+Enter both save (main.js, 양식 → 주기 업무 → 프리셋 order); the sub-task/contact row Enter-adds-a-row handlers bail out when Ctrl/⌘ is held so the two don't collide.
- `presets.js` — preset buttons + management modal + id-kind name management.
- `merge.js` (v2.8.0) — **순수 모듈**: `mergeItems(받는쪽, 끌려온쪽)` 병합 규칙 — id·알람·담당은 받는 쪽, 메모 이어붙임, 접수·마감은 이른 쪽(마감이 바뀌면 F2 재무장), 세부할일은 점검시각 오름차순(무시각은 뒤·안정 정렬), 관련인(entryKey)·식별정보·파일 중복 제거, staged 는 둘 다 대기일 때만 유지. 원본 불변·새 객체 반환.
- `render.js` — `render()`/`renderDone()`/`cardHtml()`/`persist()`; search state `q`/`dq` module-local (matching itself delegates to `filters.js`). Board columns sort by earliest arriving timestamp (pending sub `mid`s + `f.due` merged, ascending; no-time items last, newest first) since v2.3. **v2.8.0 메모 합치기 드래그**(`initMergeDrag`): 보드 두 뷰의 카드만 대상(mousedown 때 draggable 을 켜는 enableDragReorder 식 — 캘린더 상세 유령 드래그 방지), 끌던 id 는 dataTransfer 가 아니라 모듈 상태 `dragMergeId` 로 넘긴다(jsdom 테스트 가능). 병합 후 [실행 취소] 토스트가 두 원본을 복원하고, 60초 자동 재렌더는 `.card.dragging` 존재 시 건너뛴다(재렌더가 DOM 을 갈아엎어 드래그를 끊으므로).
- `calendar.js` — month grid + day detail; `calY/calM/calSel` module-local.
- `alarms.js` — `checkAlarms()` 20s poll (F5), beep, title flash, alarm toggle.
- `backup.js` — JSON backup/restore, `reconcileImported()`, XLSX export (uses global `XLSX` from the classic `vendor/xlsx.full.min.js` script tag, which must stay a non-module script loaded before `main.js`), data-dir change.
- `main.js` — entry (`<script type="module">` in index.html): wiring order, tabs, Ctrl+S/ESC (F14), clock, the initial-load IIFE (`STORE.load` → `migrateItem` → `S.loaded=true` → `reconcileImported` → pending-merge → `render`). **v3.6.4: 화면·팝업 목록을 더는 들고 있지 않는다** — 탭은 `showView()`, ESC 는 `closeTop()`, Ctrl+S/Ctrl+Enter 는 `topSave()` 로 위임한다. 새 화면·새 팝업 때문에 이 파일을 고칠 일은 없어야 한다.
- `capture-win.js` / `capture-boot.js` / `capture-bridge.js` (v2.23 global-shortcut mini capture; shortcut fixed to Ctrl+Alt+Space since v2.31, no settings UI). **v2.6.0: 미니 창의 테마·시작 화면·Ctrl+Enter 동작은 설정에서 온다** — 미니 창은 DB를 직접 읽지 않는다(설정의 소유자는 메인 창 하나). 미니 창이 부팅·포커스 때 `wmhh://capture-hello` 를 쏘면 메인이 `wmhh://capture-config` 로 `{capTheme,capStart,capSubmit}` 를 내려주고(`capture-bridge.js sendCaptureConfig`, 설정 변경 시에도 즉시 push), `applyCaptureConfig()` 가 `body.light` 토글·시작 모드·힌트 문구를 맞춘다. **v2.6.3: 화면 배치가 3P2(첫 화면 `capStart` × 둘째 화면 `capSecond`, 각각 'search'|'memo'|'form')로 바뀌고 `capSubmit`·`capTheme` 설정은 삭제됐다 — 테마는 앱 전체 하나(`theme`)이고 미니 창도 그 값을 따른다(세 화면 색 통일, 소유자 지정). 한 쌍이 겹치지 않도록 `theme.js normCapPair()` 가 항상 유효한 조합으로 정규화한다.** `'form'`(양식 메모)은 미니 창을 아예 띄우지 않고 메인 창 양식을 여는데, 이 분기는 **Rust `show_capture_window`** 에서 한다 — 웹뷰에서 갈라내면 미니 창이 떴다 사라지는 깜빡임이 생긴다. Rust 는 DB 를 읽지 않고 `set_capture_form_mode` 로 받은 값을 `CAPTURE_FORM_MODE`(AtomicBool)에 캐시하며, 프런트가 `sendCaptureConfig()` 에서 매번 함께 밀어준다(로드 직후 + 설정 변경 시). 단축키를 받으면 메인 창을 띄우고 `wmhh://open-blank-form` 을 보내 `openForm({})` 이 열린다(저장은 사용자 몫). **v2.6.6: 화면 비우기는 '감출 때'(`hideWin` → `resetSearchUI`, Rust 토글 숨김은 `wmhh://capture-hidden`) 한다** — 숨는 것뿐이라 DOM 이 남으므로, 다시 뜬 뒤에 비우면 지난 검색어가 한 프레임 번쩍인다. **v2.6.5: 화면 초기화 시점은 '창이 새로 뜰 때' 하나뿐이다** — Rust `show_capture_window` 가 `wmhh://capture-shown` 을 보내고 그때만 `openFresh()`. 웹뷰 `focus` 로 초기화하면 다른 창을 보고 돌아올 때마다 검색어가 지워진다(`focus` 는 구성 갱신·커서 복구만). 검색 목록을 다시 그릴 때는 `selectedId()` 로 고른 업무를 id 로 기억해 선택을 이어간다 — 디바운스가 뒤늦게 터져 선택이 첫 항목으로 튀던 문제. **v2.6.4: 창이 뜬 직후(450ms) 들어오는 `blur` 는 무시한다** — 다른 앱이 뜨며 포커스를 가져가면 창이 떴다 사라지던 경합. Rust 토글도 `is_visible() && is_focused()` 로 바뀌어, 뒤에 남은 창은 숨기지 않고 앞으로 끌어온다. **v2.11.0: 검색 모드 창 높이는 결과 수에 맞춰 동적**(빈 결과=컴팩트 150, 결과당 +33, 최대 406 — 빈 목록이 거대한 3단 공백으로 보이던 문제의 근본 해결; `searchHeight()`). **v3.0.2: 힌트줄은 맨 아래 푸터**(입력칸·결과 사이에 끼어 3단으로 보이던 원인 — 검색은 입력/결과 2단). 🚨 **v3.4.8 (여섯 번 헛짚은 끝에 찾은 진짜 원인): 웹뷰의 실제 배율 ≠ 앱이 건 배율(uiScale).** Windows 접근성의 **'텍스트 크기 조정'**(설정 → 접근성 → 텍스트 크기, 100~225%)이 Chromium/WebView2 에서는 **페이지 전체 확대로 함께 곱해진다**. 소유자 기기는 135% 라 실제 배율이 1.2×1.35=1.62 인데 앱은 1.2 로 알고 창을 만들어, 창이 필요한 것보다 25% 작아 목록 아래가 잘렸다(실측: 창 840×347 물리 px = CSS 519×214, 앱은 700×289 인 줄 앎). 브라우저 프로브에는 이 설정이 없어 재현되지 않는다 — **미니 창 크기 문제를 브라우저 렌더만으로 검증했다고 판단하지 말 것.** 지금은 프런트가 `sizeWin()` 으로 자기 CSS 뷰포트 폭(`innerWidth`)을 함께 보내고 Rust `note_capture_ratio` 가 '창의 논리 폭 ÷ 그 값'으로 **실제 배율을 재서** 창을 만든다(원인이 텍스트 배율이든 DPI든 zoom 이든 결과가 곧 답이다). 확인 방법: `HKCU:\Software\Microsoft\Accessibility` 의 `TextScaleFactor`. 🚫 **v3.4.6 (반복 실패 끝에 정한 규칙): 창 크기를 '재고 → 바꾸고 → 다시 재서 보정하는' 왕복을 만들지 말 것.** 창 크기는 네이티브가 나중에 바꿔 주므로 그 결과를 다시 재서 판단하면 리사이즈 지연에 좌우된다 — 지연이 80ms 를 넘는 기기에서만 깨져 개발 기계에서는 멀쩡해 보인다. v3.2.1~v3.4.5 가 이 왕복을 여섯 번 고쳐 쓰며 빈 띠 → 마지막 행 잘림 → 첫 행 잘림 → 창이 펴졌다 접힘을 차례로 만들어 냈다. 지금은 행 높이를 CSS 에서 고정(`.cap-pb-it height:33px`)하고 `pbBoxH(n)` 이 필요한 높이를 산술로 구해 **한 번만** `resize_capture` 를 부른다 (재는 값은 창 크기와 무관한 입력줄·힌트줄뿐). 행 사이 간격은 마진 상쇄로 1px 이고, 10행에서 잘릴 땐 목록 아래 padding 을 더하지 않는다 — 더하면 11번째 행이 빼꼼 보인다. CSS 상수(height/margin/padding)를 고치면 `capture-win.js` 의 `PB_*` 상수도 같이 고쳐야 한다. **v3.0.4/v3.1.0: @ 자동완성 목록(`#cap-pb`)의 표시는 CSS(`body.pb:not(.search):not(.flash)`)가 유일한 주인이다** — JS 가 인라인 `style.display` 로 펴고 접던 시절엔 그 인라인이 'block' 인 채 남아 검색 화면이 다시 3단(입력칸·빈 띠·결과)으로 갈라졌다. 새 상태를 만들 땐 클래스로만 갈라라(`closePb` 는 pbOpen 과 무관하게 항상 내용·클래스를 비운다). 검색 결과는 `↑`/`↓`+`Enter` 로 조작(`selIdx`/`.sel` — 화면 상태일 뿐 데이터와 무관). **v2.6.7: 자동 선택 없음(검색만 하면 하이라이트 0개) + 끝에서 순환하지 않고 멈춤 + hover 는 `body.mouse` 일 때만(창이 커서 밑에 떠서 엉뚱한 줄이 켜지던 문제)**, 아무것도 안 고른 채 `Enter` 는 첫 결과를 연다, 빠른 메모는 `Ctrl+Enter`/`Ctrl+S` 공용(`submitMemo`). **v2.5.21: 기본 모드가 검색으로 바뀌었다** — 창이 뜰 때(`init` + window `focus`)마다 `openFresh()`가 검색어를 비운 검색 모드로 초기화하고, Alt가 빠른 메모로 토글한다(메모 초안 textarea는 건드리지 않아 Alt로 넘어가면 이어 쓴다). 두 입력칸에는 **placeholder를 두지 않는다** — 빈 칸의 회색 문구를 실제 글자로 오해해 "그 단어 뒤로 커서가 안 간다"는 혼선이 있었다(placeholder는 값이 아니므로 클릭해도 캐럿은 항상 0). 안내는 입력칸 바로 아래 힌트줄 `#cap-hint`가 모드별 문구(`HINT`)로 표시한다. — `capture-win.js` runs ONLY in the capture webview and must not import main-app modules (store.js's top-level `__TAURI__` destructure would break, and module state would run twice); it emits the memo text as an event instead of saving. `capture-boot.js` is the capture.html-only entry so `capture-win.js` stays importable in tests. `capture-bridge.js` is the main-window side: receives the event and feeds `captureMemo()` so the F1 load gate/save queue/pending-merge all apply, plus the one-time hidden-to-tray notice toast; access `__TAURI__.event` lazily inside functions, never at module top level.

**Drag & drop caveat (v3.0.0):** the main window sets `"dragDropEnabled": false` in `tauri.conf.json`. On Windows WebView2 this is REQUIRED for HTML5 drag-and-drop (`enableDragReorder` — sub-task/preset/id-kind reordering) to work at all; the trade-off is that native OS file-drop onto the window can never be used. Don't re-enable it.

Removed in v2.31 (kept here so old references make sense): `recur-box.js` (정기함 modal), the `state.js` recurrence generator (`reconcileRecur` etc.), the `saveStatus`/`setStatus` save-failure indicator (a new, simpler save-failure banner `#saveAlert` was reintroduced in v2.5.19 — see `store.js` above), and the ⚡ 빠른 메모 settings modal (shortcut recorder + tray/autostart toggles).

## File layout (line ranges are for `legacy/뭐해야 했더라v1.41.html`)

- **Lines 1–9**: `<head>` meta/fonts.
- **Lines 10–34**: inlined SheetJS library (vendored, minified — do not hand-edit).
- **Lines 35–367**: `<style>` — CSS custom properties for the color palette (`--stage-inbox`, `--stage-today`, `--stage-doing`, `--stage-planned`, defined near the top of `:root`) drive both column dots and card left-border colors.
- **Lines 369–546**: HTML body — header/toolbar, the "바로 입력/양식 입력" capture section and form panel, the search strip, the board (4 columns), the calendar view, and the completed-items view.
- **Lines 550+**: app `<script>`, organized into clearly delimited `/* ===== */` sections (search for these to navigate):
  - **저장 계층 (storage layer)** — `STORE` object wrapping IndexedDB (`wmhh-db`), with a `localStorage` mirror as a secondary safety net and a one-time migration path from older `deskflow_*`/`wmhh_*` localStorage keys into IndexedDB.
  - **필드 정의 (field definitions)** — `CORE_FIELDS` (접수시각/마감시각) plus user-defined custom fields, merged via `reconcileCore()`.
  - **날짜/시간 유틸** — parsers for split date/time text inputs (multiple date formats accepted).
  - **자동 배치 규칙 (auto-placement rules)** — `placeOf(item)`, the core scheduling logic that decides which board column an item belongs in (`inbox`/`today`/`doing`/`planned`/`done`) purely from its due date and sub-task check-in times ("mid"). This is the most important function to understand before touching board behavior.
  - **프리셋 (presets)** — reusable form templates.
  - **바로 입력 / 양식 패널** — the two capture flows (freeform memo vs. structured form with contacts/IDs/sub-tasks).
  - **렌더 (render)** — `render()`, `cardHtml()`, `renderDone()`, `renderCal()` regenerate DOM from the `items` array; there's no virtual DOM, everything is innerHTML-based.
  - **알람 (alarms)** — `checkAlarms()` polls every 20s, comparing due/mid timestamps against now, using per-item `al` state to avoid re-firing; uses the Notification API + a beep + title-bar flashing as a fallback. **Card dots (`alarmDot` in render.js) were simplified to 3 states in v2.4.4: ○ ad-wait(대기) / ● ad-ring(울림, passed & unacked/snoozed) / ● ad-done(확인함, `al===true`); the old 미룸/snooze color (ad-snooze) was dropped — snoozed alarms fold into ad-ring. **v2.5.11 removed the snooze action itself** ('10분 뒤 다시' gone; the alarm popup is [확인] only), so new data never produces a snooze-until value — old ones stay readable and render as ad-ring. Done items/subs show no dot. `earliestSub` now shows a rung (passed-pending) sub before a future one so the red dot isn't hidden behind an upcoming check-in.**
  - **구버전 마이그레이션** — `migrateItem()` upgrades old item shapes (pre-v5: `who/org/phone` on the item's field object, `title/summary`, `notice/sr`) into the current shape (`contacts[]`, `memo`, `ids[]`).
  - **초기 로드** — the IIFE at the very end loads from IndexedDB, migrates, reconciles anything the user entered before load finished, and calls `render()`.

## Data model

An item (`it`) has: `id`, `memo`, `owner` (v2.5.0 담당자 — free text, `''` = 본인; DB `items.owner`/`subtasks.owner` via append-only migration 005; old DBs/backups default to `''`), `f` (field values, keyed by `FIELDS[].key`, e.g. `f.received`, `f.due`), `contacts[]` (`{who, org, phone, email}` — **`email` 은 v3.5.0 선택 항목이고 사람을 구분하는 데 쓰지 않는다**: 소속·이름·전화 3칸이 같으면 이메일이 달라도 같은 사람이다. migration 008 + `#[serde(default)]`, 구 데이터·구 백업은 `''`), `ids[]` (`{kind, val}` — identifier numbers like 입찰공고번호/SR번호), `subs[]` (sub-tasks: `{id, title, mid, done, al, owner}`), `files[]` (v3.0.0 — plain path strings linked to the item; stored in the `item_files` table; since v2.4.1 shown only in the form popup (not on cards), with a per-row active/edit toggle — active opens via the opener plugin, edit lets you retype the path or pick a new one), `done`, `staged` (true = still in 분류 대기/inbox), `al` (per-item alarm-fired state keyed by field name), `recur` (v2.4.0 주기 업무: on a **parent** definition = `{type,dow/day/days,time,next,paused}`; null on normal items — a non-null `recur` marks the item as an off-board parent, see recur.js), and `recurId` (v2.4.0: on a **child** = the parent's id it was spawned from; null for a normal/hand-made item). (Historically `recurId` was the v2.3 정기함 soft link; v2.4.0 revives the parent-child model using the same column.) The legacy `recur_defs` table + `S.recurDefs` still round-trip through `load_all`/JSON backups for old-data preservation (no longer the active mechanism — parents are plain items now).

Board placement is **always derived**, never stored: `placeOf(it)` recomputes the column every render from `done`/`staged`/`f.due`/sub-task `mid` timestamps relative to "now" and to today's day-boundaries (`dayBounds()`). If you change scheduling behavior, change `placeOf()`, not per-item state.

**Placement order (v2.5.18 — owner-specified; the order is load-bearing, don't reshuffle it):**

*4-column (`'time'`)* — first match wins:
1. due or a pending sub `mid` is today or already past → `today`
2. any sub is `done` → `doing`
3. `noTimeSet(it)` (no valid due AND no sub has a valid `mid`) → `inbox`
4. otherwise → `planned`

*5-column (`'owner'`)* — no `doing` column, so only two steps:
1. `noTimeSet(it)` **and** no sub is `done` → `inbox`
2. otherwise → `me|oth` × `today|plan`

**Why step 3 sits after step 2 (and why the owner mode repeats the `done` guard):** an item you've already started (any sub completed) must not fall back into the untriaged column when it has no dates. Without the `done` guard in owner mode, the *same* item lands in `doing` in 4-column but `inbox` in 5-column — it would visibly jump columns just from toggling board mode. `tests/unit/placement.test.js` pins this ("모드 전환 일관성"). `noTimeSet` counts a *completed* sub's `mid` as "time is set" (unlike `subMids`, which only collects pending ones), and treats a corrupt ISO as no-time (F7-family guard).

## Persistence & data safety

- Primary store is SQLite via `STORE` → `invoke()` (in the legacy app it was IndexedDB with a localStorage mirror).
- `S.loaded` is the load-gate flag (see `F1` comment; formerly `LOADED`) — saves are blocked until the initial load completes, specifically to prevent an empty in-memory `S.items` array from clobbering existing stored data on startup. Preserve this gate if you touch the save path.
- **Save-failure signal (v2.5.19):** the whole-dataset save runs in the background on every edit, so a silent failure could otherwise lose work with no user cue. `STORE.saveAll` surfaces failures via the `#saveAlert` banner (`showSaveError`/`clearSaveError`); it stays up until the next *items* save succeeds. Keep failures visible — don't revert this to a console-only `console.warn`.
- **번호표 = 낡은 화면의 덮어쓰기 차단(v3.3.7).** 저장이 *전체 교체*라, 오래된 상태를 들고 있는 화면이 저장 한 번만 해도 그 뒤 작업이 통째로 사라진다(실제 3회 발생). `meta` 테이블의 `data_version` 을 `load_all` 이 내려주고 `save_all` 이 `baseVersion` 으로 되돌려받아, 다르면 `SaveResult::Stale` 로 **거부**한다. 확인/쓰기/번호 올리기는 **한 트랜잭션**이어야 한다(따로 하면 둘 다 통과한 뒤 나중 것이 앞 것을 덮는다). 거부는 오류가 아니므로 Err 가 아니라 값으로 돌려주고, 프런트는 재시도하지 않는다 — 화면 내용을 `emergency_dump` 로 남기고 최신을 다시 읽는다. `backup::import_payload` 도 번호를 올린다. `baseVersion` 이 없으면(구버전 프런트) 확인 없이 저장한다. ⚠️ 저장 성공 시 `S.dataVersion` 을 새 번호로 갱신하지 않으면 두 번째 저장부터 전부 거부된다.
- **저장 실패 복구(v3.3.4) — 실패한 배치를 절대 버리지 않는다.** 예전 `saveAll` 은 `_pending` 을 비운 뒤 invoke 했고 catch 는 배너만 켰다 → 실패분이 증발했다(화면엔 남아 있으니 사용자는 모른 채 계속 작업 → 닫으면 유실). 지금은 실패하면 배치를 `_pending` 으로 되돌리고(단 더 새 배치가 있으면 그쪽이 이미 포함하므로 덮지 않는다) `RETRY_DELAYS`(1·2·4·8·15·30초, 이후 30초 유지)로 계속 재시도한다. 진입점은 `STORE._run()` 하나 — `saveAll`·재시도 타이머·`flush()` 가 모두 이걸 거치므로 동시에 두 번 저장되지 않는다. `_run()` 이 돌려주는 프로미스는 **거절되지 않는다**(실패는 배너·재시도로 다루지 호출한 화면 코드를 깨지 않는다). Rust `db::with_write_retry`(초 단위 이내)와 한 쌍이고 이쪽이 분 단위를 맡는다.
- **종료 플러시(v3.3.4):** 트레이 [종료]·창 닫기는 더 이상 `app.exit(0)` 를 직접 부르지 않는다. `commands::request_quit` 이 `wmhh://request-quit` 를 보내고 `src/quit.js` 가 `STORE.flush()` 후 `quit_now` 를 부른다. Rust 쪽 감시 타이머(`QUIT_FLUSH_GRACE_MS`)가 응답이 없어도 종료시키므로 **앱이 안 꺼지는 상태는 만들 수 없다** — 그 보장을 믿고 quit.js 는 어떤 실패도 종료를 막지 않게 짜여 있다. `request_quit` 의 반환값은 "이 호출이 절차를 시작했는가"이고, false 일 때 `prevent_close()` 를 부르면 창도 안 닫히고 앱도 안 꺼지므로 부르지 말 것.
- **비상 덤프(v3.3.4):** 연속 `DUMP_AFTER`(3)회 실패하면 `store.js` 가 `emergency_dump` 커맨드로 현재 데이터를 `<data dir>/emergency/wmhh_emergency_*.json` 에 떨군다 — SQLite 를 전혀 거치지 않는 마지막 그물. 페이로드는 `state.js backupObj()` 로 **JSON 백업과 같은 형태**여야 한다(그래야 [불러오기]로 그대로 복원된다). `backupObj`/`stripTemp` 가 state.js 에 있는 이유가 이것 — backup.js 에 두면 store.js↔backup.js 순환이 생긴다.
- **백업 호환 계약(불변):** `settings` 는 스키마가 아니라 **key/value 가방**이다(Rust `pub type Settings = serde_json::Map<String, Value>`, 테이블도 `settings(key,value)`) — 새 설정 키를 추가할 때 마이그레이션이 필요 없고, **모르는 옛 키는 버리지 않고 그대로 보존한다**(예: v2.31에서 사라진 `captureShortcut`). 복원은 관대한 파싱이다: 백업의 `v` 로 분기하지 않고, **빠진 섹션은 현재 값으로 채우고**(`backup.js` bkImp), 빠진 설정 키는 `reconcileImported()` 의 `Object.assign({},DEFAULT_SETTINGS,imported)` 가 기본값으로 메우며, 값이 이상해도 `theme.js` 의 `norm*()` 가 아는 값으로 접는다. 아이템은 `migrateItem()` 이 옛 모양을 승격한다. **임시 상태(`captureDraft`/`formDrafts`)는 내보내기·불러오기 양쪽에서 `stripTemp()` 로 제외**한다. 복원 자체는 `backup_import` 가 모든 테이블을 한 트랜잭션으로 처리하므로 실패 시 데이터는 그대로다. (다운그레이드 — 최신 백업을 옛 버전 앱에서 여는 것 — 은 보장 대상이 아니다.)
- **임시저장(v2.5.22~v2.6.2)의 안전 경계:** 양식 임시저장은 `settings` 테이블에만 쓰고 **`items` 는 절대 건드리지 않는다** — 보드/카드에 반영되는 유일한 경로는 [저장] → `persist()` → `save_all` 이다(그래서 아래 pre-shrink 스냅샷 보호를 그대로 받는다). 초안 저장도 `STORE.saveSettings` 를 타므로 **F1 게이트 안**이고, 실패하면 `#saveAlert` 배너가 뜬다. 설정은 저장 시 통째로 재작성되는 테이블이라 쓰기 압력을 두 가지로 제한한다: 직전과 동일한 내용이면 재작성 생략(`lastWritten`), 초안 총량 `DRAFT_BUDGET`(40만 자) 초과 시 오래된 것부터 폐기(단 `pruneDrafts(keep)` 로 편집 중인 초안은 보존). 최악의 경우 잃는 것은 '아직 저장하지 않은 초안'뿐이다.
- **진단 로그 `wmhh.log` (v3.3.6, 데이터 폴더 안):** 실행 시각·**실행 파일 경로**·업무 건수, 복구/가져오기/이동 사실, 종료 요청, **건수가 바뀐 저장**(`save items 18 -> 8`), 번호표 거부를 한 줄씩 남긴다(`db::log_line`, 1MB 초과 시 `.1` 로 1회 롤링). "데이터가 옛 상태로 돌아가 있다"가 세 차례 반복됐는데 `eprintln!` 은 exe 실행 시 사라져 원인을 좁힐 수 없었기 때문이다 — 실제로 이 로그가 v3.3.9 의 원인(`quit requested` 2초 뒤 `start`)을 바로 짚어줬다. **업무 내용(메모·이름·번호)은 절대 남기지 않는다 — 건수와 사건만.** 로그 실패는 무시하고 앱을 방해하지 않는다.
- **증분 저장 — 완료 업무는 다시 쓰지 않는다(v3.6.0).** 저장이 전체 교체라 비용이 **누적** 업무 수에 비례해 무한히 늘었다(실측 0.22ms×N: 1000건 265ms, 2000건 459ms). 월 75건씩 쌓이는 실사용에서 1년이면 체크박스 한 번에 0.3초씩 멈출 참이었다. 이제 `db::items::save_active_tx` 가 **`DELETE FROM items WHERE done = 0` + 쓸 행만 개별 삭제** 후 삽입한다 → 비용이 '지금 다루는 업무 수'에만 비례한다(누적 2000·활성 30에서 **459ms → 60ms**). **마이그레이션 없음** — 이미 있는 `done` 컬럼을 쓰므로 스키마가 한 글자도 안 바뀌고, 되돌리려면 호출을 `save_items_tx`(전체 교체)로 되돌리면 끝이다. ⚠️ **`save_all` 의 `deletedDone` 은 '완료 처리'가 아니라 '지워 없앤 완료 업무'다** — Rust 에게 '목록에 없다'는 *지웠다* 와 *원래 안 보내는 완료 업무다* 둘 다로 읽히는데, 미완료는 위 DELETE 가 전부 지우고 다시 넣으므로 모호함이 없고 **완료 업무를 지웠을 때만** 구분이 불가능하기 때문이다. 프런트는 `S.doneDirty`(state.js)에 표시하고 `store.js` 가 저장 성공 시에만 지운다(실패하면 남겨 재시도에 다시 싣는다 — `_pending` 과 같은 규칙). **이 설계는 '완료 업무는 수정되지 않는다'는 보장 위에 서 있다**: 수정이 가능하면 그 변경이 저장에 실리지 않아 조용히 사라지므로, 완료 업무 양식은 읽기 전용으로 열고, 고치려면 **카드의 체크박스로 완료를 취소**하게 한다(v3.6.1: 양식 안에 [되살려서 수정] 버튼을 뒀다가 없앴다 — 체크박스와 하는 일이 겹쳤다). 그 보장이 지켜지는 근거는 **`it.done` 이 바뀌는 곳이 `state.js toggleDone()` 한 곳뿐**이고 완료 업무가 목록에서 빠지는 경로가 삭제 핸들러 하나뿐이라는 것 — 새 코드가 이 둘을 늘리면 `S.doneDirty` 표시도 함께 늘려야 한다. ⚠️ **백업 복원 직후 `S.doneDirty` 를 반드시 비운다**(backup.js) — 복원은 DB 를 통째로 갈아끼우는데 옛 표시가 남으면 id 가 겹치는 엉뚱한 완료 업무를 지운다. 회귀 검사는 `node tools/save-perf-check.mjs`.
- **Pre-shrink snapshot (v2.5.20):** because `save_all` replaces the entire item set (delete+reinsert), a single bad save (frontend bug shipping a partial `S.items`, or a mass delete) can wipe everything. `db::save_items_guarded` forces a `wmhh_presave_*` backup before the overwrite when the count drops drastically; the Rust write path uses it instead of `items::save_items` directly. This is defense-in-depth on top of the F1 gate (which only covers the *startup* window).
- **Input length caps (v2.5.20):** free-form text inputs carry `maxlength` so a pathological paste can't bloat the single dataset that gets fully serialized on every save — memo 20,000→**50,000**, sub-task title 500, contact 관련인/소속 100 · 전화 40, 식별정보 명칭/번호 100, 담당 100, file path 1,000 (date/time inputs were already capped). `maxlength` constrains only new keyboard/paste input; a longer value already stored round-trips through load/save untouched, so existing data is never truncated.
- `newId()` (see `F12` comment) generates monotonically increasing IDs from `Date.now()`, bumping by 1 on collision within the same millisecond — do not switch this to `Math.random()` or a non-monotonic scheme, since `migrateItem()` seeds `S.lastId` from existing max IDs (both live in `state.js` for exactly this reason).
- Manual backup/restore is JSON via `doBackup()`/the `bkImp`/`bkFile` handlers (uses `showSaveFilePicker` when available, falling back to a download link), and there's a separate one-way XLSX export (`xlsx` button, via the vendored SheetJS). JSON backup is the only *round-trippable* format — XLSX is export-only.

## Conventions in this codebase

- Numbered inline comments like `F1`, `F5`, `F7`, `F12` mark specific past bugfixes/invariants (e.g. "F1: block saves before initial load to avoid data loss", "F12: monotonic id to avoid same-ms collisions"). Treat these as load-bearing constraints, not stylistic notes — read the comment before changing the code near it.
- Code is dense/terse by house style (short-circuit chains, ternaries, minimal whitespace) rather than heavily decomposed — match the existing style rather than introducing a different formatting convention for new code.
- All user-facing strings are Korean; keep new UI text consistent with the existing tone (plain, direct, no honorific/formal register beyond what's already there).
- **`ids[]` is called 식별정보 in the UI, everywhere** (unified in v2.5.14 — settings menu, 명칭 관리 modal, 주기 업무 안내, XLSX column header all used to say the older 식별번호). The storage side is untouched: the key stays `ids`/`kind`/`val` in the DB and in JSON backups, so this is a display-string rule only — don't rename data keys to match.
